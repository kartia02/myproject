# 검수 반영 — 200건 → 169건

`test.jsonl` 은 그대로 두고 `test_reviewed.jsonl` 을 새로 만들었다. 재현하려면 `python src/apply_review.py`.

| 판정 | 건수 |
| --- | --- |
| 맞음 | 156건 |
| 라벨수정 | 13건 |
| 버림 | 31건 |

| 층 | 남은 건수 |
| --- | --- |
| `ambiguous` | 7건 |
| `confusion` | 46건 |
| `multi` | 50건 |
| `out_of_scope` | 13건 |
| `simple` | 37건 |
| `tense` | 16건 |

## 라벨을 고친 13건

| id | 바뀐 값 | 사유 | 결정자 |
| --- | --- | --- | --- |
| `1332` | `caregiver` `아들` → `null` | caregiver: 아들 → null. '아들까지 봤다'는 돌봄 행위가 아니다 | 사람 |
| `1772` | `caregiver` `삼촌` → `null` | caregiver: 삼촌 → null. '삼촌 있는 데서'는 함께 있었을 뿐이다 | 제안 |
| `0439` | `since` `일주일째` → `어제 밤` · `until` `어제 밤` → `일주일째` | 발화 순서 규칙 — since:일주일째→어제 밤, until:어제 밤→일주일째. 검수 원판정은 '맞음'(라벨 그대로)이었다 | 사람 |
| `0098` | `until` `화요일` → `null` | until: 화요일 → null. 같은 값이라 구간이 아니다 | 제안 |
| `0693` | `since` `3일 전` → `어제` · `until` `어제` → `3일 전` | 발화 순서 규칙 — since:3일 전→어제, until:어제→3일 전. 이 행은 규칙 적용으로 모델이 오답이 된다(유리한 쪽만 고르지 않았다는 근거) | 사람 |
| `1381` | `time` `null` → `잠깐 전` | 잠깐 전 이라는 시간 때도 기록 하도록 한다 | 사람 |
| `0203` | `until` `엊그제` → `null` | until: 엊그제 → null. 같은 값이라 구간이 아니다 | 제안 |
| `2378` | `caregiver` `할머니` → `null` | caregiver: 할머니 → null. 할머니는 알려준 사람이지 준 사람이 아니다 | 제안 |
| `0235` | `since` `며칠 전` → `오늘 아침` · `until` `오늘 아침` → `며칠 전` | 발화 순서 규칙 — since:며칠 전→오늘 아침, until:오늘 아침→며칠 전. 검수 원판정은 '맞음'(라벨 그대로)이었다 | 사람 |
| `1525` | `caregiver` `아들` → `null` | caregiver는 1인칭으로 보이니 null이 맞는 듯 하다 | 사람 |
| `0889` | `until` `토요일 아침` → `null` | until: 토요일 아침 → null. 같은 값이라 구간이 아니다 | 제안 |
| `1246` | `food` `null` → `호두` | food가 호두여서 호두 한 줌 먹였음이라는 말이 나온 듯 하다 | 사람 |
| `0724` | `until` `토요일 아침` → `null` | until: 토요일 아침 → null. 같은 값이라 구간이 아니다 | 제안 |

## 버린 31건

| id | 사유 | 결정자 |
| --- | --- | --- |
| `0286` | '오빠가 있었던 일' — 반려견이 아니라 오빠의 하루를 묻는다 | 제안 |
| `0351` | 질문이 둘이고 since=오늘 / until=며칠 전 으로 구간이 뒤집혔다 | 제안 |
| `0358` | '2주간 이모 이틀 전 기억나나' — 무엇을 묻는지 알 수 없다 | 제안 |
| `0409` | 넥스가드(정제)에 '두 방울' | 제안 |
| `0475` | 넥스가드(정제)에 '한 스푼' | 제안 |
| `0520` | '와이프 몸무게' — 반려견이 아니라 사람의 몸무게다 | 제안 |
| `0549` | '아들한테 넥스가드 줬다' — 아들이 약을 받은 사람이 됐다 | 제안 |
| `0566` | '지난번엔'(과거)과 '안 난다'(현재)가 어긋나 회상인지 조회 요청인지 안 정해진다. 요청으로 읽으면 since='지난번'이 빠진 라벨 결함이 되어, 해석에 따라 정답이 갈린다 | 사람 |
| `0654` | 누나가 배변했다는 내용으로 보인다 | 사람 |
| `0815` | '언니 뭐 했더라' — 언니가 한 일을 묻는다 | 제안 |
| `0896` | '삼촌 있었던 일' — 삼촌의 일을 묻는다 | 제안 |
| `0898` | '언니 뭐 했는지' — 언니가 한 일을 묻는다 | 제안 |
| `0907` | '아내한테 무슨 일 있었는지' — 아내의 일을 묻는다 | 제안 |
| `0994` | 내용이 망가진 것처럼 보인다 | 사람 |
| `1103` | 내용이 망가진 듯 보인다 | 사람 |
| `1118` | 아까 먹은 거, 산책의 연관관계를 알수 없다 | 사람 |
| `1204` | 반려견과 관련 없는 내용인 듯 하다 | 사람 |
| `1223` | 이거 반려견이 아니라 사람이 물 마신 내용을 기록함 | 사람 |
| `1288` | 반려견이 물을 마신 기록이 아니다 | 사람 |
| `1467` | 사용자는 몸무게를 1.8kg 이런 식으로 말한다 | 사람 |
| `1531` | 반려견이 물을 마셨다는 내용이 아닌 듯 하다 | 사람 |
| `1650` | 반려견과는 관련없는 내용이다 | 사람 |
| `1850` | log_water 주어 결함 — 사람이 마신 문장 | 사람 |
| `1869` | 삼촌이 물을 마셨다는 내용으로 보인다 | 사람 |
| `1938` | log_water 주어 결함 — 사람이 마신 문장 | 사람 |
| `1966` | log_water 주어 결함 — 사람이 마신 문장 | 사람 |
| `2050` | '오타났지만 숫자는 맞음' — 뜻 없는 군더더기 (생성 규칙 8 위반) | 제안 |
| `2072` | log_water 주어 결함 — 사람이 마신 문장 | 사람 |
| `2120` | '정오에 저녁밥' — 시각과 끼니가 모순이다 | 제안 |
| `2208` | '4킬로 800' — 1467 과 같은 형식이라 같이 버린다 | 제안 |
| `2395` | '정리해뒀다' — 이미 끝냈다는 진술이라 조회 요청이 아니다 | 제안 |
