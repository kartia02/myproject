# Explainer — PawNote

이 문서는 **이 프로젝트가 무엇을 하는지 이해하고, 남에게 설명하기 위한** 것이다.

- 무엇을 만드는지의 **규격**은 [CLAUDE.md](CLAUDE.md)
- 무엇을 할지의 **실행 목록**은 [checklist.md](checklist.md)
- 왜 그렇게 정했는지의 **결정 근거**는 [context-notes.md](context-notes.md)
- **여기는 그 내용을 설명 가능한 형태로 다시 푼 것**이다. Day 6 README의 밑글이 된다

용어가 처음 나올 때마다 괄호로 뜻을 붙인다.

---

## 1. 한 문장

> **보호자가 말한 한마디를 병원에서 꺼내 쓸 수 있는 기록으로 바꾸는 온디바이스 모델.**

`온디바이스(on-device)` — 서버가 아니라 사용자 폰 안에서 직접 실행하는 것. 이 단어 하나가 "왜 작은 모델인가"를 미리 설명한다.

---

## 2. 전체 흐름 — 파인튜닝은 어디에 있는가

이 프로젝트가 만드는 것은 **화살표 하나**다.

```
보호자      "콩이 아침에 사료 반 그릇 먹었어"
                        ↓
   ┌────────────────────────────────────┐
   │  파인튜닝한 4B 모델  ← 이 프로젝트  │
   └────────────────────────────────────┘
                        ↓
   {"tool": "log_meal",
    "arguments": {"food": "사료", "amount": "반 그릇",
                  "time": "아침", "caregiver": null},
    "reason": null}
                        ↓
앱 코드      "아침" + 발화 수신 시각 → 2026-09-02T08:00 으로 저장
                        ↓
병원에서     "8월 19일부터 아침 사료를 남기기 시작했어요"
```

`Tool Call` — AI가 "이 기능을 이 값으로 실행해줘"라고 정해진 JSON 형식으로 내놓는 출력.

**앱도 DB도 음성 인식도 만들지 않는다.** 완성품의 핵심 부품 하나를 만들고, 그것이 GPT-4.1보다 싸고 빠르면서 쓸 만한지를 숫자로 증명한다. 나머지는 "이 부품이 들어갈 자리"로 문서에만 서술한다.

### 왜 음성 인식(STT)은 만들지 않는가

`STT(Speech-to-Text)` — 음성을 글자로 바꾸는 기술.

"말로 기록한다"가 이 프로젝트의 서사인데 음성 인식을 안 만든다는 것이 모순처럼 보인다. 그렇지 않다. 파이프라인을 층으로 나누면 분명해진다.

```
음성      "콩이 아침에 사료 반 그릇 먹었어"
            ↓  ① STT — OS 내장 기능
텍스트    "콩이 아침에 사료 반 그릇 먹었어"
            ↓  ② 이 프로젝트
구조화    {"tool": "log_meal", "arguments": {...}}
            ↓  ③ 앱
저장·조회  병원에서 꺼내 쓴다
```

**①을 안 만드는 이유가 둘이다.**

1. **이미 무료로 있고 온디바이스로 동작한다.** iOS·안드로이드 키보드의 받아쓰기가 한국어를 충분히 잘 처리한다. 재현해도 차별점이 0이고, 이 프로젝트가 채우려는 공백(모델 학습·서빙·평가)과도 무관하다
2. **진짜 병목이 ①이 아니라 ②다.** STT가 주는 것은 여전히 문장 한 줄이다. 그것은 일기이지 기록이 아니다 — 검색도 정렬도 안 되고 병원에서 "언제부터요?"에 답할 수도 없다

> **"말로 기록한다"에서 어려운 부분은 말을 알아듣는 것이 아니라, 알아들은 문장을 기록으로 바꾸는 것이다.**
> 기존 앱들이 못 푼 지점도 정확히 여기다.

#### 음성이 유일한 입구가 아니다

폰 키보드로 직접 치는 경로도 똑같이 이 모델이 처리한다. 그리고 그 경로는 **학습 데이터가 이미 커버하고 있다.**

```python
# lexicon.py 오타 페르소나
"desc": "폰으로 급히 입력해 오타·띄어쓰기 오류가 있음. 의미는 통함..."
"example": "콩이 아침에 사료 반그릇 줫어"
```

**타이핑 입력을 전제한 페르소나다.** "말로"는 대표 시나리오이지 유일한 입력 방식이 아니며, 음성이 어려운 상황(주변이 시끄럽거나 말하기 곤란한 자리)에서는 직접 입력이 그대로 대체 경로가 된다.

#### 정직하게 남기는 한계 — STT 오인식

STT가 잘못 알아들으면 이 모델이 받는 입력이 오염된다.

```
실제 발화   "사료 반 그릇 줬어"
STT 오인식  "사류 반 그릇 줬어"
                ↓
food="사류" 로 뽑히거나 아예 못 찾는다
```

오타 페르소나가 일부를 견디지만, **STT 오인식은 타이핑 오타와 성격이 다르다.** 타이핑 오타는 자판 인접 오류이고 STT 오인식은 발음이 비슷한 다른 단어로 바뀐다.

**완화책은 앱 단에 있다.** 통역 앱처럼 받아쓴 텍스트를 사용자에게 보여 확인받으면, 오인식이 이 모델에 도달하기 전에 걸러진다. 다만 확인을 강제하면 "말 한마디로 끝난다"는 마찰 없는 경험이 깎이므로, 저장은 바로 하고 나중에 고칠 수 있게 두는 편이 낫다. 앱 UX 영역이라 이 프로젝트의 범위 밖이지만 **설계로 답할 수 있는 문제**다.

---

## 3. 왜 파인튜닝인가 — 논리 체인 5단계

이 순서로 설명하면 상대가 따라온다.

### ① 산책 중에도 기록돼야 한다

폼과 버튼으로는 손이 자유롭지 않을 때 기록이 끊긴다. 기존 앱(반기다·똑똑집사·Pawmory)이 전부 탭 방식이라 못 푼 문제다. 그래서 **말로** 넣는다.

### ② 말로 넣으려면 오프라인이어야 한다

산책 중 지하나 시골에서는 통신이 안 된다. **서버 API를 부르는 순간 그 순간을 놓친다.** 게다가 매 발화마다 GPT를 부르면 사용자당 운영비가 감당되지 않고, 건강 기록은 사적 데이터라 서버로 안 보내는 편이 낫다.

→ **폰 안에서 돌아야 한다.**

### ③ 폰에서 돌리려면 작은 모델이어야 한다

`4B` — 파라미터 40억 개. `파라미터`는 모델 안에 학습된 숫자들이고, 많을수록 똑똑하지만 무겁다.

`4비트 양자화(quantization)` — 숫자 정밀도를 낮춰 용량을 줄이는 것. 4B를 4비트로 줄이면 약 2.5GB라 폰에 올라간다.

### ④ 작은 모델은 프롬프트로는 형식을 못 지킨다 ★ 핵심

`프롬프트(prompt)` — 모델에게 매번 지시문을 입력으로 넣어주는 방식.

프롬프트로 툴 12개 설명을 다 넣어줘도 **작은 모델은 JSON을 자주 깨뜨린다.** 키를 빠뜨리거나, 없는 툴 이름을 지어내거나, 설명을 덧붙여 파싱이 안 되게 만든다.

그리고 프롬프트는 근본적으로 **매번 다시 알려주는** 방식이라 모델이 기억하지 못한다.

→ **툴 지식을 `가중치(weights)`**(모델 내부에 학습돼 있는 숫자들)**에 아예 심는다. 그것이 파인튜닝이다.**

### ⑤ 부수 효과 — 입력이 짧아진다

툴 12개 설명을 프롬프트에 안 넣으므로 **입력이 발화 한 줄뿐**이다. 지연과 비용이 함께 내려간다. 이것을 정량화한 것이 이 프로젝트의 결과물 중 하나다. (설계 결정 2)

---

## 4. 학습 데이터 2,000쌍이란 무엇인가

### 한 쌍의 정체

**입력 하나 + 정답 하나**를 묶은 것이 한 쌍이다.

```
입력  "콩이 아침에 사료 반 그릇 먹었어"
정답  {"tool": "log_meal",
       "arguments": {"food": "사료", "amount": "반 그릇",
                     "time": "아침", "caregiver": null},
       "reason": null}
```

이런 것을 2,000개 만든다. 전부 다르게 생겼다.

| 입력 | 정답 |
| --- | --- |
| "몸무게 5.2킬로" | `log_weight(weight="5.2킬로")` |
| "엄마가 아침에 약 먹였대" | `log_medication(name="심장사상충약", caregiver="엄마")` |
| "구충제 줬어" | `log_medication(name="구충제")` — `log_care` 아님 |
| "구충제 안 밀렸나?" | `get_care_due(care_type="deworming")` |
| "요즘 물 얼마나 마셔?" | `get_trend(metric="water", period="요즘")` |
| "내일 날씨 어때?" | `{"tool": null, "reason": "out_of_scope"}` |
| "물이랑 밥 줬어" | `{"tool": null, "reason": "ambiguous"}` |

**거절도 정답으로 들어간다.** 모르면 지어내지 않고 거절하는 것 자체가 학습 대상이다. (설계 결정 4)

### 학습이 실제로 일어나는 방식

**규칙을 말로 알려주지 않는다.** 이것이 프롬프트와의 결정적 차이다.

프롬프트 방식이라면 이렇게 말할 것이다.

```
"구충제는 log_care가 아니라 log_medication입니다."
```

그리고 **매번 이 문장을 다시 넣어야 한다.** 안 넣으면 모르는 상태로 돌아간다.

파인튜닝은 예시만 보여준다.

```
"구충제 줬어"      → log_medication
"회충약 먹였어"    → log_medication
"구충 했어"        → log_medication
"양치 시켰어"      → log_care
"발톱 깎았어"      → log_care
```

**규칙을 한 번도 말하지 않았는데 모델이 스스로 뽑아낸다.** "약 종류는 이쪽, 위생 관리는 저쪽"이라는 경계를 예시들 사이에서 발견한다.

내부에서 벌어지는 일은 이렇다.

```
1. 모델이 "구충제 줬어"를 보고 답을 내놓는다
2. 처음엔 틀린다 — 예를 들어 log_care 라고 답한다
3. 정답과 비교해 얼마나 틀렸는지 잰다        ← 손실(loss)
4. 가중치를 정답 쪽으로 아주 조금 민다
5. 2,000쌍을 여러 번 반복한다                ← 에폭(epoch)
```

`손실(loss)` — 모델이 얼마나 틀렸는지 나타내는 숫자. 낮을수록 좋다.
`에폭(epoch)` — 데이터 전체를 한 번 훑는 것.

**"조금씩 미는" 것을 수만 번 반복하면 그 경계가 가중치에 자리를 잡는다.** 그때부터는 프롬프트로 알려주지 않아도 안다.

그래서 이 방식을 `SFT(Supervised Fine-Tuning)`라 부른다. `Supervised(지도)`가 "정답을 알려주면서"라는 뜻이다.

### 왜 하필 2,000개인가

세 방향에서 나온 숫자다.

| 근거 | 내용 |
| --- | --- |
| **아래로** | 툴 12개 × 난이도 층 6개 × 페르소나 6종을 덮으려면 이 정도는 필요하다. 툴당 최소 100건이 요구치 |
| **위로** | GPT-4.1 생성 비용 + T4 GPU 학습 시간. 무료 Colab 4시간 세션 안에 돌아야 한다 |
| **검증** | Day 7에 250 / 1,000 / 2,000으로 학습곡선을 재서 **"정말 2,000이 필요했나"를 직접 잰다** |

마지막이 중요하다. **2,000이 정답이라고 주장하는 것이 아니라 재보는 것이다.**

- 250 → 1,000 → 2,000으로 갈수록 점수가 계속 오르면 → 데이터가 더 필요하다
- 1,000에서 평평해지면 → 2,000은 과했고 모델 크기가 한계다

어느 쪽이 나와도 README에 쓸 내용이 생긴다.

---

## 5. 2,000 / 200 / 200 — 왜 나누는가

### 셋의 차이는 "재고 나서 무엇을 하느냐"

```
2,000  학습   →  모델이 본다.  가중치가 바뀐다.      "가르친다"
  200  검증   →  모델이 본다.  가중치는 안 바뀐다.   "확인한다"
  200  테스트 →  마지막 한 번. 가중치 안 바뀐다.    "점수를 낸다"
```

**학습 2,000건만 가중치를 바꾼다.** 나머지 400건은 모델을 바꾸지 않고 재기만 한다.

| | 개수 | 몇 번 보나 | 재고 나서 하는 일 |
| --- | --- | --- | --- |
| **학습** | 2,000 | 여러 번 | 가중치를 민다 |
| **검증** | 200 | **여러 번** | 결과를 보고 **모델을 바꾼다** |
| **테스트** | 200 | **딱 한 번** | **아무것도 안 한다.** 그 숫자가 결과다 |

요리로 비유하면 이렇다.

```
검증   간을 본다  →  짜면 물을 넣는다  →  또 본다  →  ...   (여러 번)
테스트 손님에게 낸다                                        (한 번)
```

### 왜 테스트를 한 번만 보는가

**테스트 결과를 보고 무언가를 바꾸는 순간, 테스트셋이 검증셋이 된다.**

```
테스트 → 62점 → "학습률을 바꿔볼까?" → 재학습 → 다시 테스트 → 68점

이 68점은 "처음 보는 데이터에서의 점수"가 아니다.
테스트셋을 보고 고른 결과다.
```

그러면 **"처음 보는 발화에도 통한다"는 주장이 깨진다.** 재려던 것이 바로 그것이었다.

> `"테스트셋으로 튜닝했다"`는 실험 설계 오류의 대표 사례다. 초보 프로젝트에서 흔히 나오고, 아는 사람은 바로 알아본다. 이것을 지켰다는 사실 자체가 신뢰 신호가 된다.

### "검증이 고르는 모델"과 "학습시키는 모델"은 같은 것인가

**같다. 다만 학습을 돌리면 버전이 여러 개 생긴다.**

```
Qwen3-4B-Instruct-2507  (원본 — 아직 아무것도 안 배움)
         │
         │  학습 2,000건으로 가중치를 민다
         ↓
   ┌─────┴─────┬───────────┐
   ↓           ↓           ↓
 버전 A      버전 B      버전 C     ← 설정을 바꿔가며 만든 것들
학습률 1e-4  2e-4       5e-5
   │           │           │
   └─────┬─────┴───────────┘
         │  검증 200건으로 셋을 채점 → 제일 높은 것을 고른다
         ↓
      버전 B 선택
         │
         │  테스트 200건으로 딱 한 번
         ↓
      최종 점수
```

`체크포인트(checkpoint)` — 학습 중간·완료 시점의 저장본. **검증이 고르는 것은 체크포인트다.**

"모델"이라는 단어가 문맥마다 다른 것을 가리켜 헷갈린다.

| 문맥 | 가리키는 것 |
| --- | --- |
| "Qwen3-4B **모델**을 쓴다" | 원본. 아직 학습 전 |
| "**모델**을 학습시킨다" | 만들어지는 중인 것 |
| "검증셋으로 **모델**을 고른다" | 학습이 끝난 결과물들 = 체크포인트 |

### 검증이 고르는 것은 설정만이 아니다

같은 설정 안에서도 에폭마다 체크포인트가 생긴다.

```
에폭 1 끝 → 체크포인트 저장 → 검증 62점
에폭 2 끝 → 체크포인트 저장 → 검증 71점
에폭 3 끝 → 체크포인트 저장 → 검증 68점   ← 떨어졌다
```

**3에폭이 항상 제일 좋은 것이 아니다.** 어느 시점부터 `과적합(overfitting)`(학습 데이터만 외워서 새 데이터에 약해진 상태)이 시작된다.

**그 꺾이는 지점을 찾아주는 것이 검증셋의 가장 실질적인 용도다.** 학습셋만 보고 있으면 점수가 계속 오르므로 과적합이 시작된 것을 알 수 없다. 바깥에서 재주는 눈이 필요하다.

### 셋은 같은 방식으로 만든다

내용이 다른 것이 아니라 **용도만 다르다.**

```
2,400건을 한 번에 만든다
        ↓  무작위로 쪼갠다
2,000 학습 / 200 검증 / 200 테스트
                          ↑ 여기만 사람이 눈으로 검수한다
```

**테스트 200건만 검수하는 이유** — 이것이 시험지이므로 여기가 틀리면 모든 점수가 무효다. 학습 2,000건은 몇 개 섞여 있어도 평균이 잡아주지만 시험지는 그럴 수 없다.

### 우리는 원본 가중치를 건드리지 않는다

`LoRA` — 모델 전체가 아닌 작은 부품(어댑터)만 학습시켜 GPU 요구량을 크게 낮추는 기법.

```
원본 가중치 40억 개  ← 얼려둔다. 안 바뀐다
      +
어댑터 (작은 추가 부품)  ← 이것만 학습된다
```

그래서 체크포인트가 원본 8GB가 아니라 **수십 MB**다. 여러 버전을 저장해두기도 쉽다. Day 5의 "어댑터 병합"은 그 부품을 원본에 합쳐 하나의 모델 파일로 만드는 작업이다.

`QLoRA` — 4비트 양자화 상태로 LoRA 학습. 무료 GPU로 4B급이 돌아가는 이유다.

---

## 6. 예시 하나 완주

발화 — **"2시에 강아지 산책을 시켰어"**

```
  ├─ "산책"    → activity_type: "walk"   분류 (영문 정규화)
  ├─ "2시"     → time: "2시"             표층 그대로
  ├─ (미언급)  → duration: null
  └─ "강아지"  → 슬롯 없음. 발화 장식
```

```json
{"tool": "log_activity",
 "arguments": {"activity_type": "walk", "duration": null, "time": "2시"},
 "reason": null}
```

**모델이 내린 판단은 셋이다.**

1. 12개 툴 중 `log_activity`를 골랐다
2. `"산책"`을 `walk`로 분류했다
3. `"2시"`가 시간 칸이라고 짚었다

### 세 가지를 짚어둔다

**① `"2"`가 아니라 `"2시"`** — 표층 표현을 손대지 않는다 (설계 결정 9). `"2시"`가 새벽인지 오후인지는 모델이 판단하지 않고, 발화 시각을 아는 앱이 정한다. 모델이 날짜 계산까지 하면 "시간 표현을 못 찾은 것"과 "찾았는데 계산을 틀린 것"이 오답 통계에서 섞인다.

**② `"산책"`이 아니라 `walk`** — 닫힌 집합은 영문으로 정규화한다 (설계 결정 12). `"산책"`·`"걷기"`·`"동네 한 바퀴"`가 전부 `walk`가 되므로, 이것은 **오려붙이기가 아니라 분류**다. 분류야말로 이 프로젝트가 재려는 능력이다.

**③ 미언급 파라미터는 `null`로 남는다** — 칸이 사라지지 않는다. 항상 같은 모양의 JSON이 나와야 "빠뜨린 것"과 "형식이 깨진 것"을 구분해 채점할 수 있다.

**④ 발화의 모든 단어가 슬롯으로 가지 않는다** — `"강아지"`는 어디에도 안 담긴다. 반려동물 호칭은 발화를 자연스럽게 만들 뿐이고, 앱은 이미 누구 이야기인지 안다. `caregiver`도 `log_activity`에는 칸 자체가 없다.

---

## 7. 소개 대본

### 30초

> 보호자가 말한 한마디를 구조화된 케어 기록으로 바꾸는 온디바이스 모델입니다. 산책 중에는 폼을 못 채워서 기록이 끊기는데, 그러면 정작 병원에서 "언제부터요?"에 답을 못 합니다. 4B 모델을 QLoRA로 파인튜닝해 폰 안에서 돌아가게 만들고, GPT-4.1 대비 정확도·지연·비용을 정량 비교했습니다.

### 2분

위 30초에 이 순서를 얹는다.

1. **문제** — 조사 숫자로. 투약 누락 47%, 기록 앱 사용 시 미준수 60% → 12.6%. **기록이 답이라는 것은 이미 증명돼 있고, 문제는 기록이 안 이어진다는 것**
2. **차별점** — 기존 앱은 입력을 빠르게 만들었고, 이 프로젝트는 입력을 없앤다
3. **기술 지점** — §3의 논리 체인 ①~⑤
4. **증명 방법** — 3자 비교 + 7분류 오답 분해
5. **안 한 것과 이유** — 순차 다중 호출, RAG, 진단

**5번을 빼먹지 않는다.** 범위를 긋고 근거를 대는 편이 "다 하려다 흐릿해진 프로젝트"보다 훨씬 좋게 읽힌다.

---

## 8. 예상 질문과 답

### "왜 GPT API를 안 쓰고 굳이 학습했나요?"

> 세 가지입니다. 산책 중 오프라인에서 동작해야 하고, 매 발화마다 API를 부르면 사용자당 운영비가 감당되지 않고, 건강 기록은 사적 데이터라 서버로 안 보내는 편이 낫습니다. 그리고 주장으로 끝내지 않고 **GPT-4.1과 정확도·지연·비용을 직접 비교했습니다.**

### "왜 RAG는 안 썼나요?"

> `RAG`(답하기 전에 문서를 검색해 그 내용을 근거로 답하게 하는 방식)는 **지식**을 다루는 도구인데, 이 태스크는 형식 변환이라 지식이 필요 없습니다. 품종별 표준 체중 같은 참조 데이터는 비정형 문서가 아니라 구조화된 표라서 벡터 검색이 아니라 키 조회가 맞고, 그 값으로 "정상인가"를 판단하는 것은 **안전 경계 밖**입니다.

"안 쓴 이유를 근거와 함께 말하는 것"이 "다 넣었습니다"보다 강하다.

### "파인튜닝 효과를 어떻게 증명했나요?"

> 비교 대상을 셋 뒀습니다. GPT-4.1(상한선), 같은 4B 모델 + few-shot(파인튜닝 안 한 기준선), 파인튜닝한 모델입니다. 그리고 정확도를 하나로 뭉개지 않고 **오답을 7유형으로 분해**했습니다. 툴을 잘못 고른 것과 JSON 자체가 깨진 것은 심각도가 다르기 때문입니다.

### "LLM을 안 쓴 곳이 있나요?"

> 있습니다. "심장사상충 약 32일째" 같은 알림은 **날짜 뺄셈이지 추론이 아니라서** 규칙 엔진이 맡습니다. 시간 표현도 모델이 `"아침"`을 그대로 출력하고 실제 날짜 변환은 앱 코드가 합니다. LLM에 맡기면 오답 원인이 섞여 파인튜닝 효과를 분리 측정할 수 없습니다.

요즘 대부분 LLM으로 해결하려 하므로 이 답이 특히 강하다.

### "학습 데이터는 어떻게 만들었나요?"

> 파인튜닝에서 어려운 부분은 학습 코드가 아니라 **학습 데이터를 어떻게 신뢰할 수 있게 만드느냐**였습니다. 정답을 코드로 먼저 확정하는 역방향 생성으로 정답 오류를 없애고, 경계 케이스마다 한 문장짜리 판정 규칙을 정해 데이터 안의 모순을 제거했으며, 라벨과 발화가 어긋나는 건은 자동 검증으로 걸러냈습니다. **테스트 200건은 전부 사람이 눈으로 확인했습니다.**

### "하이퍼파라미터는 어떻게 골랐나요?"

> 검증셋으로 골랐고, **테스트 200건은 최종 평가 때 한 번만 사용했습니다.**

### "음성 인식을 안 만들었다면서 '말로 기록한다'는 건 무슨 뜻인가요?"

> 음성 인식은 OS에 무료로 내장돼 있고 온디바이스로 동작하므로 재현할 이유가 없었습니다.
> **"말로 기록한다"에서 어려운 부분은 말을 알아듣는 것이 아니라, 알아들은 문장을 구조화된 기록으로 바꾸는 것**입니다. STT가 주는 것은 여전히 문장 한 줄이고, 그것만으로는 검색도 조회도 안 됩니다. 기존 앱들이 못 푼 지점도 거기입니다.
> 그리고 음성이 유일한 입구도 아닙니다. 폰으로 직접 치는 경로를 학습 데이터의 페르소나 하나가 이미 담당하고 있습니다.
> 다만 STT 오인식이 입력을 오염시키는 문제는 실제 한계입니다. 앱 단에서 받아쓴 텍스트를 사용자에게 확인받는 단계로 흡수할 수 있지만, 이 프로젝트의 측정 범위에는 넣지 않았습니다.

**마지막 문장이 중요하다.** 완화책을 말할 수 있으면서 측정하지 않았다는 것도 함께 밝히는 편이, 다 해결한 척하는 것보다 신뢰를 준다.

---

## 9. 한 문장으로 남길 것

> **파인튜닝에서 코드는 거들 뿐이고, 진짜 작업물은 2,000쌍 그 자체다.**

학습 스크립트는 Unsloth 예제를 거의 그대로 쓰면 되는 30줄이다. **차이를 만드는 것은 전부 데이터 쪽이다.** 지금까지 정한 설계 결정 17개가 대부분 이 2,000쌍의 품질을 위한 것이었다.

| 결정 | 무엇을 막으려고 |
| --- | --- |
| 결정 3 — 역방향 생성 | **정답이 틀리는 것** |
| 결정 13·15·16 — 경계 규칙 | **정답이 모순되는 것** |
| 결정 14 — 라벨 재정렬 | **정답과 입력이 어긋나는 것** |
| 결정 17 — 자연스러움 검증 | **입력이 가짜인 것** |
| 페르소나·금지 목록·시드 사전 | **2,000개가 서로 닮는 것** |
| 분포 확인 (툴 167건·caregiver 21.4%) | **한쪽으로 쏠리는 것** |

**모델은 데이터에 있는 것만 배운다. 데이터가 곧 명세서다.**
