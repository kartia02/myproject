# PawNote — CLAUDE.md

> **말로 남기고, 병원에서 꺼내 쓰는 반려동물 케어 기록.**
> 보호자의 한마디("콩이 아침에 사료 반 그릇 먹었어")를 구조화된 기록으로 바꾸는
> **온프렘 소형 모델**을 QLoRA로 파인튜닝하고, GPT-4.1 대비 정확도·지연·비용을 정량 비교한다.

기술적으로는 **자연어 → Tool Call(JSON) 변환 모델**을 만드는 프로젝트다.
목표는 기능이 많은 앱이 아니라 **Fine-tuning → Baseline 비교 → Evaluation → Serving**을 끝까지 닫아 증명하는 것.

---

## 왜 만드는가

국내 양육 실태조사에서 애로사항 상위 4개 중 2개가 건강 관련이었다 — **건강 이상 시 대처하기 힘들다 34.4%**, **건강 이상을 파악하기 힘들다 32.9%**. 해외 연구에서는 **개 보호자의 47%가 투약을 놓치고**, **46~51%가 예방약을 매년 잊는다**. 그런데 **기록 앱을 쓰면 미준수가 60% → 12.6%로 떨어진다.**

즉 기록이 문제를 해결한다는 건 이미 증명돼 있다. 문제는 **기록이 안 이어진다는 것**이다. 국내 기록 앱들(반기다·똑똑집사·Pawmory)은 전부 폼과 버튼 탭 방식이고, Pawmory의 셀링포인트가 "3초 안에 기록"일 만큼 업계도 입력 마찰을 핵심 문제로 인식하고 있다.

> **기존 앱은 입력을 빠르게 만들었다. 이 프로젝트는 입력을 없앤다.**
> 말 한마디를 구조화 기록으로 바꾸고, 서버가 아니라 폰 안에서 처리한다.

산책 중이거나 밥을 주는 중에는 손이 자유롭지 않다. 그때 폼을 못 채워서 기록이 끊긴다.

조사 과정과 전체 근거는 [context-notes.md](context-notes.md) §2 참고.

## 왜 작은 모델인가 (온디바이스)

- 매 발화마다 GPT API를 호출하면 사용자당 운영비가 감당되지 않는다
- 반려동물 기록은 사적 데이터다. 서버로 안 보내는 편이 낫다
- 산책 중 지하·시골에서도 **오프라인**으로 동작해야 한다

## 프로젝트 조건

- **용도** 이력서·포트폴리오. 규모가 아니라 **결과 숫자**로 승부한다
- **기간** 7일 (Day 0 준비 + Day 1~7). 2026-08-31 착수
- **환경** 무료 Google Colab · T4 GPU · VRAM 16GB · 세션 4시간 내외 · 일일 할당량 있음

---

## 핵심 설계 결정

변경할 때는 반드시 근거를 [context-notes.md](context-notes.md)에 기록한다.

| # | 결정 | 근거 |
| --- | --- | --- |
| 1 | **단일 Tool Call로 범위 한정** | 순차 의존 호출은 실행 결과가 필요한 Agent Loop 영역. 실행 환경 시뮬레이션 + 궤적 평가가 필요해 7일에 안 들어감 |
| 2 | **툴 스펙을 프롬프트에 넣지 않고 학습** | 툴 지식을 가중치에 내재화 → 추론 시 입력 토큰 대폭 감소(비용·지연 서사) + base few-shot과 격차 확보 |
| 3 | **역방향 생성으로 데이터 합성** | 정답 Tool Call을 코드로 먼저 균등 샘플링 → 자연어 질문을 GPT가 생성. 파라미터 분포가 구조적으로 고르고 정답이 틀릴 일이 없음 |
| 4 | **거절도 동일 JSON 스키마로 출력** | 툴 호출을 안 하는 방식이면 "의도적 거절"과 "형식 붕괴"를 구분할 수 없음 |
| 5 | **난이도는 툴 개수가 아니라 자연어 모호성으로 확보** | 실제 난점은 보호자가 툴 이름과 전혀 다른 구어체로 말한다는 것 |
| 6 | **툴 셋은 사용자 조사에서 역산** | 감으로 정했다면 `log_water`와 `caregiver`가 빠졌을 것 |
| 7 | **모델은 착수 직전 선정** | 최신 모델·Unsloth 지원 목록이 수시로 바뀜 |
| 8 | **모델이 할 일과 규칙이 할 일을 분리** | 아래 참조 |
| 9 | **시간 표현을 모델이 정규화하지 않는다** | 모델은 `"아침"`을 그대로 출력하고 ISO 변환은 앱 코드가 맡는다. 날짜 산술 오답이 툴 라우팅 오답과 같은 칸에 섞이면 7분류 분해가 흐려짐. 결정 8의 연장 |
| 10 | **RAG를 쓰지 않는다** | 태스크가 형식 변환이라 지식이 불필요. 품종 표준값은 비정형 문서가 아니라 구조화된 표이므로 벡터 검색이 아닌 키 조회가 맞고, 그 값으로 "정상인가"를 판단하는 것은 안전 경계 밖. 결정 8의 연장 |
| 11 | **수량도 모델이 정규화하지 않는다** | `amount_ml`·`duration_min`·`weight_kg`를 자유 문자열 `amount`·`duration`·`weight`로 통일. `"많이"`·`"짧게"`를 null로 버리지 않고, 단위 산술이 툴 라우팅 오답과 섞이지 않게 한다. 결정 9의 확장 |
| 12 | **닫힌 집합은 영문 정규화, 열린 집합만 표층** | 결정 9·11이 배제한 것은 열린 집합의 *변환*이지 닫힌 집합의 *분류*가 아니다. `"응가" → feces`는 계산이 아니라 분류이고, 분류가 이 프로젝트가 재려는 능력이다 |
| 13 | **약은 전부 `log_medication`** | `log_care`는 "약이 아닌 위생 관리"로 재정의. 경계가 한 문장으로 정의돼야 검수 200건의 일관성이 유지된다 |
| 14 | **역방향 생성에 라벨 재정렬 검증** | 표층 라벨과 GPT가 만든 발화가 어긋나면 모델이 맞아도 오답 처리된다. 발화가 아니라 라벨을 고친다 — 발화의 자연스러움이 이 데이터셋의 자산이므로 |
| 15 | **이상 징후는 전용 칸이 있는 툴이 담당** | `"설사했어"`는 `log_excretion.condition=diarrhea`가 담고, 담을 칸이 없으면 `log_symptom`. 규칙을 스키마에서 도출해 자의성을 없앤다 |
| 16 | **조회계 경계는 세 축으로 가른다** | ① 나열이냐 요약이냐 → `get_records`/`get_trend` ② 진료 맥락 + 종류 미지정 → `get_vet_summary` ③ **묻는 목적**이 "있었던 일"이냐 "이제 할 일"이냐 → `get_records`/`get_care_due`. 시제 어미가 아니다 |
| 17 | **발화 자연스러움을 사람이 검증한다** | 읽고 "얘가 뭘 물어보는 거지?"가 들면 버리거나 고친다. 결정 14는 라벨-발화 정합만 보고 발화 자체의 자연스러움은 못 잡는다. 조회계가 특히 어색해진다 |

### 결정 8 — 두 층을 섞지 않는다

| 층 | 하는 일 | 담당 |
| --- | --- | --- |
| **A층** | 말하면 기록된다 | **파인튜닝 모델. 이 프로젝트의 평가 대상** |
| **B층** | 놓친 걸 알려준다 ("심장사상충 약 32일째") | **규칙 엔진. LLM 아님** |

B층을 LLM으로 만들면 진단 영역으로 미끄러지고, 애초에 필요도 없다 — "마지막 투여 후 30일 경과"는 날짜 뺄셈이지 추론이 아니다. **안 쓸 곳을 아는 것도 설계다.**

MVP에는 모델이 조회할 수 있는 `get_care_due` 툴까지만 넣고, 능동 알림 규칙 엔진은 확장으로 미룬다.

### 결정 9 — 시간 표현은 모델이 정규화하지 않는다

모델은 **발화에 나온 시간 표현을 그대로** 슬롯(파라미터가 들어갈 칸)에 담는다. 절대 시각으로 바꾸는 일은 앱 코드가 맡는다.

```
발화     "콩이 아침에 사료 반 그릇 줬어"
모델     {"tool": "log_meal", "arguments": {..., "time": "아침"}, ...}
앱 코드  "아침" + 발화 수신 시각(2026-09-01 08:12) → 2026-09-01T08:00 저장
```

이유가 넷이다.

1. **오답 원인이 섞이지 않는다.** 모델이 정규화까지 하면 "시간 표현을 못 찾은 것"과 "찾았으나 날짜 계산을 틀린 것"이 7분류표의 같은 칸에 떨어져 파인튜닝 효과를 분리 측정할 수 없다
2. **정답이 오염되지 않는다.** 결정 3의 역방향 생성에서 GPT가 날짜 계산을 틀리면 정답이 깨진다. 표층 표현을 라벨로 쓰면 산술이 개입하지 않는다
3. **채점이 자의적이지 않다.** `"아침"`이 07시인지 08시인지는 관례이지 정답이 아니다. 관례라면 라벨링 가이드에서 다툴 게 아니라 함수 안에 한 줄로 둔다
4. **실사용에서 더 정확하다.** 자정 경계와 타임존은 발화 수신 시각을 아는 앱 코드가 틀릴 수 없다

**결정 8과 같은 원칙이다.** 날짜 뺄셈은 추론이 아니라 계산이므로 규칙이 맡는다.

**입력에 기준 시각을 넣고 모델이 정규화하는 방식(A안)은 Day 7 확장 실험으로 남긴다.** 이 데이터셋에서 코드 변환만으로 파생되므로 GPT 재호출도 재검수도 필요 없다. 결과가 어느 쪽이든 결정 9를 주장이 아니라 측정으로 바꿔준다.

---

## 안전 경계 (타협 금지)

- **기록·조회 전용. 진단·처방·투약 판단은 범위 밖.**
- `log_symptom`은 증상을 **기록**할 뿐 원인을 판단하지 않는다
- 이상 징후가 보여도 **병명을 말하지 않고** 수의사 상담으로 안내한다
- README에 이 경계를 명시한다

잘못된 조언은 실제로 동물을 해칠 수 있다. 그리고 경계를 문서화하는 것 자체가 판단력의 증거로 읽힌다.

---

## Tool 스펙 (12개)

각 툴이 어떤 조사 결과에서 나왔는지 함께 기록한다.

> **값 도메인의 단일 원본은 [src/tools.py](src/tools.py)다.** 아래 표는 근거 추적용 요약이고,
> enum 허용값·필수 여부·표층 시드 어휘는 코드 쪽을 본다. 데이터 생성기·채점기·베이스라인
> 프롬프트가 모두 그 파일 하나를 참조한다.

### 기록계 (log_*)

| 툴 | 파라미터 | 근거 |
| --- | --- | --- |
| `log_meal` | food, amount, time, caregiver | 수의사 문진 항목 · 중복 급여 방지 |
| `log_water` | amount, time, caregiver | 수의사 문진 항목. 음수량 증가는 초기 신호인데 가장 안 세는 항목 |
| `log_excretion` | type, condition, time | 수의사 문진 항목 · 배설물 관리 번거로움 34.1% |
| `log_medication` | name, dose, time, caregiver | **투약 누락 47%.** 최대 실패 영역이라 `log_care`에서 분리. **약이면 전부 여기** (결정 13) |
| `log_care` | care_type, time, caregiver | **약이 아닌 위생 관리** — 양치·발톱·목욕·항문낭·빗질·귀 청소. 털 관리 번거로움 34.1% |
| `log_symptom` | symptom, severity, time | 건강 이상 파악 어려움 32.9% · 미묘한 통증 신호(하품·입술 핥기)를 절반 이상이 놓침. **이상 상태 전용 칸이 없는 나머지 전부** (결정 15) |
| `log_activity` | activity_type, duration, time | 활력 변화 = 수의사 문진 항목 |
| `log_weight` | weight, date | 점진적 변화 감지 |

### 조회계 (get_*)

| 툴 | 파라미터 | 근거 |
| --- | --- | --- |
| `get_records` | record_type, since, until, caregiver | **"오늘 밥 줬어?" — 다인 가구 중복 급여 방지** |
| `get_trend` | metric, period | 건강 이상 파악 어려움 32.9%. 매일 보면 점진적 변화를 못 느낌 |
| `get_care_due` | care_type | 예방약 연간 망각 46~51% |
| `get_vet_summary` | since | **진료 시 기억이 흐려짐. 이 프로젝트의 시그니처 기능** |

### `caregiver` 필드가 중요한 이유

연구에서 **"여러 명이 돌보면 누가 투약했는지 불확실해진다"** 가 투약 실패의 원인으로 명시됐다. 효과적인 기능 세 가지도 기록·알림·**가구 구성원 간 공유**였다.

```
"엄마가 아침에 약 먹였대"   → log_medication(caregiver="엄마")
"오늘 밥 줬나?"             → get_records(record_type="meal", since="오늘")
```

두 번째가 실사용에서 가장 자주 나올 발화일 가능성이 높다.

**1인칭 주어는 `caregiver`에 담지 않는다.** `"내가 약 먹였어"` → `caregiver: null`. 발화한 사람이 누구인지는 앱이 아는 정보이므로 로그인 사용자로 채운다 — 결정 9와 같은 원칙이다. 시드 사전의 `CAREGIVER`에도 3인칭만 둔다.

---

## 출력 스키마 (고정)

```json
{"tool": "log_meal", "arguments": {"food": "사료", "amount": "반 그릇", "time": "아침", "caregiver": null}, "reason": null}
{"tool": null, "arguments": {}, "reason": "out_of_scope"}
{"tool": null, "arguments": {}, "reason": "ambiguous"}
```

- `out_of_scope` — 지원하지 않는 요청 ("내일 날씨 어때?", "우리 개 무슨 병이야?")
- `ambiguous` — 툴은 있으나 핵심 정보가 없거나 두 툴에 걸쳐 있음 ("그거 기록해줘", "물이랑 밥 줬어")
- **`time`·`since`·`until`·`date`·`period`는 발화의 표층 표현 그대로** — `"아침"`, `"저번주 화요일"`, `"어제 밤"`. ISO 변환은 앱 코드 담당 (결정 9)
- **`amount`·`duration`·`weight`도 표층 그대로** — `"반 그릇"`, `"짧게"`, `"5킬로 200"`. 단위 변환은 앱 코드 담당 (결정 11)
- **enum 파라미터는 영문 키로 정규화** — `"응가"` → `"feces"` (결정 12). 허용값은 [src/tools.py](src/tools.py)
- **미언급 파라미터는 `null`.** 필수 파라미터가 확정되지 않으면 `ambiguous`로 거절한다

> **진단 요청은 `out_of_scope`로 처리한다.** 안전 경계가 스키마 수준에서 강제되는 구조다.

### 직렬화 규칙 (학습 라벨 · 고정)

- `json.dumps(obj, ensure_ascii=False)` — 한글은 이스케이프하지 않는다
- 키 순서 고정 — `tool` → `arguments` → `reason`. `arguments` 내부는 `tools.py`의 파라미터 정의 순서
- 거절 시 `arguments`는 빈 객체 `{}`

### 채점 규칙 (Day 2 채점기 · 고정)

**판정 순서.** 위에서 걸리면 아래는 보지 않는다.

1. 출력에서 첫 `{`부터 짝이 맞는 `}`까지 추출 → `json.loads` 실패면 **파싱 실패**
   - 코드펜스(` ```json `)로 감싼 출력은 허용한다. 앱에서 한 줄로 벗겨낼 수 있는 것을 형식 붕괴로 치면 지표가 모델의 사소한 습관으로 오염된다. 대신 **코드펜스 사용률을 별도 지표로 기록**한다
2. 최상위 키가 `tool`/`arguments`/`reason` 셋과 다르거나 타입이 어긋나면 **스키마 위반**
3. `tool`이 12개 목록에 없으면 **툴 환각**
4. `arguments`의 키 집합이 해당 툴의 파라미터와 다르면 **스키마 위반**
5. enum 파라미터 값이 허용값 밖이면 **파라미터 오류**
6. 나머지는 값 비교

**값 비교의 정규화.** 표층 계열 파라미터는 아래만 적용하고 그 이상은 하지 않는다.

- 앞뒤 공백 제거 · 내부 공백 전부 제거 (`"반 그릇"` == `"반그릇"`)
- 전각 → 반각, 영문 소문자화

동의어 사전과 편집 거리는 **쓰지 않는다.** 자의성이 들어가면 결정 9의 "채점이 자의적이지 않다"가 깨지고, 애초에 결정 14의 라벨 재정렬이 표현 불일치를 제거한다. 다만 **조사만 다른 오답**(`"사료를"` vs `"사료"`)은 따로 집계해 README에 보고한다.

---

## 데이터셋 구성

**학습 2,000 / 검증 200 / 테스트 200.**
**테스트 200건은 반드시 사람이 눈으로 검수한다** — 정답이 틀린 시험지로 재면 모든 숫자가 무효다.

| 유형 | 비중 | 예시 |
| --- | --- | --- |
| 단순 1툴 | 22% | "몸무게 5.2킬로" |
| 파라미터 다중 | 28% | "아침 8시에 사료 반 그릇 줬어" |
| 어휘 미스매치·혼동 쌍 | 28% | "구충제 줬어" (약인가 케어인가) |
| 시제·맥락 판별 | 10% | "구충제 안 밀렸나?" (과거형이지만 다음 일정을 묻는다) |
| out_of_scope | 7% | "내일 날씨 어때?" |
| ambiguous | 5% | "물이랑 밥 줬어", "심장사상충 약 언제야" |

합계 100%. 결정 9로 날짜 정규화가 빠지면서 기존 "암묵적 파라미터" 층은 **시제·맥락 판별**로 성격이 바뀌었고, 남은 비중은 이 프로젝트의 핵심 난이도인 혼동 쌍에 얹었다.

**`caregiver` 언급은 별도 유형이 아니라 위 유형들에 교차 분포**시킨다. 전체의 약 20%.

**합성 데이터 균질화 방지** — 역방향 생성 · 화자 페르소나 주입 · 어휘 시드 사전 · 배치당 n개 상이 요청 · 금지 목록 주입 · 임베딩 유사도 0.9 이상 중복 제거 · `distinct-2`로 다양성을 **측정**(확보했다가 아니라 수치로).

---

## 비교 대상 3개

| 대상 | 역할 |
| --- | --- |
| GPT-4.1 + 툴 목록 프롬프트 | 현실의 상한선 |
| Base 모델 + few-shot | **파인튜닝 기여도 증명용. 빠뜨리면 프로젝트가 성립 안 함** |
| Base 모델 + QLoRA 파인튜닝 | 본 프로젝트 산출물 |

## 평가 — 7분류 오답 분해표

정확도를 하나로 뭉개지 않는다. 오답에는 급이 있다.

| 출력 유형 | 심각도 |
| --- | --- |
| 완전 일치 | — |
| 툴 정답 · 파라미터 오류 | 낮음 |
| 툴 오류 (존재하는 툴) | 중간 — 회복 가능 |
| **툴 환각 (없는 툴 생성)** | 높음 — 실행 불가 |
| **스키마 위반 · 파싱 실패** | 최고 — 출력 자체가 깨짐 |
| 과잉 거절 (툴 있는데 null) | 중간 |
| 놓친 거절 (툴 없는데 호출) | 높음 |

부가 지표 — 지연 p50/p95, 1,000건당 비용, 입력 토큰 수(설계 결정 2의 효과).

> **예상 하이라이트** — 파인튜닝은 정확도보다 **형식 안정성**을 극적으로 올린다. 스키마 위반·툴 환각이 거의 0으로 떨어지는 패턴이 나올 가능성이 높다.

## Success Criteria (착수 전 고정, 사후 변경 금지)

- **P0 최소 성공** — 데이터 생성 → 학습 → 평가 → Ollama 서빙까지 완주. 3자 비교표와 오답 분해표 산출. **성능 수치와 무관하게 성립**
- **P1 목표 성공** — base few-shot 대비 완전 일치율 **+15%p 이상**, 스키마 위반율 **5% 미만**, 툴 환각 **2% 미만**
- **P2 도전 성공** — GPT-4.1 완전 일치율의 **90% 이상** 유지하면서 건당 비용 **1/10 이하**, p50 지연 더 낮음

P0을 성능과 분리한 것이 핵심이다. 결과가 나쁘게 나와도 "부정적 결과를 정직하게 측정한 실험"으로 성립한다.

### 비용 계산 기준 (Day 0 확정, 사후 변경 금지)

두 가지를 따로 낸다. 섞으면 유리한 쪽을 골랐다는 인상이 남는다.

| 기준 | 계산법 | 용도 |
| --- | --- | --- |
| **(a) 계산 자원 환산** | 로컬 추론 시간 × 클라우드 GPU 시간 단가 vs GPT-4.1 API 요금 | **P2 판정은 이걸로 한다.** 더 엄격한 쪽 |
| (b) 사업자 부담 | GPT는 API 요금, 온디바이스는 0 (사용자 기기) | 서사용. 판정 근거로 쓰지 않는다 |

(b)가 실제 제품 논리이지만 온디바이스는 사업자 비용이 0에 수렴해 비교 자체가 성립하지 않는다. **판정은 (a)로 하고 (b)는 README에 별도로 서술한다.**

---

## Tech Stack

- **학습** Unsloth + `trl` SFTTrainer + `peft` LoRA (4bit QLoRA)
- **모델** **`Qwen3-4B-Instruct-2507`** (2026-09-02 확정) — 학습 `unsloth/Qwen3-4B-Instruct-2507-unsloth-bnb-4bit`, 서빙 `unsloth/Qwen3-4B-Instruct-2507-GGUF`. 선정 과정과 탈락 근거는 [context-notes.md](context-notes.md) §3 결정 7
- **데이터 생성** OpenAI API (`gpt-4.1`)
- **평가** 자체 Python 스크립트
- **서빙** Ollama (GGUF 변환). vLLM은 범위 밖
- **저장** Google Drive 마운트 필수 — 세션이 끊기면 체크포인트 소실

## 범위 밖

- **순차 의존 다중 호출** — Agent Loop 영역, 별도 과제
- 병렬 독립 다중 호출 — 확장 후보 1순위
- 능동 알림 규칙 엔진 — 확장 (툴 `get_care_due`까지만 MVP)
- 진단·처방 — 안전 경계
- **실기기(폰) 추론 측정** — 온디바이스 타당성은 GGUF 파일 크기와 **CPU-only 추론 지연**으로 간접 논증한다. 실제 단말 벤치마크는 범위 밖이며 README에 그렇게 명시한다
- 시간 표현 → ISO 정규화 — 모델이 아니라 앱 코드 담당 (결정 9)
- **음성 인식(STT)** — OS 내장 기능을 쓴다. 모델 입력은 **텍스트 발화부터** 시작한다. 진짜 병목은 "말을 알아듣는 것"이 아니라 "알아들은 문장을 기록으로 바꾸는 것"이다. STT 오인식은 앱이 받아쓴 텍스트를 사용자에게 확인받는 단계로 흡수하고, 음성이 어려운 상황을 위해 **직접 입력 경로를 함께 둔다** — 오타 페르소나가 이미 그 경로를 학습 데이터에 담고 있다
- vLLM, 웹·모바일 앱 UI, 여러 모델 학습 비교

## 폴백 (Day 4 판정)

Day 4까지 학습이 안 돌면 **포기하고 축소한다.** Day 2~3에 "GPT-4.1 vs base few-shot" 비교표가 이미 확보돼 있으므로, 프로젝트를 *"소형 모델로 자연어 케어 기록을 구조화할 수 있는가 — 프롬프트 접근의 한계 분석"* 으로 재정의하면 그대로 성립한다.

**부담이 커지면 툴을 12개 → 10개로 축소한다.** 줄이는 순서는 `log_activity` → `log_weight`. 조사 근거가 상대적으로 약한 순서다.

---

## 작업 규칙 (Claude 행동 가이드)

1. **한국어 출력, 문장 끝에 콜론 금지** — `.`/`?`/`!`로 종료
2. **기술 용어는 첫 등장 시 한 줄 설명을 붙인다** — 사용자가 명시적으로 요청. 용어를 피하지 말고 "용어 + 짧은 설명" 형태로. 사전식 정의보다 "지금 맥락에서 왜 중요한지"를 붙일 것. 모르는 용어는 [context-notes.md](context-notes.md) §5-2 용어집에도 추가. 프롬프트·파인튜닝·RAG의 차이는 §5-1에 정리돼 있다
3. **새 `.py` 파일 첫 줄에 한국어 헤더 주석** — 역할 한 줄. 설정 파일 제외
4. **커밋 컨벤션** `타입: 한국어 설명` — `add`·`delete`·`edit`·`fix`. 한 논리 단위 = 한 커밋
5. **단순성 우선** — MVP 목록 밖의 기능·추상화를 임의로 추가하지 않는다
6. **가정 명시** — 불확실하면 질문. 해석이 여럿이면 제시
7. **GPU 시간은 자원이다** — GPU 없이 되는 작업을 Colab 세션에서 하지 않는다
8. **안전 경계를 넘지 않는다** — 진단·처방 기능을 제안하지 않는다

## 문서 인덱스

- [checklist.md](checklist.md) — Day별 실행 체크리스트. 진행할 때마다 체크하고 하단 로그를 남긴다
- [context-notes.md](context-notes.md) — 주제 선정 배경, **사용자 조사 과정과 결과**, 설계 근거, 리스크, 용어집
- [explainer.md](explainer.md) — **이 프로젝트를 이해하고 남에게 설명하기 위한 문서.** 파인튜닝이 어디에 쓰이는지, 학습 2,000쌍이 무엇인지, 학습·검증·테스트를 왜 나누는지. 소개 대본과 예상 질문 포함. **Day 6 README의 밑글**

> 네 문서의 역할이 갈린다 — **CLAUDE.md는 규격, checklist는 할 일, context-notes는 근거, explainer는 설명.**
> 새 내용을 적을 때 어디에 속하는지 먼저 정한다.
