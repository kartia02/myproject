# Labeling Guide — PawNote

**데이터를 검수할 때 펼쳐놓고 보는 판정표다.**

- 30건 샘플 테스트 (Day 1)
- 테스트 200건 사람 검수 (Day 2)
- 검수 중 새 경계 케이스를 만나면 **여기에 추가한다**

값 도메인의 단일 원본은 [src/tools.py](src/tools.py)다. 이 문서는 **"어느 툴이고 어느 칸인가"를 정하는 규칙**만 담는다.

> 규칙은 전부 **한 문장으로 정의된다.** 케이스를 나열하는 방식이면 목록에 없는 발화를 만나는 순간 막히기 때문이다.

---

## 0. 판정 순서

위에서 걸리면 아래는 보지 않는다.

```
① 이 앱이 처리할 수 있는 요청인가        → 아니면 out_of_scope
② 무엇을 말하는지 확정되는가              → 아니면 ambiguous
③ 기록인가 조회인가                       → log_* / get_*
④ 어느 툴인가                            → §2·§3
⑤ 각 칸에 무엇을 넣는가                   → §4
⑥ 이 발화를 사람이 실제로 하는가          → 아니면 버린다 (§6)
```

---

## 1. 거절 판정

### `out_of_scope` — 지원하지 않는 요청

| 유형 | 예시 |
| --- | --- |
| 날씨·기온 | "내일 날씨 어때?" |
| **진단 요구** | "우리 개 무슨 병이야?", "이거 위험해?" |
| **투약 판단 요구** | "이거 두 알 줘도 돼?" |
| 예약·검색 | "근처 동물병원 예약해줘" |
| 추천 | "사료 뭐가 좋아?" |
| 상식·지식 | "말티즈 수명이 어떻게 돼?" |
| 구매·주문 | "사료 좀 주문해줘" |
| 잡담 | "너 이름 뭐야?" |

> **진단·투약 판단은 안전 경계다.** 병명·원인·위험 여부를 묻거나 "줘도 되는지"를 물으면 무조건 `out_of_scope`다. 이 경계는 타협하지 않는다.

### `ambiguous` — 툴은 있으나 확정할 수 없음

| 유형 | 예시 | 왜 |
| --- | --- | --- |
| 지시대명사뿐 | "그거 기록해줘" | 무엇을 가리키는지 없다 |
| **사건이 둘** | "물이랑 밥 줬어" | 한 번의 호출로 못 담는다 (결정 1) |
| 필수 파라미터 미확정 | "화장실 갔어" | 대변인지 소변인지 없다 |
| **사람도 두세 갈래로 읽음** | "심장사상충 약 언제야" | 먹였나·줘야 하나·샀나 (결정 17) |
| 대상 없음 | "아까 그거 했어" | 동작만 있고 대상이 없다 |

> **정보가 부족한데 파라미터를 지어내는 것이 "지원 안 함"보다 위험하다** (결정 4). 애매하면 거절이 정답이다.

#### 두 사건을 한 발화에 담으면 하나만 저장하지 않는다

```
"오늘 설사도 하고 산책하다 주저앉기도 했어"  →  ambiguous
```

하나만 저장하면 나머지가 조용히 사라져 **"보호자의 기억을 붙잡아둔다"는 목적과 정반대**가 된다. 거절하고 앱이 되묻는다.

---

## 2. 기록계 툴 경계 (`log_*`)

### 규칙 A — 약이면 전부 `log_medication` (결정 13)

| 툴 | 범위 |
| --- | --- |
| `log_medication` | **약이면 전부.** 처방약·구충제·심장사상충약·영양제. 먹이든 바르든 무관 |
| `log_care` | **약이 아닌 위생 관리.** 양치·발톱·목욕·항문낭·빗질·귀 청소 |

| 발화 | 판정 |
| --- | --- |
| "구충제 줬어" | `log_medication` |
| "영양제 챙겨줬어" | `log_medication` |
| "심장사상충약 먹였어" | `log_medication` |
| "양치 시켰어" | `log_care` |
| "발톱 깎았어" | `log_care` |

> "경구 투여인가"로 나누지 않는다. 바르는 구충제에서 다시 흔들린다.

### 규칙 B — 이상 징후는 전용 칸이 있는 툴이 담당 (결정 15)

> **이상 상태를 담을 전용 칸이 그 툴에 있으면 그 툴, 없으면 `log_symptom`.**

전용 칸은 둘뿐이다 — `log_excretion.condition`, `log_symptom.severity`.

| 발화 | 담을 칸 | 판정 |
| --- | --- | --- |
| "설사했어" | `condition=diarrhea` | `log_excretion` |
| "혈변 봤어" | `condition=bloody` | `log_excretion` |
| "응가가 딱딱해" | `condition=hard` | `log_excretion` |
| "산책하다 주저앉아" | 없음 | `log_symptom` |
| "밥을 안 먹어" | 없음 (`amount`는 양 칸이지 상태 칸이 아니다) | `log_symptom` |
| "물을 안 마셔" | 없음 | `log_symptom` |
| "토했어" | 없음 | `log_symptom` |

**주의** — `log_meal.amount`·`log_activity.duration`은 **양을 담는 칸이지 상태 칸이 아니다.** 이상 표현이면 `log_symptom`으로 간다.

경계선이 헷갈릴 때 — **양을 말하면 기록계, 이상을 말하면 `log_symptom`.**

```
"사료 조금 먹었어"     → log_meal(amount="조금")      양 진술
"밥을 안 먹어"         → log_symptom                  이상 진술
```

---

## 3. 조회계 툴 경계 (`get_*`)

세 축으로 가른다 (결정 16). 먼저 각 툴이 **하는 일**부터 구분한다.

| 툴 | 하는 일 | 출력 |
| --- | --- | --- |
| `get_records` | **꺼내기** | 기록을 그대로 나열 |
| `get_trend` | **합치기** | 여러 건을 하나의 값으로 |
| `get_vet_summary` | **추리기** | 여러 종류를 훑어 진료에서 말할 것만 |
| `get_care_due` | **앞을 보기** | 주기 항목의 다음 차례 |

### 축 ① 나열이냐 요약이냐 — `get_records` / `get_trend`

> **개별 기록을 나열해서 답이 되면 `get_records`, 여러 건을 합쳐야 답이 되면 `get_trend`.**

| 발화 | 판정 |
| --- | --- |
| "오늘 물 마셨어?" | `get_records(record_type="water", since="오늘")` |
| "어제 밥 몇 번 줬어?" | `get_records` |
| "이번주 산책 몇 번 했어?" | `get_records` |
| "요즘 물 얼마나 마셔?" | `get_trend(metric="water", period="요즘")` |
| "물 마시는 게 늘었나?" | `get_trend` |
| "체중 변화 어때?" | `get_trend` |

**시간 표현은 보조 신호일 뿐 기준이 아니다.** `"이번주"`는 양쪽에 다 들어간다.

```
"이번주 산책 몇 번?"    → 세면 됨   → get_records
"이번주 산책량 어때?"   → 합쳐야 함 → get_trend
```

### 축 ② 진료 맥락과 종류 특정 — `get_vet_summary` / `get_records`

> **진료·병원 맥락이 있고 기록 종류를 특정하지 않으면 `get_vet_summary`. 종류가 특정되면 맥락이 있어도 `get_records`.**

`get_vet_summary`는 파라미터가 `since` 하나뿐이라 **종류를 좁힐 칸이 없다.**

| 발화 | 진료 맥락 | 종류 특정 | 판정 |
| --- | --- | --- | --- |
| "최근 기록 좀 보여줘" | ✗ | ✗ | `get_records` |
| "이번주 밥 기록 보여줘" | ✗ | ✓ | `get_records` |
| "병원 갈 건데 최근 기록 좀" | ✓ | ✗ | `get_vet_summary(since="최근")` |
| "내일 진료 있어서 정리 좀" | ✓ | ✗ | `get_vet_summary` |
| "병원 갈 건데 약 먹인 기록만" | ✓ | ✓ | `get_records(record_type="medication")` |

**단어가 아니라 맥락을 본다.** `"내일 데려가야 하는데 요 며칠 어땠는지 정리 좀"` 은 `"병원"`·`"진료"` 어느 단어도 없지만 `get_vet_summary`다.

### 축 ③ 묻는 목적 — `get_records` / `get_care_due`

> **"무슨 일이 있었나"를 물으면 `get_records`, "이제 해야 하나"를 물으면 `get_care_due`.**
> **시제 어미가 아니라 묻는 목적으로 가른다.**

| 발화 | 겉보기 시제 | 묻는 목적 | 판정 |
| --- | --- | --- | --- |
| "심장사상충 약 언제 줬지?" | 과거 | 있었던 일 | `get_records` |
| "지난달에 구충제 했나?" | 과거 | 있었던 일 | `get_records` |
| **"심장사상충 약 한 달 됐나?"** | **과거** | 이제 할 때인가 | **`get_care_due`** |
| **"구충제 안 밀렸나?"** | **과거** | 놓쳤는지 확인 | **`get_care_due`** |
| "약 챙길 때 됐나?" | 과거형 어미 | 예정 확인 | `get_care_due` |
| "예방접종 다음이 언제지?" | 미래 | 이제 할 때인가 | `get_care_due` |

**`"한 달 됐나"`·`"안 밀렸나"` 가 이 축의 핵심이다.** 과거형인데 묻는 것은 미래다.

### 기록과 조회의 비대칭 (결정 13)

예방약은 `log_medication`으로 **기록**되지만 `get_care_due`로 **조회**된다. 의도한 비대칭이다.

```
"심장사상충 약 먹였어"   → log_medication(name="심장사상충약")
"심장사상충 약 언제야"   → get_care_due(care_type="heartworm")
```

---

## 4. 파라미터 채우기

### 세 종류를 구분한다 (결정 12)

| 종류 | 처리 | 해당 파라미터 |
| --- | --- | --- |
| **닫힌 집합 (enum)** | **영문 키로 정규화** | `type`·`condition`·`care_type`·`activity_type`·`severity`·`record_type`·`metric` |
| **열린 집합** | **표층 그대로** | `food`·`amount`·`name`·`dose`·`symptom`·`duration`·`weight`·`caregiver` |
| **시간 표현** | **표층 그대로** | `time`·`since`·`until`·`date`·`period` |

```
"응가 쌌어"      → type: "feces"      분류다. 영문
"반 그릇 줬어"   → amount: "반 그릇"   받아쓰기다. 표층
"아침에"         → time: "아침"        받아쓰기다. 표층
```

### 시간·수량은 계산하지 않는다 (결정 9·11)

```
"2시에 산책했어"      → time: "2시"        "14:00" 아님
"저번주 화요일부터"   → since: "저번주 화요일"  "2026-08-25" 아님
"5킬로 200"          → weight: "5킬로 200"    "5.2" 아님
"짧게 했어"          → duration: "짧게"       null 아님
```

**`"많이"`·`"짧게"` 같은 모호 표현도 버리지 않고 그대로 담는다.** 정보를 통째로 잃지 않기 위해서다.

### `since`·`until`은 발화에 나온 순서대로 담는다

> **먼저 말한 것이 `since`, 나중에 말한 것이 `until`이다. 어느 쪽이 이른 시각인지 따지지 않는다.**

```
"오늘 아침이랑 며칠 전에 한 번 봐줄래?"
  → since: "오늘 아침"   until: "며칠 전"      먼저 나온 것이 since
```

시간 순으로 정렬하려면 **어느 쪽이 이른지 계산해야 한다.** 그것은 결정 9가 모델에게서 걷어낸 바로 그 일이다. 모델은 표층을 순서대로 담고, **날짜를 아는 앱이 정렬한다.**

```
모델   since="오늘 아침"  until="며칠 전"
앱     2026-09-05 ~ 2026-09-08 으로 바꾸며 정렬
```

**두 칸에 같은 값이 들어가면 `until`을 비운다.** `since="화요일" · until="화요일"` 은 구간이 아니라 라벨링 사고다.

#### 두 시점을 말해도 구간으로 담는다

`"A랑 B"`는 시점 두 개를 각각 묻는 말이지만, `get_records`는 구간 하나만 받는다. **그래도 버리지 않는다** — `A`~`B` 구간이 두 시점을 **모두 포함**하므로, 사이에 낀 기록이 딸려 올 뿐 원하는 것은 빠지지 않는다. 거절해서 아무것도 주지 않는 것보다 낫다.

정확히 두 점만 집는 것은 **병렬 독립 다중 호출**의 몫이고 MVP 범위 밖이다 (확장 1순위).

> **왜 이 경우가 중요한가** — `"오늘 아침이랑 며칠 전에도 그랬어"` 는 **재발**을 말하는 발화다. 재발은 수의사가 가장 먼저 묻는 것이라 버려서는 안 되는 유형이다.
>
> 다만 **기록할 때는 사용자가 재발을 말할 필요가 없다.** `"토했어"` 한마디면 앱이 과거 기록을 조회해 `"3일 만에 재발"` 을 붙인다 — 날짜 조회이지 추론이 아니므로 B층(규칙 엔진)이 맡는다 (결정 8).

> **실측** — `until`이 찬 test 12건 중 `"부터~까지"` 형태는 **0건**, train 111건 중 3건뿐이었다. **사람은 구간으로 말하지 않는다.** 시간 표현 두 개를 독립적으로 샘플링한 것이 원인이고, 데이터 생성의 한계로 README에 기록한다.

### `caregiver`는 돌본 사람일 때만 채운다

> **그 사람이 실제로 돌봄 행위를 했을 때만 담는다. 함께 있었거나, 봤거나, 전해 준 사람은 `null`이다.**

1인칭을 `null`로 두는 규칙(아래)의 확장이다. `caregiver`는 **"누가 했는가"** 를 담는 칸이지 발화에 나온 사람을 담는 칸이 아니다.

| 발화 | `caregiver` | 왜 |
| --- | --- | --- |
| "엄마가 약 먹였대" | `"엄마"` | 엄마가 했다 |
| "아들도 있었고" | **`null`** | 있었을 뿐이다 |
| "아들까지 봤다" | **`null`** | 본 것은 돌봄이 아니다 |
| "할머니가 물 많이 마셨다고 알려주셨어요" | **`null`** | 알려준 사람이다 |
| "삼촌 있는 데서 거의 안 마셨어요" | **`null`** | 장소를 가리킬 뿐이다 |

#### 사람이 주체가 되어버린 발화는 버린다

`caregiver`가 문장의 **주어나 대상** 자리로 밀리면 반려동물 기록이 아니게 된다. 라벨은 그대로여도 발화가 다른 뜻이므로 버린다.

```
"룸메가 한 그릇 물을 마셨습니다"      → 룸메가 마셨다      버린다
"아들한테 넥스가드 줬다"              → 아들이 받았다      버린다
"요즘 언니 뭐 했는지 봐야 함"         → 언니가 한 일       버린다
"와이프 몸무게 최근 거 뭐로 되어있지"   → 사람의 몸무게      버린다
```

**남기는 것과 가르는 선은 하나다 — 그 사람이 반려동물에게 무엇을 했는가.**

```
남긴다  "루비 배변이 ... 언니가 챙긴 거 같은데"    언니가 루비를 챙겼다
버린다  "요즘 언니 뭐 했는지 봐야 함"              언니가 한 일
```

> `log_water`가 특히 위험하다. 밥은 `"줬다·먹였다"`라 주어가 보호자지만, 물은 `"마셨다"`가 자연스러워 **주어가 마시는 쪽**이 된다. 실측에서 `caregiver`가 찬 `log_water` 92건 중 59건이 사람이 마신 문장이었다.

### 조사는 떼고 담는다

```
발화 "아침에 사료를 반 그릇 줬어"
     → time: "아침"   ("아침에" 아님)
     → food: "사료"   ("사료를" 아님)
```

### 1인칭 주어는 `caregiver`에 담지 않는다 (결정 9의 확장)

| 발화 | `caregiver` |
| --- | --- |
| "약 먹였어" | `null` |
| **"내가 약 먹였어"** | **`null`** |
| "엄마가 약 먹였대" | `"엄마"` |

발화한 사람이 누구인지는 앱이 아는 정보이므로 로그인 사용자로 채운다.

### 미언급 파라미터는 `null`

칸이 사라지지 않는다. 항상 같은 모양의 JSON이 나와야 "빠뜨린 것"과 "형식이 깨진 것"을 구분해 채점할 수 있다.

### 발화의 모든 단어가 슬롯으로 가지 않는다

```
"콩이 아침에 사료 줬어"
  ↑ 반려동물 호칭은 어느 칸에도 안 담긴다
```

앱은 이미 누구 이야기인지 안다.

---

## 5. 필수 파라미터가 없으면 거절

| 툴 | 필수 | 없으면 |
| --- | --- | --- |
| `log_excretion` | `type` | `ambiguous` — "화장실 갔어" |
| `log_medication` | `name` | `ambiguous` — "약 먹였어"만으로는 부족 |
| `log_care` | `care_type` | `ambiguous` |
| `log_symptom` | `symptom` | `ambiguous` |
| `log_activity` | `activity_type` | `ambiguous` |
| `log_weight` | `weight` | `ambiguous` |
| `get_trend` | `metric` | `ambiguous` |

`log_meal`·`log_water`·`get_records`·`get_care_due`·`get_vet_summary`는 필수 파라미터가 없다. 전부 `null`이어도 유효한 호출이다 (전체 조회·단순 기록).

---

## 6. 발화 자연스러움 (결정 17)

> **읽고 "얘가 지금 뭘 물어보는 거지?" 라는 생각이 들면 버리거나 고친다.**

**조회계(`get_*`)를 특히 주의한다.**

| 계열 | 왜 |
| --- | --- |
| 기록계 | **혼잣말이라 안전하다.** "밥 줬어"·"토했어"는 실제로 그렇게 말한다 |
| **조회계** | **앱에 묻는 말이라 아무도 해본 적이 없다.** 상상해서 쓰면 어색해진다 |

### 걸러낼 신호

- 사람이 두세 갈래로 읽는다 → `ambiguous`로 옮기거나 버린다
- 앱에 명령하는 말투다 ("조회해줘"·"보여줘"만 반복) → 가족에게 묻는 말로 고친다
- 실제로 하지 않는 말이다 → 버린다

### 어색함이 아니라 라벨이 갈리는지를 본다

> **모든 해석에서 라벨이 같으면 남긴다. 해석에 따라 라벨이 달라지면 버린다.**

어색함만으로 버리면 기준이 취향이 된다. 시험지에 필요한 것은 **정답이 하나로 정해지는가**이므로 거기에 맞춘다.

| | 발화 | 판정 |
| --- | --- | --- |
| 남긴다 | "산책 달이 품종 뭐엿지" | `"산책"`이 붕 떠 있지만 어떻게 읽어도 품종 질문이라 `out_of_scope` 하나뿐이다. 오히려 `log_activity` 쪽으로 끄는 미끼라 좋은 문항이다 |
| 버린다 | "지난번엔 몸무게 기억이 잘 안 난다" | 회상이면 툴을 부르면 안 되고, 조회 요청이면 `since="지난번"`이 들어가야 한다. **읽는 방향이 정답을 바꾼다** |

조각·오타는 오타 페르소나가 의도적으로 넣은 것이므로 그것만으로는 버리는 근거가 안 된다.

### 혼잣말은 거절 사유가 아니다

`"…다시 생각해봐야겠다"`처럼 앱이 아니라 자기에게 하는 말이어도 **도메인 안이면 `out_of_scope`가 아니다.** `out_of_scope`는 *앱이 지원하지 않는 종류*라는 뜻이고, 정보가 모자란 것은 `ambiguous`다. 기록계가 원래 혼잣말이라는 위 표와 같은 이유다.

### 검수 요령

**조회계 발화를 따로 모아서 연달아 읽는다.** 섞여 있으면 어색함이 잘 안 보이는데, 모아 놓으면 바로 드러난다.

---

## 7. 자동 검증이 못 잡는 것 — 검수에서 눈으로 볼 것

[src/verify.py](src/verify.py)는 **라벨 값이 발화 안에 문자열로 있는지**만 본다. 뜻은 모른다. 아래 세 유형은 **검증을 통과하는데 라벨이 틀린다.** 30건 샘플 테스트에서 실측한 것들이다.

### ① 뜻을 바꾸는 조사

```
라벨   caregiver = "할아버지"
발화   "오늘 할아버지처럼 구름이 이빨 닦기 해줬어요"
```

`"할아버지"` 라는 글자는 있으니 통과하지만, **할아버지가 한 게 아니다.** `"처럼"`·`"같이"` 는 격조사처럼 붙어서 주어를 비유로 바꿔버린다.

> **검수 요령** — `caregiver` 가 찬 발화는 **그 사람이 실제로 한 것인지**를 본다.

### ② 다른 표현에 삼켜진 매치

```
발화   "조금 전부터 몸을 웅크려 있는..."
채움   severity = null → "mild"
```

`"조금"` 이 `symptom_severity.mild` 의 시드 어휘라서 걸렸지만, 여기서는 **시간 표현 `"조금 전"` 의 일부**지 정도가 아니다.

> **검수 요령** — 역방향으로 채워진 칸(`severity`·`condition` 등)은 **그 단어가 정말 그 뜻으로 쓰였는지**를 본다.

### ③ 활용형

```
시드   "묽게"
발화   "응가가 좀 묽었던 것 같은데"
```

문자열이 겹치지 않아 **채우지도 못하고 버리지도 못한다.** 생성 프롬프트가 1차 방어이고 여기가 2차 그물인데, 둘 다 새는 구멍이다.

### ④ 덜 구체적인 라벨 — 재보고 그대로 두기로 한 것

```
발화   "초코 응가 딱딱 점심때 좀 힘들어함"
라벨   time = "점심"        ← "점심때" 도 시드 값인데 부분 문자열로 통과했다
```

정방향 1단계가 *"라벨이 발화에 있는가"* 만 보고 **더 구체적인 형태가 있는지는 안 본다.** ②의 반대 방향이다.

**고치려다 되돌렸다.** "더 긴 값이 걸리면 교체"로 넓게 잡으면 **오히려 망가진다.**

| 바뀔 내용 | 발화 | 결과 |
| --- | --- | --- |
| `activity_type training → walk` | "산책 끝나고 **앉아 연습** 했습니다" | **오답.** 기록 대상은 앉아 연습이다 |
| `until "어제" → "3일 전"` | "어제 엄마가 3일 전…" | **오답.** 3일 전은 `since` 쪽이다 |

정방향에서 다른 값이 걸리는 것은 **더 구체적인 형태가 아니라 대개 발화 속 다른 언급**이기 때문이다. 규칙을 *"현재 표층을 **포함**하면서 더 긴 것"* 으로 좁히면 같은 언급만 남는다.

**그 좁은 규칙으로 재보니 `test` 0건 · `val` 0건 · `train` 9건(0.5%)이었다.** 시험지에 없으므로 고치지 않는다.

> **넓은 규칙이 시험지 5건 중 4건을 오답으로 바꿨을 것이다.** 자동 보정은 잘못 잡으면 조용히 정답을 망가뜨린다 — 규모를 재보고 결정한다.

> ①~③은 **고치지 않기로 한 한계다.** 학습 2,000건은 평균이 흡수하고, **테스트 200건만 사람이 본다** — 시험지만 정확하면 숫자는 유효하다.

### 라벨은 맞는데 발화가 어색한 경우

버릴지 말지는 §6으로 판단한다. 자주 나오는 세 패턴이다.

| 패턴 | 예시 | 원인 |
| --- | --- | --- |
| 파라미터가 적은데 메모체 | "구름이 여섯시쯤 형 물" | 담을 값이 둘뿐이라 문장이 안 된다 |
| 기록인데 질문으로 시작 | "며칠 전 쉬 보다가 피 나왔던 거 기억나?" | 조회로 읽힌다 |
| 한 발화에 질문 둘 | "금요일에 증상 있었어? 그리고 어제는?" | `since`·`until` 이 한 구간으로 안 묶였다 |

---

## 8. 새 경계 케이스를 만나면

1. **한 문장으로 정의되는 규칙**을 만든다. 케이스 나열은 금지
2. 가능하면 **스키마에서 근거를 찾는다** — "이 칸이 있으니 이 툴" 처럼. 자의성이 사라진다
3. 이 문서에 추가하고, 근거가 크면 [context-notes.md](context-notes.md) §3에 결정으로 남긴다
4. 값 도메인을 고쳤으면 [src/tools.py](src/tools.py)·[src/lexicon.py](src/lexicon.py)도 함께 고친다

---

## 9. 한 장 요약

```
진단·투약 판단·날씨·추천·구매·잡담     → out_of_scope
두 사건 / 지시대명사 / 필수 없음 / 다의 → ambiguous

약이면 전부                            → log_medication
약 아닌 위생 관리                      → log_care
이상 상태 담을 칸 있으면 그 툴          → log_excretion
칸 없으면                              → log_symptom

나열로 답이 되면                       → get_records
합쳐야 답이 되면                       → get_trend
진료 맥락 + 종류 미지정                → get_vet_summary
"이제 할 때인가"를 물으면              → get_care_due

enum          영문 키
그 외 전부     표층 그대로 (조사만 뗀다)
미언급        null
1인칭 주어    caregiver = null
```
