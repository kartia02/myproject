# Context Notes — PawNote

이 문서는 **왜 이렇게 결정했는지**를 남긴다.
무엇을 할지는 [checklist.md](checklist.md), 무엇인지는 [CLAUDE.md](CLAUDE.md).

작성 2026-08-31.

---

## 0. 한눈에 보기

> 방향이 흐려질 때 이 절부터 다시 읽는다.

### 한 문장

**보호자의 기억을 대신 붙잡아두는 도구.**
진료실에서 "언제부터요?"라는 질문에 "글쎄요" 대신 날짜를 말할 수 있게 한다.

### 사용자는 보호자 한 명이다

**수의사는 우리 시스템의 사용자가 아니다.** 우리 화면을 볼 일도, 계정을 만들 일도 없다.

```
보호자가 기록  →  보호자가 요약을 확인  →  보호자가 수의사에게 말함
                                              ↑ 수의사는 이 흐름 밖
```

이 한 줄이 기능 추가를 고민할 때 경계선 역할을 한다. 세 가지가 여기서 따라 나온다.

1. **`get_vet_summary`의 출력은 보호자가 그대로 읽을 수 있는 문장**이어야 한다. 의무기록 양식이 아니다
   ```
   8월 19일부터 아침 사료를 남기기 시작
   8월 22일 구토 1회
   8월 25일 물 마시는 양이 평소의 2배
   체중 5.4kg → 5.1kg (2주간)
   ```
   날짜와 사실만. 해석은 없다
2. **수의사를 사용자로 넣으면 완전히 다른 제품이 된다** — 의료 시스템 안으로 들어가면 신뢰성 요구 수준·책임 소재·인증 문제가 전부 달라진다. 개인 프로젝트가 건드릴 영역이 아니다
3. **안전 경계와 일관된다** — 우리는 의료 정보를 만들지 않는다. 원래 보호자의 것이었던 기억을 안 잊게 도울 뿐이다

### 역할 분담

| 단계 | 담당 |
| --- | --- |
| 매일 말로 기록 | 보호자 + **우리 모델** |
| "뭔가 이상한데?" 하고 느낌 | 보호자 |
| "언제부터였지?"에 답함 | **우리 기록** |
| 원인을 찾고 진단 | **수의사** |

우리가 담당하는 건 1번과 3번뿐이다. **찾아주지 않는다. 찾을 수 있는 재료를 준비할 뿐이다.**

### 7일 안에 실제로 만드는 것

| | 만드나 |
| --- | --- |
| 발화를 Tool Call로 바꾸는 **모델** | **만든다. 이게 전부다** |
| 모바일 앱 UI | 안 만든다 |
| DB, 실제 저장 | 안 만든다 |
| 음성 인식(STT) | 안 만든다 |
| 알림 기능 | 안 만든다 |

**완성된 제품의 핵심 부품 하나**를 만들고, 그것이 GPT-4.1보다 싸고 빠르면서 쓸 만한지를 숫자로 증명한다. 나머지는 "이 부품이 들어갈 자리"로 문서에만 서술한다.

### 목표는 두 겹이다

| 관점 | 목표 |
| --- | --- |
| **제품** | 말로 기록해서, 병원에서 답할 수 있게 한다 |
| **기술 (포트폴리오)** | 파인튜닝 → 베이스라인 비교 → 평가 → 서빙을 끝까지 닫아 증명한다 |

**평가받는 건 아래쪽이다.** 제품 관점은 "왜 만들었나"에 답하고 학습 데이터를 자연스럽게 만들어주는 역할이다. 실제 결과물은 비교표와 오답 분해표다.

> ⚠ **이 둘을 헷갈리면 프로젝트가 망한다.** 제품을 완성하려 들면 앱·DB·알림에 시간을 쓰게 되고, 정작 평가받을 학습과 측정이 부실해진다.

---

## 1. 이 주제에 도달한 경로

### 출발점 — 무엇을 안 해봤는가

직전 팀 프로젝트 ClickMe(광고 전주기 플랫폼)에서 담당한 영역은 **generator 도메인(광고 생성) 단독**이었다. 이미지 생성 프롬프트, 텍스트 오버레이, 전략 템플릿, 누끼 3-tier 폴백, SSE. 즉 **생성형 AI 애플리케이션 파이프라인은 충분히 다뤘다.**

반대로 공백이 명확했다.

- **모델 레이어 (학습·서빙·양자화)** — 전무. API 호출만 해봤다
- 에이전트 오케스트레이션 — 다른 팀 담당
- RAG·검색 — 다른 팀 담당
- 평가·관측 루프 — 프로젝트 전체에 부재

이력서 관점에서 **API 소비자와 모델을 다루는 사람은 분류가 다르다.** 후자가 훨씬 드물다. 그래서 파인튜닝을 택했다.

### 검토했으나 버린 선택지

| 후보 | 버린 이유 |
| --- | --- |
| **생성 품질 평가 + 프롬프트 자가개선 하네스** | 초기 1순위였으나 두 번 반려. ClickMe 설명과 겹쳐 읽혀 **이력서에서 두 칸 쓰고 한 칸만큼만 보이는** 손해 |
| **사내 지식 RAG 에이전트** | 지원자 절반이 보유. 3일이면 흔한 챗봇 하나로 끝나 변별력이 거의 0 |
| **승인 게이트 있는 실행 에이전트** | 승인 UI·툴 연동이 필수라 UI 비용이 큼 |
| **MCP 서버 구축** | 탈락이 아니라 **2단계로 보류.** 아래 참조 |

### MCP 서버를 2단계로 미룬 이유

MCP 서버는 3일에 완주 가능하고 회사 도입 즉시성이 최고였다. 다만 최종 기준이 **"내가 할 수 있는 영역을 넓힌다"** 였으므로 기술 축이 더 낯선 파인튜닝을 택했다.

두 주제는 **순차로 이으면 하나의 서사가 된다.**

1. (지금) 자연어 → Tool Call 변환 모델을 만든다
2. (나중) MCP 서버를 만들고, 이 모델을 그 서버의 라우팅 두뇌로 붙인다
3. 결합 서사 — *"개인 도메인에서 검증한 온디바이스 툴 라우팅을 사내 시스템으로 확장한다"*

**동시 진행은 금지.** 두 단계가 직렬 의존이라 앞이 늦어지면 뒤가 통째로 막힌다.

> MCP를 만들 때 대비 — **호출 로그를 남기는 기능을 처음부터 넣어둘 것.** 그 로그가 그대로 다음 학습 데이터(증류용 궤적)가 된다.

### 왜 도메인을 반려동물로 바꿨는가

처음 설계는 사내 툴(이슈 트래커·배포·문서) 시나리오였다. 바꾼 이유는 **"어떤 기술을 쓸까"만 있고 "누구를 위한 것인가"가 없었기 때문이다.** 7일을 혼자 붙잡고 있으려면 관심이 붙는 소재가 필요했다.

중요한 건 **기술 축이 한 줄도 안 바뀐다는 점**이다. 파인튜닝 → 베이스라인 비교 → 평가 → 서빙이라는 뼈대는 그대로고, 반려동물은 그 위에 얹히는 소재다. 오히려 세 가지가 좋아졌다.

1. **자연어 난이도가 진짜로 올라간다.** 개발자의 말은 이미 정형화돼 있지만 보호자의 말은 애칭·생략·구어체·단위 모호("반 그릇")가 뒤섞인다. 억지로 어렵게 만들 필요가 없다
2. **"왜 작은 모델인가"가 선명해진다.** 온디바이스(사용자 폰 안에서 직접 실행)라는 근거가 사내 시나리오의 "비용 절감"보다 강하다
3. **"왜 만들었어요?"에 답이 생긴다**

**반려동물로 완전히 새 기획을 하지 않은 이유** — 다른 주제(증상 이미지 분류, 건강 이상 감지)는 7일 안에 데이터 확보가 안 되고, 공개 의료 데이터가 거의 없으며, 다뤄도 위험하다. 소재만 갈아끼우는 것이 정확히 최적이었다.

---

## 2. 사용자 조사 — 무엇을 어떻게 찾았고 무엇이 나왔는가

> **이 절이 이 프로젝트에서 가장 중요한 부분이다.**
> 툴 셋을 감이 아니라 조사에서 역산했고, 그 과정이 곧 README의 핵심 섹션이 된다.

### 왜 조사했는가

처음엔 "내가 키우면서 번거로웠던 것"으로 툴을 정하려 했다. 그런데 **사람마다 놓치는 지점이 다르다.** 한 사람의 경험으로 툴을 정하면 학습 데이터의 발화가 그 사람에게만 자연스럽게 편향된다. 그래서 실제 조사·연구를 찾아봤다.

### 조사 방법

네 갈래로 나눠 검색했다.

| 갈래 | 알고 싶었던 것 |
| --- | --- |
| 국내 양육 실태조사 | 보호자가 실제로 꼽는 어려움의 **순위** |
| 해외 투약 준수 연구 | 무엇을, 얼마나 놓치는가에 대한 **숫자** |
| 수의사 문진 가이드 | 병원에서 **실제로 묻는 항목** |
| 기존 반려동물 기록 앱 | 이미 있는 것과 **왜 부족한가** |

### 결과 ① 국내 실태조사 — 상위 4개 중 2개가 건강

| 애로사항 | 비율 |
| --- | --- |
| 여행 가기 힘들다 | 37.4% |
| **건강 이상 시 대처하기 힘들다** | **34.4%** |
| 배설물·털 관리가 번거롭다 | 34.1% |
| **건강 이상을 파악하기 힘들다** | **32.9%** |

가구 유형별로도 1인가구·부부가구·반려묘가구 모두 "건강 이상 시 대처하기 힘들다"를 공통으로 꼽았다. 프로젝트 방향이 조사로 뒷받침됐다.

### 결과 ② 투약 준수 연구 — 문제와 해법이 모두 숫자로

- 개 보호자 **47%가 투약을 놓침** (Odom 2024), 고양이 39% (Odom 2025)
- 전자 모니터링 결과 **정시 투여는 64%뿐** (Adams 2005)
- **1일 3회 투약은 1~2회 대비 9배 더 놓침** (Adams 2005)
- **46~51%가 예방약을 매년 잊음** (Merck 2025)
- **기록 앱 사용 시 미준수 12.6% vs 미사용 60%** (Ribas 2020)
- 연구가 꼽은 효과적인 기능 셋 — **기록 · 알림 · 가구 구성원 간 공유**
- 미준수 원인 중 하나로 **"여러 명이 돌보면 누가 투약했는지 불확실해진다"** 가 명시됨

마지막 두 줄이 설계를 바꿨다. → `caregiver` 필드 도입.

Ribas 2020의 60% → 12.6%는 **"기록 도구가 실제로 효과 있다"는 인용 가능한 근거**다. "왜 이걸 만드나"에 감이 아니라 출처로 답할 수 있게 됐다.

### 결과 ③ 수의사 문진 항목 — 로그 항목이 여기서 나왔다

수의사가 진료 때 묻는 것은 **"언제부터 시작됐는지, 얼마나 자주 나타나는지, 식사·음수·활력·배변 습관에 어떤 변화가 있었는지"** 였다.

또 하나 — **"진료라는 긴장된 상황에서는 기억이 흐릿해지므로, 예약을 결심한 날이 아니라 처음 이상하다고 느낀 순간부터 기록하라"** 는 조언이 반복해서 나왔다.

이 두 가지가 툴 두 개를 만들어냈다.

- **음수(`log_water`)** — 내 초안에 없던 항목이다. 물 마시는 양 증가는 신장·당뇨 등의 초기 신호인데 보호자가 가장 안 세는 항목이다
- **`get_vet_summary`** — "내일 병원 가는데 요약해줘". 평소 기록의 **목적**을 완성하는 툴이다

부수적으로, 개 보호자 절반 미만만이 미묘한 통증 신호(하품, 입술·코 핥기, 표정 변화)를 인식한다는 연구도 있었다. → `log_symptom`의 값 목록에 행동 신호를 포함한다.

### 결과 ④ 기존 앱 — 나쁜 소식이 아니라 좋은 소식

국내에 이미 있다. **반기다**(기록 다이어리, 알람, 4.3점), **똑똑집사**("여럿이서 함께 케어"), **Pawmory**("3초 안에 기록").

세 가지를 알 수 있다.

1. **수요가 검증됐다.** 아무도 안 쓰는 걸 만드는 게 아니다
2. **똑똑집사가 공동 돌봄을 판다** → `caregiver` 필드의 필요성을 시장이 이미 인정
3. **Pawmory의 셀링포인트가 "3초 안에 기록"** → 업계도 **입력 마찰을 핵심 문제로 인식**하고 있다

그런데 셋 다 **여전히 폼·버튼 탭 방식**이다. "3초"도 결국 탭이다. 차별점은 여기서 나온다.

> 기존 앱은 입력을 **빠르게** 만들었다. 이 프로젝트는 입력을 **없앤다.**
> 말 한마디를 구조화 기록으로 바꾸고, 서버가 아니라 폰 안에서 처리한다.

산책 중이거나 밥을 주는 중에는 손이 자유롭지 않다. 그때 폼을 못 채워서 기록이 끊긴다.

### 조사 → 툴 매핑 (근거 추적)

| 툴 | 나온 근거 |
| --- | --- |
| `log_meal` | 수의사 문진 · 중복 급여 |
| **`log_water`** | **수의사 문진. 조사로 발견한 누락분** |
| `log_excretion` | 수의사 문진 · 배설물 관리 34.1% |
| `log_medication` | **투약 누락 47%.** 최대 실패 영역이라 독립 툴로 분리 |
| `log_care` | 털 관리 34.1% (양치·발톱·목욕·항문낭·빗질·귀 청소). **구충은 결정 13으로 `log_medication`으로 이관** |
| `log_symptom` | 건강 이상 파악 32.9% · 미묘한 통증 신호 인식률 |
| `log_activity` | 활력 변화 = 수의사 문진 |
| `log_weight` | 점진적 변화 감지 |
| `get_records` | **다인 가구 중복 급여. "오늘 밥 줬어?"** |
| `get_trend` | 건강 이상 파악 32.9%. 매일 보면 변화를 못 느낌 |
| `get_care_due` | 예방약 연간 망각 46~51% |
| **`get_vet_summary`** | **진료 시 기억 흐려짐. 조사로 발견한 신규 툴** |
| **`caregiver` 필드 (전 툴 공통)** | **"누가 줬는지 불확실" = 투약 실패 원인. 조사로 발견** |

**조사가 바꾼 것 세 가지 — `log_water`, `get_vet_summary`, `caregiver`.** 감으로 만들었으면 셋 다 없었다.

### 출처

- [KB 반려가구의 반려동물 양육 행태](https://kbthink.com/main/economy/economic-in-depth-analysis/economic-research-report/series5-230604.html)
- [KB 반려가구의 반려동물 양육 경험](https://kbthink.com/investment/deepdive/research/250629-4.html)
- [Pet Medication Adherence: Why Owners Miss Doses — 연구 인용 모음](https://remewdy.com/science) (Odom 2024·2025, Adams 2005, Merck 2025, Ribas 2020)
- [Dog Owners Often Miss The Subtle Signs Their Pet Is in Pain](https://www.sciencealert.com/dog-owners-often-miss-the-subtle-signs-their-pet-is-in-pain-study-finds)
- [동물병원 방문 준비법 — 수의사에게 무엇을 말해야 하나](https://petnote-plus.com/app/ko/lp/guides/how-to-prepare-for-a-vet-visit)
- [강아지가 평소와 다를 때 병원 상담 전 기록해야 할 것](https://mysolodog.com/what-to-record-before-vet-consultation-for-unusual-dog-behavior/)
- [반기다 — 반려동물 기록 다이어리](https://apps.apple.com/kr/app/id6443524869)
- [Pawmory — 3초 안에 기록하는 반려동물 일기](https://apps.apple.com/kr/app/id6761605394)
- [똑똑집사 — 반려동물 돌봄 기록 앱](https://apps.apple.com/lv/app/id1611344641)
- [2022년 동물보호 국민의식조사 (농식품부)](https://www.mafra.go.kr/home/5109/subview.do?enc=Zm5jdDF8QEB8JTJGYmJzJTJGaG9tZSUyRjc5MiUyRjU2NTI4MiUyRmFydGNsVmlldy5kbyUzRg%3D%3D)

---

## 3. 설계 결정의 근거

### 결정 1 — 단일 Tool Call로 범위 한정

"다중 호출"에는 성격이 다른 두 개가 섞여 있다.

| 유형 | 예시 | 실행 결과 필요 | 판정 |
| --- | --- | --- | --- |
| 순차 의존 | "지난주 증상 기록 보고 병원 요약 만들어줘" | **필요** | **범위 밖** |
| 병렬 독립 | "물이랑 밥 둘 다 기록해줘" | 불필요 | 확장 1순위 (지금은 `ambiguous`) |

순차 의존은 Agent Loop이다. 실행 환경 시뮬레이션, 궤적 형태 학습 데이터, 궤적 단위 평가가 모두 필요해 **그것만으로 별도 프로젝트 분량**이다.

**이 배제는 감점이 아니라 가점이다.** README에 범위와 근거를 명시하면 "다 하려다 흐릿해진 프로젝트"보다 훨씬 좋게 읽힌다. 실제 시스템에서 이 모델은 **라우팅 부품**으로 동작한다고 서술한다.

### 결정 2 — 툴 스펙을 프롬프트에 넣지 않고 학습 ★ 가장 중요

학습 입력에 툴 12개 설명을 넣을 것인가 말 것인가.

- 넣고 학습 → 추론 시에도 매번 넣어야 함 → 입력 토큰이 계속 크다
- **안 넣고 학습 → 툴 지식이 가중치에 내재화 → 추론 시 발화만 넣으면 됨**

미포함을 택한 이유가 셋이다.

1. **"base가 이미 잘함" 리스크를 원천 봉쇄한다.** few-shot base는 스펙 없이는 툴 이름조차 모르므로 격차가 확실히 벌어진다
2. **비용·지연 서사가 강해진다.** 입력 토큰 80~90% 감소를 정량화할 수 있어 *"정확도는 GPT의 92%인데 건당 비용은 1/30"* 이라는 문장이 나온다
3. **온디바이스 서사와 맞물린다.** 폰에서 매 발화마다 긴 프롬프트를 처리하는 건 현실적이지 않다

**한계는 정직하게 README에 쓴다** — 툴 스펙이 바뀌면 재학습이 필요하며, 툴이 자주 바뀌는 환경에는 부적합하다. 트레이드오프 명시가 오히려 신뢰를 준다.

### 결정 3 — 역방향 생성

GPT에게 "질문과 정답을 만들어줘"라고 하면 쉽고 비슷한 것만 나온다. 순서를 뒤집는다.

1. 코드로 Tool Call을 랜덤 생성 — `log_meal(food="사료", amount="반 그릇", time="08:00", caregiver="엄마")`
2. GPT에게 **"이 호출을 유발할 자연스러운 한국어 발화"** 를 쓰게 함
3. 결과는 "발화 → 정답 호출" 쌍

파라미터 조합이 구조적으로 고르게 퍼지고, 정답이 먼저 확정돼 있어 **정답이 틀릴 여지가 없다.**

### 결정 4 — 거절도 동일 JSON으로

툴 호출을 아예 하지 않는 방식이면 **"의도적 거절"과 "출력 형식 붕괴"가 같은 모습으로 보인다.** 그러면 7분류 오답 분해가 불가능해진다.

`reason`을 둘로 나눈 이유 — 실무에서 더 위험한 실패는 "지원 안 함"보다 **"정보가 부족한데 파라미터를 지어내는 것"** 이다.

**진단 요청("우리 개 무슨 병이야?")을 `out_of_scope`로 처리**하면 안전 경계가 스키마 수준에서 강제된다.

### 결정 5 — 난이도는 자연어 모호성으로

세 방법을 비교했다.

| 방법 | 판정 |
| --- | --- |
| 툴 개수 늘리기 | **거의 무효.** 서로 명확히 다르면 30개여도 쉽다. 프롬프트만 길어져 손해 |
| 툴 기능을 더 비슷하게 | 효과는 있으나 **인위적** |
| **자연어 모호성 높이기** | **정답.** 진짜 난점은 사용자가 툴 이름과 다른 말을 쓰는 것 |

반려동물 도메인은 이게 **자연히** 나온다.

- **애칭·생략** — "콩이 아침에 반 그릇밖에 안 먹었어"
- **단위 모호** — "반 그릇", "한 줌", "조금"
- **시제 생략** — "심장사상충 약 언제야" (과거 조회인가 다음 일정인가)
- **구어체·오타** — "산책 좀 짧게 했다", "밥 얼마나 먹었었지"
- **경계 케이스** — "설사했어" (배변 기록인가 증상인가)

> 경계 케이스는 **라벨링 규칙을 문서화하고 일관되게 적용한다.** README에 "라벨링 가이드" 섹션을 두는 것 자체가 데이터 엔지니어링 역량의 증거다.

### 결정 6 — 툴 셋은 조사에서 역산 (§2 참조)

### 결정 7 — 모델은 착수 직전 선정

핵심은 "어떤 모델을 쓰는가"가 아니라 **"제약된 환경에서 작은 모델로 실제 업무를 처리할 수 있는가"** 다. 모델은 교체 가능한 변수다.

| 순위 | 기준 | 성격 |
| --- | --- | --- |
| 1 | T4 16GB QLoRA 학습 가능 | 하드 제약 |
| 2 | Unsloth 지원 | 준하드 제약 (없으면 T4에서 사실상 불가) |
| 3 | GGUF 변환·Ollama 서빙 | Day 5가 막힘 |
| 4 | 한국어 성능 | 태스크가 한국어 구어체 |
| 5 | Structured Output / JSON | 태스크 핵심 |
| 6 | Instruction Following | 4·5에 대체로 따라옴 |
| 7 | Tool Calling 사전학습 | **낮을수록 유리할 수 있음** |

**7번의 역설** — 툴 호출에 이미 특화된 모델은 base 성능이 높아 파인튜닝 개선폭이 안 나온다. "잘하는 모델"보다 "적당히 하는 모델"이 이 프로젝트엔 낫다.

**모델 비교에 시간을 쓰지 말 것. Day 0에 2시간 이내.** 1~3번만 통과하면 고르고, 진짜 성능 비교는 Day 3의 base 측정이 겸한다.

#### 확정 (2026-09-02) — `Qwen3-4B-Instruct-2507`

로드는 `unsloth/Qwen3-4B-Instruct-2507-unsloth-bnb-4bit`, 서빙은 같은 이름의 `-GGUF`.

**최신 세대가 오히려 탈락했다.** 결정 7이 "최신 모델을 쓰는 게 목적이 아니다"라고 못박아 둔 것이 실제로 작동한 사례다.

| 후보 | ① T4 QLoRA | ② Unsloth | ③ GGUF | 판정 |
| --- | --- | --- | --- | --- |
| Qwen3.5-4B | **✗** | ○ | ○ | **탈락** |
| Gemma 4 E4B | △ | ○ | ○ | 위험 |
| Gemma 3 4B | ○ | ○ | ○ | 통과 |
| **Qwen3-4B-Instruct-2507** | **○** | **○** | **○** | **채택** |

- **Qwen3.5 탈락** — Unsloth 문서가 *"It is not recommended to do QLoRA (4-bit) training on the Qwen3.5 models"* 라고 명시한다. bf16 중심 설계인데 T4는 Turing이라 bf16을 못 쓰고, Mamba 하이브리드 Triton 커널이 *"especially on T4 GPUs"* 컴파일이 느리다고 경고한다. 우선순위 1번에서 걸린다
- **Gemma 4 위험** — E4B의 LoRA VRAM이 17GB로 T4의 16GB를 넘고, E2B·E4B는 vision+audio 멀티모달이다. 26B-A4B는 MoE·A100 필요

**Gemma 3 4B와의 최종 비교에서 갈린 지점 셋.**

1. **텍스트 전용** — Gemma 3는 4B부터 vision이 붙는다(270M·1B만 text-only). 쓰지 않을 SigLIP 인코더가 T4 VRAM을 먹고, GGUF 변환에 `mmproj` 분리가 따라오며, **Day 5의 온디바이스 논증에서 파일 크기가 불리해진다**
2. **thinking 분리 체크포인트** — `Instruct-2507`은 `Thinking-2507`과 별개라 `<think>` 블록이 JSON을 깨뜨릴 위험이 없다. Qwen3 기본 하이브리드 모드의 chat template 함정을 우회한다
3. **아티팩트가 이미 있다** — 4bit 학습용과 GGUF 서빙용이 모두 공개돼 있어 Day 3·Day 5의 미지수가 줄어든다

**우선순위 7번(툴 호출 사전학습이 낮을수록 유리)에 걸리지 않는가** — Qwen3-4B의 BFCL 점수는 높지만 그것은 **툴 스펙이 프롬프트에 주어진 상태**의 점수다. 결정 2로 스펙을 주지 않으므로 base few-shot은 툴 이름조차 모른다. 이 축은 결정 2가 이미 방어하고 있어 7번을 양보해도 안전하다.

> **Day 3 진입 직후 최우선 확인** — bnb-4bit 설정의 compute dtype이 `bfloat16`이다. T4에서는 fp16 경로로 떨어져야 한다. Unsloth가 자동 감지하지만 여기서 막히면 그날 GPU 할당량이 통째로 날아간다.

### 결정 8 — 모델이 할 일과 규칙이 할 일을 분리

"놓친 걸 케어한다"를 구현할 때 갈림길이 있다.

- **A층 — 입력을 쉽게.** 말하면 기록된다 → **파인튜닝 모델. 평가 대상**
- **B층 — 놓친 걸 알림.** "심장사상충 약 32일째" → **규칙 엔진. LLM 아님**

B층을 LLM으로 만들면 진단 영역으로 미끄러지고, 애초에 필요도 없다. "마지막 투여 후 30일 경과"는 날짜 뺄셈이지 추론이 아니다. 규칙이 더 정확하고 빠르고 안전하다.

**"모델을 안 쓸 곳을 아는 것"이 포트폴리오에서 강한 신호다.** 요즘 전부 LLM으로 때우려 하기 때문이다.

### 결정 9 — 시간 표현은 모델이 정규화하지 않는다

초안의 출력 스키마는 `"time": "2026-09-01T08:00"` 처럼 절대 시각이었다. 그런데 **결정 2로 모델 입력이 발화 한 줄뿐이라 모델은 오늘이 며칠인지 알 방법이 없다.** 학습 데이터의 날짜를 외울 뿐이고 평가에서는 전부 틀린다. 설계 충돌이었다.

두 방향을 놓고 비교했다.

| | 방식 |
| --- | --- |
| **A안** | 입력 앞에 기준 시각을 붙이고(`[2026-09-01 08:12] 아침에 사료 줬어`) 모델이 `2026-09-01T08:00`을 계산 |
| **B안** | 모델은 `"아침"`을 그대로 출력하고, 앱 코드가 발화 수신 시각과 합쳐 변환 |

**B안을 채택했다.** 최종 저장값은 어느 쪽이든 같고, **누가 계산하느냐만 다르다.**

| 항목 | A안 | B안 | 유리 |
| --- | --- | --- | --- |
| **오답 원인 분리** | 라우팅 실패와 산술 실패가 7분류표 같은 칸에 섞임 | 라우팅 실패만 남음 | **B** |
| **정답 신뢰도** | GPT가 날짜를 틀리면 정답이 오염됨 (결정 3의 장점이 깨짐) | 산술이 개입하지 않음 | **B** |
| **검수 부담** | 테스트 200건 시간 필드를 손으로 계산해 검증 | 문자열 확인으로 끝 | **B** |
| **채점 기준** | `"아침"`은 몇 시인가, 허용 오차는 — 자의적 | 문자열 일치 | **B** |
| **결정 8 정합** | 날짜 뺄셈을 다시 모델에 얹음 | 규칙 엔진에 맡김 | **B** |
| **실사용 정확성** | 자정 경계·타임존을 모델이 추측 | 기기 시각을 그대로 사용 | **B** |
| 난이도 층 | 산술 난이도 추가 | 시제 판별만 남음 | A |
| 출력 완성도 | 그대로 저장 가능 | 후처리 함수 필요 (20줄 규모) | A |

A가 이기는 두 칸을 검증해보면 실효가 약하다.

- **"난이도가 올라간다"** — 올라가는 건 날짜 산술 난이도이고, 이 프로젝트가 재려는 건 툴 라우팅이다. 재려는 능력에 다른 능력이 섞이면 그 숫자의 의미가 약해진다
- **"base와 격차가 커진다"** — 결정 2로 base는 툴 이름조차 모른다. 격차는 이미 확보돼 있다

반대로 **A의 비용은 이 프로젝트의 차별점(7분류 분해 · 사람 검수 200건 · 결정 8)을 직접 깎는다.** 강점을 깎아서 난이도를 사는 거래라 채택하지 않았다.

#### A안은 버리지 않고 Day 7로 보낸다

**B로 만든 데이터셋에서 A형 데이터는 코드로 파생된다.**

```
B 라벨   time = "저번주 화요일"
   ↓     기준 시각을 임의 배정하고 정규화 함수를 돌린다
A 라벨   time = "2026-08-25",  입력 앞에 "[2026-09-01 08:12]" 부착
```

GPT 재호출도 재검수도 필요 없고, **라벨을 GPT가 아니라 규칙 엔진이 계산하므로 A의 최대 약점인 정답 오염도 사라진다.** 반대 방향(A → B)은 표층 표현이 라벨에서 사라져 복원이 안 된다. **B가 상위 집합이다.**

Day 7에 이 실험을 돌리면 결과가 어느 쪽이든 쓸 문장이 나온다.

| 결과 | README에 쓸 문장 |
| --- | --- |
| A가 눈에 띄게 나쁨 | "날짜 산술을 규칙으로 뺀 판단이 옳았다. 모델에 맡기면 완전 일치율 nn%p 하락" |
| A가 비슷함 | "4B급도 정규화를 학습할 수 있었다. 그럼에도 타임존·자정 경계 정확성 때문에 규칙을 택했다" |

**결정 8·9가 주장에서 측정으로 바뀐다.** 실패할 수 없는 실험이라 버퍼 일정에 얹기 좋다.

**한계는 명시한다** — 이 실험이 재는 것은 "모델이 우리가 정한 정규화 관례를 학습할 수 있는가"이지 절대적인 시간 이해력이 아니다. `"아침" → 08:00`은 우리가 함수에 정한 관례다. 그리고 그 자의성이야말로 정규화를 코드에 두는 근거다.

#### 같은 원칙의 확장 — 1인칭 `caregiver`는 `null`

결정 9의 핵심은 **"앱이 확실히 아는 것을 모델이 맞히게 하지 않는다"** 이다. 이 원칙이 `caregiver`에도 그대로 적용된다.

| 발화 | `caregiver` | 앱이 채우는 값 |
| --- | --- | --- |
| "약 먹였어" | `null` | 로그인 사용자 |
| "내가 약 먹였어" | `null` | 로그인 사용자 |
| "엄마가 약 먹였대" | `"엄마"` | 그대로 |

발화한 사람이 누구인지는 앱이 아는 정보다. 발화 수신 시각과 성격이 같다.

**기술적 이유도 겹친다.** 한국어 1인칭은 `나 + 가` → `내가`로 형태가 바뀌어 발화 안에 `"나"`라는 문자열이 남지 않는다. 조사를 뗀 라벨을 만들 수 없어 결정 14의 라벨 검증을 통과할 수 없다. 3인칭(`"엄마가"` → `"엄마"`)에는 이 문제가 없다.

**`null`이 "미언급"과 "본인"을 함께 뜻하게 되지만 실익 손실이 없다.** 둘 다 앱 사용자 본인이 한 일이라 앱이 같은 이름으로 채운다. 구분이 필요한 쪽은 3인칭이고, 그것은 정확히 남는다 — `caregiver` 필드의 조사 근거가 *"여러 명이 돌보면 누가 투약했는지 불확실해진다"* 였으므로 원래 목적에도 맞다.

**난이도는 오히려 옮겨간다.** 모델이 "1인칭 주어는 슬롯에 넣지 않는다"를 학습해야 하고, 이는 발화에 있는 표현을 무조건 담지 않는다는 판별이라 결정 5(자연어 모호성)의 난이도에 해당한다.

### 결정 10 — RAG를 쓰지 않는다

"반려견마다 품종이 다른데 각각의 특징을 AI가 알아야 하지 않나"라는 질문에서 나온 결정이다.

#### 모델의 태스크에는 품종 지식이 개입할 자리가 없다

```
"콩이 아침에 사료 반 그릇 줬어"  → log_meal(...)
"몸무게 5.2킬로"                  → log_weight(5.2)
```

말티즈든 리트리버든 "사료 반 그릇 줬어"는 똑같이 `log_meal`이다. **기록하고 꺼내오는 일이라 대상이 무엇인지 알 필요가 없다.**

#### 품종 지식이 필요해지는 순간은 안전 경계 밖이다

| 질문 | 필요한 것 | 판정 |
| --- | --- | --- |
| "5.2kg이면 정상인가?" | 품종별 표준 체중 | **판단 = 안전 경계 밖** |
| "물을 이만큼 마시는 게 많은 건가?" | 체중당 음수량 기준 | **판단 = 안전 경계 밖** |
| "이 나이면 노령인가?" | 품종별 노령 진입 시기 | **판단 = 안전 경계 밖** |

전부 해석·판단이다. "정상인가요?"에 답하는 순간 진단 영역이고 그건 수의사의 일이다.

#### 설령 넣는다 해도 RAG는 맞지 않는다

| | RAG가 맞는 경우 | 품종 표준값 |
| --- | --- | --- |
| 데이터 형태 | 비정형 문서 (긴 글, PDF) | **구조화된 표** |
| 찾는 방식 | 의미 유사도로 관련 부분 검색 | **키로 정확히 조회** |
| 답의 성격 | 문서 내용을 요약·인용 | 숫자 하나 |

품종 300개짜리 표에서 "말티즈" 행을 꺼내는 건 DB 조회다. 벡터 검색을 쓰면 정확한 값 대신 "비슷한 걸 찾아오는" 방식이 되어 **오히려 부정확해진다.**

**결정 8과 같은 논리다.** 날짜 뺄셈을 LLM에 안 맡긴 것처럼, 표 조회도 검색에 안 맡긴다.

#### 품종 정보는 프로필에 한 번 넣으면 된다

```
사용자 프로필 (앱 상태)   품종: 말티즈, 나이: 4세, 체중: 5.2kg
     ↓
모델                      발화 → Tool Call 변환만 (품종 모름)
     ↓
규칙 엔진 (B층)           필요하면 프로필 + 품종 표준 테이블 참조
```

매 발화마다 모델이 알 필요가 없다. 오히려 모델 입력에 품종을 넣으면 **결정 2를 깎아먹는다** — 툴 라우팅에 무관한 정보라 입력만 길어지고 노이즈가 된다.

#### 층이 하나 늘어난다

| 층 | 하는 일 | 담당 | MVP |
| --- | --- | --- | --- |
| **A층** | 발화 → Tool Call | 파인튜닝 모델 | **포함** |
| **B층** | 주기 계산, 놓친 항목 알림 | 규칙 엔진 | 확장 |
| **C층** | 품종별 표준값 참조 | **참조 테이블 (RAG 아님)** | 확장 |

#### 7일 프로젝트에 RAG를 넣으면 안 되는 실질적 이유

1. **둘 다 얕아진다** — MCP를 2단계로 미룬 것과 정확히 같은 이유
2. **평가가 흐려진다** — 파인튜닝 효과를 재는 게 목적인데 RAG가 끼면 기여도 분리가 안 된다
3. **안전 경계와 충돌한다** — 의학 정보를 검색해 답하기 시작하면 진단으로 미끄러진다

#### README에 쓸 문장

> **왜 RAG를 안 썼는가** — 모델의 태스크는 형식 변환이라 지식이 필요 없다. 품종별 표준값 같은 참조 데이터는 구조화된 표이므로 벡터 검색이 아니라 키 조회가 맞다. 그리고 그 값으로 "정상인가"를 판단하는 것은 안전 경계 밖이다.

"왜 안 썼나"를 근거와 함께 설명하는 것이 "다 넣었습니다"보다 강하다. 결정 8·9와 같은 계열의 판단이라 문서 전체가 일관돼 보이기도 한다.

---

### 결정 11 — 수량도 모델이 정규화하지 않는다 (결정 9의 확장)

초안 스펙에는 숫자형 파라미터가 셋 있었다 — `amount_ml`, `duration_min`, `weight_kg`. Day 0에 툴 스펙을 채우다 결정 9와 정면으로 부딪혔다.

```
"물 좀 많이 마셨어"     → amount_ml = ?   추정해야 한다
"산책 좀 짧게 했다"     → duration_min = ?  추정해야 한다
"5킬로 200 나왔어"      → weight_kg = 5.2   단위 산술이 필요하다
```

**전부 자유 문자열로 통일했다.** `amount`·`duration`·`weight`로 이름을 바꾸고 표층 표현을 그대로 담는다. 근거는 결정 9와 완전히 동일하다.

1. **오답 원인이 섞이지 않는다** — 수량 추정 실패가 툴 라우팅 실패와 같은 칸에 떨어지면 안 된다
2. **정보가 버려지지 않는다** — 숫자형을 유지하고 모호할 때 null을 넣으면 `"많이"`라는 실제 정보가 통째로 사라진다. 문자열이면 앱 코드가 나중에 해석할 여지가 남는다
3. **채점이 통일된다** — 모든 비-enum 파라미터가 문자열 일치로 채점된다. 숫자 허용 오차 같은 자의적 규칙이 필요 없다
4. **결정 8과 정합** — 단위 변환은 계산이지 추론이 아니다

#### 정규화 함수는 무게에만 둔다

`"5킬로 200"` → `5.2` 변환은 `normalize_time`과 나란히 **`normalize_weight`** 로 앱 코드에 둔다.

**`amount`·`duration`은 변환하지 않는다.** 셋의 성격이 다르기 때문이다.

| 값 | 변환 | 가능한가 |
| --- | --- | --- |
| `"5킬로 200"` → `5.2` | 단위 산술 | **확정적으로 가능** |
| `"반 그릇"` → 절대량 | 그릇 크기를 모름 | **불가능** |
| `"짧게"` → 분 | 기준이 없음 | **불가능** |

결정 11이 남긴 것은 **"나중에 해석할 여지"이지 "변환 가능성"이 아니다.** `"반 그릇"`의 절대량은 앱 코드도 모르고, 사용자별 급여량 기준이 쌓이기 전에는 누구도 계산할 수 없다. 표층 그대로 저장하고 해석을 미룬다.

### 결정 12 — 닫힌 집합은 영문 정규화, 열린 집합만 표층

결정 9·11이 "정규화하지 마라"로 읽히면 과잉 적용된다. 경계를 명확히 그었다.

| 집합 성격 | 처리 | 파라미터 |
| --- | --- | --- |
| **닫힌 집합 (enum)** | **영문 키로 정규화** | `type`·`condition`·`care_type`·`activity_type`·`severity`·`record_type`·`metric` |
| **열린 집합** | **표층 그대로** | `food`·`amount`·`name`·`dose`·`symptom`·`duration`·`weight`·`caregiver` |
| **시간 표현** | **표층 그대로** | `time`·`since`·`until`·`date`·`period` |

결정 9가 배제한 것은 **열린 집합의 변환**(무한한 시간·수량 표현을 계산으로 옮기는 일)이지, **닫힌 집합의 분류**가 아니다. `"응가" → feces`는 계산이 아니라 분류이고, 분류야말로 이 프로젝트가 재려는 능력이다.

영문을 택한 이유가 셋이다.

1. **실제 Tool Call API 관례와 같다** — 앱 코드가 그대로 받아 쓴다
2. **표층 파라미터와 역할이 눈으로 구분된다** — 라벨만 봐도 어느 슬롯이 분류이고 어느 슬롯이 받아쓰기인지 알 수 있다. 라벨링 가이드가 짧아진다
3. **base와의 격차 확보에 유리하다** — 한글 표층을 그대로 쓰면 태스크가 "발화에서 단어 오려붙이기"에 가까워져 few-shot base도 어느 정도 해낸다. 결정 2와 같은 방향이다

### 결정 13 — 약은 전부 `log_medication`, `log_care`는 위생 관리

체크리스트의 경계 케이스 `"구충제 줬어" → medication인가 care인가`를 규칙으로 확정했다. 초안에서는 `log_care`의 근거 항목에 "구충"이 들어가 있어 두 툴이 겹쳤다.

| 툴 | 재정의 |
| --- | --- |
| `log_medication` | **약이면 전부.** 처방약·구충제·심장사상충약·영양제. 먹이든 바르든 무관 |
| `log_care` | **약이 아닌 위생 관리.** 양치·발톱·목욕·항문낭·빗질·귀 청소 |

**한 문장으로 정의되는 경계**를 택했다. "경구 투여인가"로 나누면 바르는 구충제에서 다시 흔들리고, 케이스별 판정은 라벨 200건을 사람이 검수할 때 일관성이 무너진다.

**난이도는 줄지 않는다.** 발화는 여전히 `"구충 했어"`, `"약 챙겨줬어"`처럼 모호하고, 모델은 이 규칙을 학습으로 익혀야 한다. 규칙이 없으면 학습이 안 되는 것이지 어려워지는 게 아니다.

#### 기록과 조회가 비대칭인 점

예방약은 `log_medication`으로 **기록**되지만 `get_care_due`로 **조회**된다.

```
"심장사상충 약 먹였어"   → log_medication(name="심장사상충약")
"심장사상충 약 언제야"   → get_care_due(care_type="heartworm")
```

의도한 비대칭이다. 기록은 사건이고 조회는 주기이며, 둘을 잇는 매핑(`"심장사상충약"` → `heartworm`)은 B층 규칙 엔진의 일이다. `get_care_due`의 값 도메인에만 `heartworm`·`deworming`·`vaccine`이 있고 `log_care`에는 없는 이유다.

### 결정 14 — 역방향 생성에 라벨 재정렬 검증을 넣는다

결정 3(역방향 생성)과 결정 9·11(표층 라벨)이 만나는 지점에 정합성 구멍이 있다.

```
샘플링된 정답   time="아침",  amount="반 그릇"
GPT 생성 발화   "콩이 오전에 사료 절반 정도 줬어"
                     ↑ 라벨에 없는 표현
```

모델이 발화대로 `"오전"`·`"절반 정도"`를 출력하면 **정답 동작인데 오답으로 채점된다.** 완전 일치율이 이유 없이 깎이고 7분류표의 "파라미터 오류" 칸이 오염된다. 역방향 생성은 정답을 먼저 확정하므로 라벨은 절대 틀리지 않지만, **라벨과 발화가 어긋날 수는 있다**는 것이 결정 3의 사각지대였다.

**생성 후 검증 단계를 넣는다.**

1. 표층 계열 파라미터(`free`·`time`)의 라벨 문자열이 발화 안에 실제로 존재하는지 확인
   - **조사를 뗀 상태로 비교한다.** 라벨 `"아침"`과 발화 `"아침에"`는 일치로 본다. 시드 사전이 조사 없는 형태로 통일돼 있으므로(`lexicon.py`), 조사 차이만으로 재정렬이 일어나면 안 된다
2. 없으면 **발화가 아니라 라벨을 고친다** — 발화에서 해당 슬롯의 실제 표현을 찾아 라벨을 재정렬
3. 어느 표현이 그 슬롯인지 특정할 수 없으면 그 건을 버린다

**오타 페르소나가 라벨 슬롯을 때리면 3번에 걸려 통째로 폐기된다.** `"콩이 아치베 사료 줬어"`에서 `"아치베"`가 시간 슬롯임을 자동 판별할 수 없기 때문이다. 재정렬로 살려도 오타가 정답으로 학습돼 일반화가 안 된다. 그래서 **오타는 조사·어미·띄어쓰기에만 넣고 파라미터 값이 되는 단어는 정확히 쓰도록** 페르소나 정의에 못박았다. 입력이 거칠다는 취지는 살고 라벨은 안전하다.

발화를 다시 만들게 하는 대신 라벨을 맞추는 이유는 **발화의 자연스러움이 이 데이터셋의 자산**이기 때문이다. 결정 5(난이도는 자연어 모호성으로)를 깎지 않는다.

**부수 효과** — 파라미터 값 분포가 균등 샘플링에서 약간 흐트러진다. 균등성은 어휘 시드 사전이 이미 담당하고 있고, 재정렬은 표현만 바꿀 뿐 enum 값과 툴은 건드리지 않으므로 결정 3의 핵심(툴·파라미터 조합의 균등 분포)은 유지된다.

**버려지는 비율을 기록한다.** 재정렬 실패율이 높으면 역방향 생성 프롬프트가 라벨을 무시하고 있다는 신호이므로, 프롬프트 수정의 지표가 된다.

### 결정 15 — 이상 징후는 전용 칸이 있는 툴이 담당한다

체크리스트의 경계 케이스 두 개를 하나의 규칙으로 묶었다.

```
"설사했어"           log_excretion 인가 log_symptom 인가
"산책하다 주저앉아"   log_activity 인가 log_symptom 인가
```

값 도메인이 실제로 겹쳐 있었다 — `excretion_condition.diarrhea`의 표층 어휘에도 `"설사"`가 있고 `SYMPTOM` 목록에도 `"설사"`가 있었다. 규칙 없이 역방향 생성을 돌리면 **같은 발화에 정답이 둘인 데이터**가 만들어진다. 학습이 상쇄되고, 7분류표의 `툴 오류` 칸이 오염되며, 무엇보다 **테스트 200건 검수에 판단 기준이 없어진다.**

#### 규칙

> **이상 상태를 담을 전용 칸이 그 툴에 있으면 그 툴, 없으면 `log_symptom`.**

전용 칸은 둘뿐이다 — `log_excretion.condition`, `log_symptom.severity`.

| 발화 | 담을 칸 | 판정 |
| --- | --- | --- |
| "설사했어" | `condition=diarrhea` | `log_excretion` |
| "혈변 봤어" | `condition=bloody` | `log_excretion` |
| "오줌 색이 진해" | `condition` | `log_excretion` |
| "산책하다 주저앉아" | 없음 | `log_symptom` |
| "밥을 안 먹어" | 없음 (`amount`는 양 칸이지 상태 칸이 아니다) | `log_symptom` |
| "토했어" | 없음 | `log_symptom` |

#### 왜 이 규칙인가

**이미 내린 결정을 문장으로 옮겨 적은 것뿐이다.** `excretion_condition`에 `soft`·`diarrhea`·`hard`·`bloody`를 정의한 순간 "비정상 배변도 `log_excretion`이 담당한다"고 결정한 셈이다. 대안(이상이면 전부 `log_symptom`)을 택하면 저 네 값이 사실상 사장되어 **"enum을 왜 만들었나"** 라는 질문을 받는다.

**규칙이 스키마에서 도출되므로 자의적이지 않다.** 처음 보는 발화에도 적용되어 검수 중에 막히지 않는다. 결정 13의 "한 문장으로 정의되는 경계"와 같은 형태다.

#### 정보는 사라지지 않는다

`log_excretion`을 골라도 "이상하다"는 정보는 `condition`에 남는다. 그것을 이상 신호로 읽는 것은 **B층 규칙 엔진의 일**이다 — `condition in ("diarrhea", "bloody", "soft")` 조회이지 추론이 아니다. `get_vet_summary`는 배변 기록도 함께 훑으므로 요약에서 빠지지 않는다. 결정 8의 연장이다.

#### 가장 정확한 답은 채택하지 못했다 — 정직하게 기록한다

`"설사했어"`는 배변 사건이면서 동시에 증상이므로, **두 툴을 모두 호출하는 것이 현실에 가장 충실하다.** 채택하지 않은 이유는 그 방식이 틀려서가 아니라 **결정 1이 병렬 독립 다중 호출을 범위 밖으로 두었기 때문**이다.

배열 출력으로 바꾸면 채점기 7분류 재설계 · 데이터 2,400건 재생성 · GPT-4.1 베이스라인 재측정이 따라와 7일 중 1~2일이 소모된다. P0(완주)을 직접 위협하는 거래라 미뤘다.

**Day 7의 병렬 다중 호출 확장에서 `"설사했어"`를 대표 사례로 쓴다.** 단일 호출 제약이 무엇을 잃게 하는지 숫자로 낼 수 있고, 그러면 이 타협이 주장이 아니라 측정이 된다. 결정 9의 A안을 Day 7로 보낸 것과 같은 패턴이다.

> **한 사건 한 발화와 두 사건 한 발화는 다른 문제다.** `"설사했어"`는 사건 하나를 두 가지로 읽는 경우이고, `"설사도 하고 주저앉기도 했어"`는 사건이 둘이라 `ambiguous`로 거절한다. 후자에서 하나만 저장하면 나머지가 조용히 사라져 "보호자의 기억을 붙잡아둔다"는 목적과 정반대가 된다 (결정 4).

### 결정 16 — 조회계 경계는 세 축으로 가른다

조회계 4종(`get_records`·`get_trend`·`get_care_due`·`get_vet_summary`)은 값 도메인이 크게 겹친다. `record_type` 8개와 `trend_metric` 6개 중 **6개가 같다.** 축을 셋으로 나눠 정리한다.

#### 축 ① 나열이냐 요약이냐 — `get_records` / `get_trend`

> **개별 기록을 나열해서 답이 되면 `get_records`, 여러 건을 합쳐야 답이 되면 `get_trend`.**

파라미터 설계에서 도출된 규칙이다.

| 툴 | 시간 파라미터 | 설계 의도 |
| --- | --- | --- |
| `get_records` | `since` · `until` | **양 끝을 정한 구간.** 그 사이 기록을 꺼낸다 |
| `get_trend` | `period` | **덩어리 기간.** 하나의 값으로 요약한다 |

`since`·`until` **둘로** 범위를 잡는 것은 나열 설계이고, `period` **하나로** 받는 것은 요약 설계다.

| 발화 | 판정 |
| --- | --- |
| "오늘 물 마셨어?" | `get_records(record_type="water", since="오늘")` |
| "어제 밥 몇 번 줬어?" | `get_records` |
| "이번주 산책 몇 번 했어?" | `get_records` |
| **"요즘 물 얼마나 마셔?"** | **`get_trend(metric="water", period="요즘")`** |
| "물 마시는 게 늘었나?" | `get_trend` |
| "체중 변화 어때?" | `get_trend` |

**시간 표현은 보조 신호일 뿐 주된 기준이 아니다.** `"이번주"`는 `since`에도 `period`에도 들어갈 수 있어서, `"이번주 산책 몇 번?"`(세면 됨 → records)과 `"이번주 산책량 어때?"`(합쳐야 함 → trend)가 갈린다. 기준은 언제나 나열이냐 요약이냐다.

**`get_trend`를 없애고 `get_records`로 통합하는 안은 버렸다.** `aggregate` 같은 플래그로 옮기면 모델이 판단할 내용은 그대로인데 파라미터만 늘고 `until`이 무의미해진다. 무엇보다 조사에서 나온 `get_trend`의 목적 — *"매일 보면 점진적 변화를 못 느낌"* — 이 사라진다. 음수량 증가처럼 하루치로는 안 보이고 일주일을 겹쳐야 보이는 신호가 이 툴의 존재 이유다.

> **`get_trend`도 판단하지 않는다.** `"지난주 평균의 2배"`까지가 출력이고 `"이상한가"`는 보호자가 느끼고 수의사가 판단한다. §0의 역할 분담에서 **"뭔가 이상한데?" 하고 느끼는 단계를 돕는 툴**이다.

#### 축 ② 진료 맥락과 종류 특정 — `get_vet_summary` / `get_records`

> **진료·병원 맥락이 있고 기록 종류를 특정하지 않으면 `get_vet_summary`. 종류가 특정되면 맥락이 있어도 `get_records`.**

`get_vet_summary`의 파라미터가 `since` 하나뿐이라는 것이 근거다. **종류를 좁힐 칸이 아예 없다.** 전체를 훑어 추리는 것이 이 툴의 일이므로, `"약 기록만"` 같은 요청은 담을 수단이 없다.

| 발화 | 진료 맥락 | 종류 특정 | 판정 |
| --- | --- | --- | --- |
| "최근 기록 좀 보여줘" | ✗ | ✗ | `get_records` |
| "이번주 밥 기록 보여줘" | ✗ | ✓ | `get_records` |
| **"병원 갈 건데 최근 기록 좀"** | ✓ | ✗ | **`get_vet_summary(since="최근")`** |
| "내일 진료 있어서 정리 좀" | ✓ | ✗ | `get_vet_summary` |
| **"병원 갈 건데 약 먹인 기록만"** | ✓ | ✓ | **`get_records(record_type="medication")`** |

**난이도는 진료 맥락 쪽에 있다.** `"내일 데려가야 하는데 요 며칠 어땠는지 정리 좀"` 처럼 `"병원"`·`"진료"`·`"수의사"` 어느 단어도 없이 맥락만으로 판별해야 하는 발화가 나온다. 어휘 매칭이 아니라 맥락 판별이므로 결정 5가 말한 난이도에 해당한다.

**`get_vet_summary`를 없애고 `get_records`로 통합하는 안은 버렸다.** 이 툴은 조사에서 발견한 신규 툴 3개 중 하나이고(§2), 앱이 "지금이 진료용인지"를 알 방법이 없어 요약 시점을 판단할 수 없다.

#### 축 ③ 묻는 목적 — `get_records` / `get_care_due`

> **"무슨 일이 있었나"를 물으면 `get_records`, "이제 해야 하나"를 물으면 `get_care_due`.**
> **시제 어미가 아니라 묻는 목적으로 가른다.**

초안은 "과거형이면 `get_records`, 미래형이면 `get_care_due`"였으나 **폐기했다.** 실제 발화의 상당수가 과거형으로 미래를 묻기 때문이다.

| 발화 | 겉보기 시제 | 묻는 목적 | 툴 |
| --- | --- | --- | --- |
| "심장사상충 약 언제 줬지?" | 과거 | 있었던 일 | `get_records` |
| "지난달에 구충제 했나?" | 과거 | 있었던 일 | `get_records` |
| **"심장사상충 약 한 달 됐나?"** | **과거** | **이제 할 때인가** | **`get_care_due`** |
| **"구충제 안 밀렸나?"** | **과거** | **놓쳤는지 확인** | **`get_care_due`** |
| "약 챙길 때 됐나?" | 과거형 어미 | 예정 확인 | `get_care_due` |
| "예방접종 다음이 언제지?" | 미래 | 이제 할 때인가 | `get_care_due` |

`"한 달 됐나"`·`"안 밀렸나"`가 이 축의 핵심 난이도다. **마지막 투여로부터 얼마나 지났는지를 통해 다음 차례가 됐는지를 확인하는 발화**이고, 예방약 연간 망각 46~51%(§2)에서 나온 `get_care_due`의 실제 사용 장면이다. 어미만 보는 규칙으로는 절대 잡히지 않는다.

**`"심장사상충 약 언제야"`는 경계 케이스에서 뺐다.** 사람이 읽어도 세 가지로 갈리기 때문이다 — 언제 먹였나(`get_records`) · 언제 줘야 하나(`get_care_due`) · 언제 샀나(툴 없음 → `out_of_scope`). 사람이 못 가르는 것을 모델에게 시킬 수 없으므로 `ambiguous` 예시로 옮겼다. 결정 4의 *"정보가 부족한데 지어내는 것이 더 위험하다"* 에 해당한다.

#### 조회계 3종의 역할 구분

축 ①·②·③을 합치면 조회계가 이렇게 갈린다. 값 도메인이 겹쳐도 **하는 일이 다르다.**

| 툴 | 하는 일 | 출력 형태 |
| --- | --- | --- |
| `get_records` | **꺼내기** | 기록을 그대로 나열 |
| `get_trend` | **합치기** | 여러 건을 하나의 값으로 — "지난주의 2배" |
| `get_vet_summary` | **추리기** | 여러 종류를 훑어 진료에서 말할 것만 골라 정리 |
| `get_care_due` | **앞을 보기** | 주기 항목의 다음 차례 |

### 결정 17 — 발화 자연스러움을 사람이 검증한다

결정 16 축③을 정하다가 발견한 문제다. 경계 케이스로 적어둔 `"심장사상충 약 언제야"`를 사람이 읽었을 때 **"이게 뭘 물어보는 거지?"** 가 먼저 나왔다. 예시 자체가 실제로 쓰이지 않는 말이었다.

#### 결정 14로는 못 잡는다

| | 잡는 것 | 방식 |
| --- | --- | --- |
| **결정 14** | 라벨이 발화 안에 있는가 | **자동** (문자열 비교) |
| **결정 17** | 이 말을 실제로 하는가 | **사람** |

결정 14는 라벨과 발화의 **정합성**만 본다. 발화가 애초에 어색해도 라벨만 맞으면 통과한다. 그런데 결정 14 자신이 *"발화의 자연스러움이 이 데이터셋의 자산"* 이라고 선언해 두었으므로, **자산을 지킬 절차가 없던 셈이다.**

#### 조회계가 특히 위험하다

| 계열 | 왜 |
| --- | --- |
| 기록계 (`log_*`) | **혼잣말이라 안전하다.** `"밥 줬어"`·`"토했어"`는 실제로 그렇게 말한다 |
| **조회계 (`get_*`)** | **앱에 묻는 말이라 아무도 해본 적이 없다.** 상상해서 쓰게 되고, 상상한 말은 어색해진다 |

실제로 지금까지 어색하다고 걸린 예시는 전부 조회계였다.

#### 판정 기준

> 발화를 읽고 **"얘가 지금 뭘 물어보는 거지?"** 라는 생각이 들면 버리거나 고친다.

주관적으로 보이지만 날카롭다. `"심장사상충 약 언제야"`가 이 기준에서 걸렸고, 그 과정에서 세 번째 해석(`"언제 샀나"`)까지 드러났다. **사람이 세 갈래로 읽는 발화는 모델도 맞히지 못한다.**

#### 적용 지점 네 곳

| 지점 | 무엇을 |
| --- | --- |
| **경계 케이스 목록** | 목록의 예시 발화부터 이 기준을 통과해야 한다 |
| **역방향 생성 프롬프트** | 조회계는 "앱에 말하듯"이 아니라 **"가족에게 묻듯"** 쓰게 지시한다 |
| **30건 샘플 테스트** | 판정 기준을 명시한다. 여기서 걸러야 재생성 비용을 안 문다 |
| **테스트 200건 검수** | 라벨만 보지 말고 발화도 본다. **조회계를 따로 모아** 점검 |

두 번째가 실질적인 해법이다. 질문형 페르소나의 `"가족에게 묻듯 짧게"` 지시가 `"오늘 밥 줬나?"` 라는 자연스러운 발화를 만들어냈으므로, 이 원칙을 조회계 전체로 넓힌다.

#### 왜 지금 잡아야 하는가

2,400건을 생성한 뒤에 발견하면 **재생성 비용을 그대로 문다.** 그리고 경우 B(성능이 안 나옴)의 원인 판별 순서에 이 항목이 없어서, 어색한 발화로 학습해 일반화가 실패해도 **원인을 찾지 못한다.** 생성 전에 절차로 박아두는 것이 유일하게 싼 시점이다.

---

## 4. 리스크와 대응

### 경우 A — base 모델이 이미 잘함

먼저 안심할 근거 — **툴 이름 정확도가 95%여도 완전 일치율과 스키마 준수율은 훨씬 낮은 게 보통이다.** 지표를 7분류로 쪼개면 대개 차이가 드러난다.

그래도 차이가 없으면 순서대로 조정한다.

1. 자연어 모호성 강화 (구어체·단위 모호·시제 생략 비중 확대)
2. 혼동 쌍 비중 확대 — 경계 케이스를 더 촘촘히 깐다. **정규화 난이도로 올리는 방식은 쓰지 않는다** (결정 9)
3. **툴 스펙을 프롬프트에서 제거** ← 결정 2로 이미 채택. 선제 대응 완료
4. 더 작은 모델(1.5B급)로 교체 — 작을수록 파인튜닝 효과가 크다

### 경우 B — 파인튜닝해도 성능이 안 나옴

**데이터 문제인지 모델 크기 문제인지 판별 순서.**

1. **학습셋으로 평가해본다**
   - 학습셋도 못 맞힘 → **학습 자체가 안 된 것.** 설정 문제(학습률·데이터 포맷·마스킹). 여기서 멈추고 설정부터 고친다
   - 학습셋은 높은데 테스트가 낮음 → **일반화 실패 = 데이터 다양성 부족**
2. **난이도 층별 분해**
   - 단순 1툴도 틀림 → 포맷·학습 문제
   - 단순한 건 맞는데 모호한 것만 틀림 → 데이터 문제
   - **기록계는 맞는데 조회계만 틀림 → 발화 자연스러움 문제** (결정 17). 조회계 발화는 상상해서 쓰게 되므로 어색해지기 쉽고, 그러면 실제 말투에 일반화되지 않는다. 학습 데이터의 조회계 발화를 사람이 다시 읽어본다
3. **손실(loss) 곡선**
   - 학습 손실이 안 떨어짐 → 학습률 문제
   - 학습 손실은 떨어지는데 검증 손실이 오름 → 과적합
4. **학습곡선 (250 / 1,000 / 2,000건)**
   - 아직 상승 중 → 데이터 부족
   - 평평 → 모델 한계

> 4번은 확장 실험이지만 **경우 B의 원인 판별을 겸한다.** 하나의 실험이 두 질문을 해결하므로 가성비가 가장 높다.

### 환경 리스크

| 리스크 | 대응 |
| --- | --- |
| Colab 세션 4시간 끊김 | 구글 드라이브 마운트, 체크포인트 주기적 저장 |
| 일일 GPU 할당량 소진 → 며칠 잠김 | Day 7을 통째로 버퍼로 비움. GPU 불필요 작업(Day 1~2)을 앞에 배치 |
| T4가 안 잡히는 날 | 데이터·평가 작업을 앞당겨 처리 |
| GPT-4.1 데이터 생성 비용 | 프롬프트 확정 전 소량으로 검증하고 재생성 루프를 줄인다 |
| **작업 부담 증가** | **툴 12개 → 10개 축소. 순서는 `log_activity` → `log_weight`** (조사 근거가 상대적으로 약한 순) |

### 도메인 리스크

**진단·처방으로 넘어가는 것이 가장 큰 위험이다.** 잘못된 조언은 실제로 동물을 해칠 수 있다. `log_symptom`은 증상을 **기록**할 뿐이고, 진단 요청은 `out_of_scope`로 거절한다. 이 경계를 README와 스키마 양쪽에 박아둔다.

---

## 5. 배경 개념과 용어집

### 5-1. 프롬프트 · 파인튜닝 · RAG — 무엇이 다른가

> 이 절은 **왜 이 프로젝트가 파인튜닝인가**를 설명한다. README의 "왜 파인튜닝인가" 절로 거의 그대로 옮겨 쓸 수 있다.

#### 한 줄 차이

| | 손대는 것 | 모델 자체는 |
| --- | --- | --- |
| **프롬프트** | **입력** | 그대로 |
| **파인튜닝** | **모델의 가중치** | 바뀜 |

**가중치(weights)** 는 모델 내부에 학습돼 있는 숫자들이다. "4B = 파라미터 40억 개"의 그 숫자들. 프롬프트는 이걸 안 건드리고, 파인튜닝은 이걸 조금 고친다.

- **프롬프트** — 매번 신입에게 매뉴얼을 쥐여주며 "이렇게 해줘"라고 말하는 것. 매뉴얼을 안 주면 원래대로 돌아간다
- **파인튜닝** — 그 사람을 훈련시켜 매뉴얼 없이도 몸에 배게 하는 것

#### ClickMe에서 겪은 것이 정확히 프롬프트 방식의 한계였다

generator 도메인에서 `_EDIT_PROMPT_TEMPLATE`에 "상품 패키지 인쇄 텍스트는 픽셀 그대로 보존", "상품이 사람에 가려지지 않게" 같은 지시문을 계속 추가했다. 이건 **매 호출마다 그 문장들을 모델에게 다시 보내는 것**이고, 모델은 다음 호출에서 이 지시를 기억하지 못한다.

그래서 `fix: FOMO 상품가림`, `fix: 텍스트 잔존`, `fix: 강조색 가독성` 커밋이 반복됐고 대응은 대부분 "지시문을 하나 더 추가"였다. **모델이 지시를 안 지키면 할 수 있는 게 지시를 더 길게 쓰는 것뿐인 구조**다. 프롬프트가 길어지면 앞의 지시가 묻히고, 또 안 지켜진다.

파인튜닝은 그 루프를 끊는 다른 층위의 도구다.

> **ClickMe에서 파인튜닝을 안 한 게 아니라 못 했다.** `gpt-image-1`·`gemini-2.5-flash-image`는 API로만 쓰는 모델이라 가중치에 접근할 수 없다. 파인튜닝을 하려면 **오픈 웨이트 모델**(가중치가 공개돼 내려받을 수 있는 모델)이 필요하고, 이번에 4B급 오픈 모델을 쓰는 이유가 그것이다.

#### 프롬프트 vs 파인튜닝 비교

| 축 | 프롬프트 | 파인튜닝 |
| --- | --- | --- |
| 바꾸는 대상 | 입력 텍스트 | 모델 가중치 |
| 변경 반영 | **즉시** | 재학습 필요 (수십 분~수 시간) |
| 필요 데이터 | 0건 ~ 예시 몇 개 | **수백~수천 건** |
| 비용 구조 | 매 호출마다 지시 토큰 값 | 학습 1회 비용, 이후 추론은 저렴 |
| 잘하는 것 | 맥락 주입, 빠른 실험, 자주 바뀌는 규칙 | **형식 준수, 스타일 내재화, 반복 태스크** |
| 약한 것 | **형식 안정성**, 긴 프롬프트 비용 | 자주 바뀌는 규칙 |

#### 학습은 "자료를 넣는 것"이 아니라 "짝을 보여주는 것"

이미지 수백 장을 던지는 게 아니라 **입력과 정답을 짝지어** 준다.

| | 입력 | 정답 |
| --- | --- | --- |
| 이미지 생성 파인튜닝이라면 | "FOMO 전략 광고, 상품은 텀블러" | 실제로 그렇게 만들어진 이미지 |
| **이번 프로젝트** | "콩이 아침에 사료 반 그릇 줬어" | `{"tool": "log_meal", ...}` |

이 짝을 2,000개 반복해 보여주며 "이런 입력엔 이런 출력"을 익히게 한다. 그래서 SFT(Supervised Fine-Tuning)가 "입력-정답 쌍을 주고 학습시키는 방식"인 것이다. **파인튜닝은 이미지 전용 기법이 아니라, 무엇을 짝지어 주느냐에 따라 대상이 달라진다.**

#### ⚠ 가장 흔한 오해 — 문서·PDF를 넣으면 그 내용을 알게 된다?

**아니다.** PDF 100장을 파인튜닝에 넣으면 모델은 그 문서들의 **말투와 형식을 흉내내게** 될 뿐이다. "3페이지에 뭐라고 써 있어?"라고 물으면 못 답하고 **그럴듯한 걸 지어낸다.** 사실이 정확히 저장되는 게 아니라 통계적으로 흐릿하게 스며들기 때문이다.

문서 내용을 근거로 답하게 하려면 **RAG**를 쓴다. 답하기 전에 문서를 검색해 그 내용을 프롬프트에 넣어주는 방식이다.

| | 파인튜닝 | RAG |
| --- | --- | --- |
| 가르치는 것 | **행동·형식·말투** | **사실·지식** |
| 문서가 바뀌면 | 재학습 | 문서만 교체 |
| "출처가 뭐야?" | 답 못 함 | 답할 수 있음 |

> **파인튜닝은 "어떻게 답할지"를 가르치고, RAG는 "무엇을 답할지"를 알려준다.**

**이번 프로젝트가 파인튜닝인 이유가 여기 있다.** "발화를 JSON으로 바꾼다"는 형식 변환이지 지식이 필요한 일이 아니다. 만약 "우리 강아지 품종 정보를 알려줘" 같은 기능이었다면 RAG를 썼어야 한다.

#### 파인튜닝은 딥러닝의 일부다

파인튜닝은 딥러닝과 비슷한 게 아니라 **딥러닝 그 자체**다.

- **딥러닝** — 신경망에 데이터를 주고 가중치를 조정해 학습시키는 것
- **LLM** — 그렇게 만들어진 딥러닝 모델
- **파인튜닝** — 이미 학습된 그 모델의 학습을 **조금 더 이어서 하는 것**

차이는 종류가 아니라 **규모**다.

| | 사전학습(pretraining) | 파인튜닝 |
| --- | --- | --- |
| 데이터 | 수조 단어 | **2,000건** |
| 시간 | 수 주~수 개월 | **30분~수 시간** |
| 비용 | 수십억 원 | **무료 Colab** |
| 하는 일 | 언어를 처음부터 배움 | 이미 아는 걸 특정 작업에 맞춤 |

**"처음부터 학습"이 아니라 "이미 학습된 모델을 조금 조정"** 하는 것이라서 개인이 일주일 안에 할 수 있다. LoRA·QLoRA는 그 조정을 더 싸게 만드는 기법이다.

#### 셋을 언제 쓰나 — 최종 정리

| 하고 싶은 것 | 방법 | 예시 |
| --- | --- | --- |
| 빠르게 실험, 자주 바뀌는 규칙 | **프롬프트** | ClickMe 이미지 지시문 |
| 형식·말투·행동을 일관되게 | **파인튜닝** | **이번 프로젝트** |
| 특정 문서를 근거로 답하게 | **RAG** | 사내 문서 챗봇 |

셋은 경쟁 관계가 아니라 층이 달라 **함께 쓰기도 한다.** 실무에서는 "RAG로 문서를 찾아오고, 파인튜닝한 모델이 정해진 형식으로 정리하고, 프롬프트로 그때그때 톤을 조정"하는 식으로 겹쳐 쓴다.

#### 이 프로젝트의 비교 설계가 곧 이 질문의 답이다

| 비교 대상 | 이게 곧 |
| --- | --- |
| GPT-4.1 + 툴 목록 프롬프트 | **프롬프트 방식 (강한 모델)** |
| Base 4B + few-shot | **프롬프트 방식 (약한 모델)** |
| Base 4B + 파인튜닝 | **파인튜닝 방식** |

2번과 3번의 차이가 곧 "같은 모델에서 프롬프트 대신 파인튜닝을 했을 때 무엇이 달라지는가"다. **예상 하이라이트(정확도보다 형식 안정성이 크게 개선)** 는 위 비교표의 "잘하는 것 / 약한 것" 칸에서 나온 예측이다.

즉 ClickMe에서 겪은 "지시문을 늘려도 안 지켜지는" 문제가 실제로 파인튜닝으로 해결되는지를 직접 측정하게 된다.

---

### 5-2. 용어집

진행 중 나오는 용어. 모르는 게 있으면 여기부터. **새 용어가 나오면 여기에 추가한다.**

| 용어 | 뜻 |
| --- | --- |
| **파인튜닝** | 학습된 모델에 내 데이터를 추가 학습시켜 특정 작업에 특화시키는 것 |
| **sLLM** | 작은 언어 모델. 성능은 낮지만 직접 돌릴 수 있고 훨씬 싸다 |
| **LoRA** | 모델 전체가 아닌 작은 부품만 학습시켜 GPU 요구량을 크게 낮추는 파인튜닝 기법 |
| **양자화(quantization)** | 숫자 정밀도를 낮춰 모델 용량을 줄이는 것 |
| **QLoRA** | 4비트 양자화 상태로 LoRA 학습. 무료 GPU로도 4B급이 돌아가는 이유 |
| **SFT** | Supervised Fine-Tuning. "입력-정답" 쌍을 주고 학습시키는 방식 |
| **VRAM** | GPU 전용 메모리. 모델이 여기 올라가야 돌아간다 |
| **파라미터 / 4B** | 모델 내부의 학습된 숫자 개수. 4B = 40억 |
| **에폭(epoch)** | 데이터 전체를 한 번 훑는 것 |
| **하이퍼파라미터** | 학습률·에폭 수처럼 학습 방식을 정하는 설정값 |
| **손실(loss)** | 모델이 얼마나 틀렸는지 나타내는 숫자. 낮을수록 좋다 |
| **체크포인트** | 학습 중간 저장본 |
| **과적합** | 학습 데이터만 외워서 새 데이터에 약해진 상태 |
| **few-shot** | 프롬프트에 예시 몇 개를 넣어 원하는 형태를 유도하는 방식 |
| **베이스라인** | 비교 기준선 |
| **p50 / p95** | 지연 시간의 중앙값 / 느린 쪽 5% 경계. 평균만 보면 튀는 케이스가 가려진다 |
| **GGUF** | Ollama가 쓰는 모델 파일 형식 |
| **Unsloth** | QLoRA 학습을 2배 빠르고 메모리 적게 돌려주는 라이브러리 |
| **Hugging Face** | 모델·데이터셋이 모여 있는 플랫폼. AI판 GitHub. **전부 git 저장소**라 `git push` 로 올린다 |
| **모델 카드** | 모델 저장소의 `README.md`. 모델은 열어봐도 뭘 하는지 알 수 없으므로 **용도·한계·학습 데이터·평가 결과를 적어주는 것**이 관례로 굳었다. 상단 YAML 은 HF 가 검색·필터에 쓰고 본문은 사람이 읽는다 |
| **HF Spaces** | HF 의 웹앱 호스팅. `app.py` 와 `requirements.txt` 를 올리면 알아서 빌드해 URL 을 준다. **무료는 CPU 2코어**이고 한동안 방문이 없으면 잠든다(첫 요청 1~2분) |
| **Gradio** | 파이썬 몇 줄로 입력창·버튼·출력 화면을 만드는 라이브러리. Spaces 에서 가장 많이 쓴다 |
| **LFS** | 큰 파일을 git 저장소 밖에 따로 두고 포인터만 커밋하는 확장. HF 가 GGUF 같은 GB 단위 파일을 이걸로 다룬다 |
| **distinct-2** | 전체 2단어 조합 중 서로 다른 것의 비율. 텍스트 다양성 지표 |
| **Hard Negative** | 정답과 비슷하지만 틀린, 혼동을 유발하도록 의도한 사례 |
| **증류(distillation)** | 큰 모델의 능력을 작은 모델에 옮겨 담는 기법 |
| **MVP** | 핵심만 담은 최소 기능 첫 버전 |
| **온디바이스(on-device)** | 서버가 아니라 사용자 기기(폰) 안에서 직접 실행하는 것 |
| **온프렘(on-premise)** | 외부 클라우드가 아닌 내부 서버에서 돌리는 것 |
| **Tool Call** | AI가 "이 기능을 이 값으로 실행해줘"라고 정해진 형식(JSON)으로 내놓는 출력 |
| **슬롯(slot)** | Tool Call에서 파라미터 값이 들어갈 칸. "슬롯 채우기"는 발화에서 각 칸에 넣을 값을 찾아내는 일 |
| **표층 표현** | 사용자가 실제로 말한 그대로의 문자열. `"아침"`을 `08:00`으로 바꾸지 않은 상태 |
| **정규화(normalization)** | 표현이 제각각인 값을 정해진 형식으로 통일하는 것. "아침"·"아침 8시"·"오전 8시" → `08:00` |
| **MCP** | AI에 외부 도구를 연결하는 표준 규격. 2단계 프로젝트 주제 |
| **가중치(weights)** | 모델 내부에 학습돼 있는 숫자들. 파인튜닝은 이걸 조금 고치고, 프롬프트는 안 건드린다 |
| **딥러닝** | 신경망에 데이터를 주고 가중치를 조정해 학습시키는 것. **파인튜닝은 딥러닝의 일부다** (§5-1) |
| **사전학습(pretraining)** | 모델을 처음부터 만드는 대규모 학습. 수조 단어·수 개월·수십억 원. 파인튜닝은 그 뒤를 잇는 소규모 조정 |
| **오픈 웨이트 모델** | 가중치가 공개돼 내려받아 고칠 수 있는 모델. 파인튜닝의 전제 조건 (API 전용 모델은 불가) |
| **RAG** | 답하기 전에 문서를 검색해 그 내용을 근거로 답하게 하는 방식. **파인튜닝과 역할이 다르다** — 파인튜닝은 "어떻게 답할지", RAG는 "무엇을 답할지" (§5-1) |
| **추론(inference)** | 학습이 끝난 모델을 실제로 돌려 답을 얻는 것. 학습과 대비되는 단계 |

---

## 6. 이력서에 들어갈 목표 문장

프로젝트를 이 문장이 나오도록 설계한다. 반대가 아니다.

> **PawNote — 말로 남기는 반려동물 케어 기록 (온디바이스 sLLM)**
> 보호자 발화를 12종 Tool Call로 변환하는 4B 모델을 QLoRA 파인튜닝.
> GPT-4.1 대비 완전 일치율 nn% 수준을 유지하며 건당 비용 1/nn, p50 지연 nn% 단축.
> 툴 스펙을 가중치에 내재화해 입력 토큰 nn% 절감.
> 국내 양육 실태조사·해외 투약 준수 연구·수의사 문진 항목에서 역산해 툴 셋 도출.
> 오답을 7유형으로 분해해 스키마 위반·툴 환각을 별도 지표로 관리.
