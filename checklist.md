# Checklist — PawNote

Day별 실행 목록. 완료하면 `[x]`로 바꾼다.
설계 근거는 [context-notes.md](context-notes.md), 규격은 [CLAUDE.md](CLAUDE.md).

**GPU 표시** — 🟢 GPU 불필요(로컬) · 🔴 Colab GPU 필요
무료 티어 할당량이 자원이므로 🟢 작업을 GPU 세션에서 하지 않는다.

**축소 트리거** — 작업 부담이 커지면 툴 12개 → 10개로 줄인다. 순서는 `log_activity` → `log_weight`.

---

## Day 0 — 준비 (2시간 이내) 🟢

- [x] 모델 후보 3개 조사 — Unsloth 지원 목록에서 4B급 이하 확인
- [x] 우선순위 1~3(T4 QLoRA 가능 · Unsloth 지원 · GGUF 변환 가능) 통과 여부만 확인하고 **하나로 확정**
  - **`Qwen3-4B-Instruct-2507`** — 학습 `unsloth/Qwen3-4B-Instruct-2507-unsloth-bnb-4bit`, 서빙 `-GGUF`
  - Qwen3.5는 QLoRA 비권장·bf16 중심이라 탈락, Gemma 4는 VRAM 초과·멀티모달이라 제외
- [x] 확정 모델명과 선정 이유를 [context-notes.md](context-notes.md) §3 결정 7에 1줄 추가
- [x] 저장소 초기화, `.gitignore` (데이터·체크포인트·`.env` 제외)
- [x] OpenAI API 키 `.env` 설정 — `.env.example` 복사해서 채우기
- [ ] 구글 드라이브에 프로젝트 폴더 생성 (체크포인트 저장용)

> 모델 비교에 2시간 넘게 쓰지 말 것. 진짜 성능 비교는 Day 3이 겸한다.
> **툴 호출을 이미 잘하는 모델은 오히려 피한다** — base가 잘하면 개선폭이 안 나온다.

---

## Day 1 — 툴 스펙 + 데이터 생성기 🟢

### 툴 스펙 확정

- [x] 툴 12개 정의 — 이름·파라미터·타입·설명 → [src/tools.py](src/tools.py)
- [x] 각 파라미터의 값 도메인 정의
  - [x] `record_type` / `care_type` / `activity_type` / `metric` 의 허용값 목록 (+ `excretion_type`·`condition`·`severity`·`due_care_type`)
  - [x] `amount`는 자유 문자열 허용 ("반 그릇", "한 줌" 같은 모호 단위가 실제 발화)
  - [x] `caregiver`는 자유 문자열, 미언급 시 `null`
  - [x] **1인칭 주어는 `caregiver`에 담지 않는다** — `"내가 약 먹였어"` → `null`. 앱이 로그인 사용자로 채운다 (결정 9의 확장)
  - [x] **시간 계열(`time`·`since`·`until`·`date`·`period`)은 표층 표현 그대로** — `"아침"`, `"저번주 화요일"` (결정 9)
  - [x] **수량 계열(`amount`·`duration`·`weight`)도 표층 그대로** — 숫자형 폐지 (결정 11)
  - [x] **enum은 영문 키로 정규화** (결정 12)
  - [x] 필수 파라미터 지정 — 확정 못 하면 `ambiguous`
- [x] 시간 표현 목록 정리 — 모델이 뱉을 수 있는 표현의 범위 ("아침", "점심때", "어제 밤", "저번주 화요일", "3일 전") → [src/lexicon.py](src/lexicon.py) 6종 53개
  - [x] **조사를 뗀 형태로 통일** — `"아침에"` 아닌 `"아침"`. 사전에 둘 다 있으면 같은 발화에 정답이 둘 생긴다
- [ ] **정규화 함수 작성** (`normalize_time`) — 표층 표현 + 기준 시각 → ISO. 모델이 아니라 앱 코드 쪽. Day 7의 A안 실험에서 라벨 생성에도 재사용한다
- [ ] ~~**정규화 함수 작성** (`normalize_weight`)~~ → **Day 6 README로 이관.** 7일 파이프라인에 소비처가 없다 (채점은 표층 일치, 학습 라벨도 표층, Day 7 A안은 시간 전용, 앱은 안 만듦)
  - 근거였던 *"weight는 단위 산술이라 확정 변환된다"* 가 부정확했다 — `"10키로 조금 넘게"`는 산술로 안 된다. **`weight`도 `amount`·`duration`과 종류가 같고 정도만 다르다**
  - 이 사실이 결정 11을 오히려 강화한다. 예외라고 본 칸이 예외가 아니었으므로 "수량은 열린 집합이라 모델이 정규화하지 않는다"가 세 칸 모두에서 일관되게 성립한다
  - README에서는 15줄짜리 함수로 *"표층으로 두면 앱이 못 쓰는 것 아니냐"* 에 답하고, `"10키로 조금 넘게"`가 변환되지 않는 것까지 보여 **한계를 정직하게 드러내는 자료**로 쓴다

### 데이터 생성 파이프라인

- [x] **Tool Call 랜덤 생성기** 작성 — 파라미터 조합을 균등 샘플링 → [src/sampler.py](src/sampler.py)
  - [x] 층 6종 비중 구현 + 혼동 쌍 8개 정의 (해소된 경계 케이스에서 도출)
  - [x] 거절 층 시드 → [src/lexicon.py](src/lexicon.py) `OUT_OF_SCOPE_SEEDS` 8종 · `AMBIGUOUS_SEEDS` 5종
  - [x] 분포 확인 — 툴 166~274건(요구치 100 충족) · `caregiver` 21.6% · seed 고정 재현성
- [x] 어휘 시드 사전 작성 → [src/lexicon.py](src/lexicon.py) (열린 집합) + [src/tools.py](src/tools.py) `ENUMS` (닫힌 집합 110개)
  - [x] 반려동물 호칭 (이름·애칭·"우리 애"·"강아지")
  - [x] 사료·간식·영양제 표현
  - [x] 모호 단위 ("반 그릇", "한 줌", "조금", "많이")
  - [x] 케어 항목 은어 (심장사상충·구충제·항문낭·발톱)
  - [x] 증상 표현 — 미묘한 통증 신호(하품·입술 핥기) 포함
  - [x] 금지 목록 — GPT가 기대는 상투 표현 차단
- [x] 화자 페르소나 목록 (급한 메모체 / 존댓말 / 반말 일기체 / 오타 섞인 입력 / 질문형 / 수다형) — 6종
- [x] **역방향 생성 프롬프트** 작성 — Tool Call → 자연스러운 한국어 발화 → [src/generate.py](src/generate.py)
  - [x] 툴 이름을 노출하지 않는다 — GPT에겐 툴이 하는 일의 한국어 설명만 준다
  - [x] 배치(6건)로 묶어 "서로 다르게" 요구 + 금지 표현 주입
  - [x] 페르소나 설명을 배치당 한 번 싣고 내부 결정 번호는 제거
- [x] **라벨 재정렬 검증 단계 작성** (결정 14) → [src/verify.py](src/verify.py)
  - [x] 3단계 — ① 라벨이 발화에 있으면 통과 ② 없으면 **같은 슬롯의 값 풀** 안에서 찾아 라벨을 고친다 ③ 특정 불가면 폐기
  - [x] 채점기와 **동일한 정규화** 사용 — 여기서 통과한 것이 채점에서 틀리면 안 된다
  - [x] 한 글자 후보는 재정렬에 쓰지 않는다 — `"다"`가 `"줬다"`·`"먹었다"`에 걸려 엉뚱한 라벨로 바뀐다
  - [x] **역방향 검사 추가** — `null` 인 칸도 발화를 훑어 값이 있으면 채운다. 정방향만 보면 `"응가 상태가 묽었다"` 인데 `condition=null` 인 데이터가 통과한다
  - [x] 재정렬률·폐기율을 기록 — 높으면 생성 프롬프트가 라벨을 무시한다는 신호
    - v1 → v2 → **v3: 폐기 0% · 수정 13.3%**(전부 역방향 채움, 정방향 교체 0건)
- [x] **역방향 생성 프롬프트에 조회계 지시 넣기** (결정 17) — `get_*` 발화는 "앱에 명령하듯"이 아니라 **"가족에게 묻듯"**. 조회 발화는 아무도 해본 적이 없어 상상하면 어색해진다
- [x] 소량(30건) 생성 테스트 → 발화가 실제 보호자 말투인지 확인 후 프롬프트 수정 — **v1·v2·v3 세 번**
  - [x] **판정은 [labeling-guide.md](labeling-guide.md)를 기준으로 한다**
  - [x] **판정 기준 — 읽고 "얘가 뭘 물어보는 거지?"가 들면 버리거나 고친다** (결정 17)
  - [x] **조회계(`get_*`) 발화를 따로 모아 점검** — 기록계보다 어색해지기 쉽다. 여기서 걸러야 재생성 비용을 안 문다
  - [x] **종료 기준을 먼저 정하고 돌았다** — 폐기율 5% 미만 + 명백한 라벨 오류 0~1건이면 본생성으로. 경계선 케이스는 고치지 않고 [labeling-guide.md](labeling-guide.md) §7에 기록. 기준이 없으면 무한히 고칠 수 있다
- [x] **데이터셋 빌드 진입점 작성** → [src/build_dataset.py](src/build_dataset.py). 30건 테스트와 2,400건 본생성이 **같은 경로**를 쓴다
- [x] **`labeling-guide.md` 시작** — 경계 케이스 판정 규칙 → [labeling-guide.md](labeling-guide.md)
  - [x] 판정 순서 6단계 · 거절 규칙 · 기록계 경계(결정 13·15) · 조회계 세 축(결정 16) · 파라미터 채우기(결정 9·11·12) · 필수 파라미터 표 · 자연스러움 기준(결정 17) · 한 장 요약
  - [x] 필수 파라미터 표를 [src/tools.py](src/tools.py)와 대조 확인

### 라벨링 가이드에 들어가야 할 경계 케이스

- [x] "설사했어" → `log_excretion`인가 `log_symptom`인가 → **`log_excretion`. 이상 상태 전용 칸이 있으면 그 툴** (결정 15)
- [x] "구충제 줬어" → `log_medication`인가 `log_care`인가 → **`log_medication`. 약이면 전부** (결정 13)
- [x] "영양제 챙겨줬어" → `log_medication`인가 `log_meal`인가 → **`log_medication`** (결정 13)
- [x] "구충제 안 밀렸나?" → `get_records`인가 `get_care_due`인가 → **`get_care_due`. 과거형이지만 묻는 목적이 "이제 할 때인가"** (결정 16 축③)
  - [x] ~~"심장사상충 약 언제야"~~ → **경계 케이스에서 제외.** 사람이 세 갈래로 읽는다(먹였나·줘야 하나·샀나) → `ambiguous` 예시로 이동 (결정 17)
- [x] "요즘 물 얼마나 마셔?" → `get_records`인가 `get_trend`인가 → **`get_trend`. 나열로 답이 되면 records, 합쳐야 답이면 trend** (결정 16 축①)
- [x] "병원 갈 건데 최근 기록 좀" → `get_vet_summary`인가 `get_records`인가 → **진료 맥락 + 종류 미지정이면 `get_vet_summary`, 종류가 특정되면 `get_records`** (결정 16 축②)
- [x] "산책하다 자꾸 주저앉아" → `log_activity`인가 `log_symptom`인가 → **`log_symptom`. 담을 칸이 없다** (결정 15)

**완료 기준** — 30건 샘플이 실제 보호자 말투로 읽히고 서로 충분히 다르다.

---

## Day 2 — 데이터 완성 + 평가 스크립트 + 베이스라인 1 🟢

### 데이터 생성

- [x] 2,400건 생성 → **실제 산출 학습 1,960 / 검증 200 / 테스트 200**
  - 생성 2,400 → API 실패 9 → 라벨 검증 폐기 28 → 완전 중복 3 → **2,360**
  - **설계 목표는 2,000이었고 실제는 1,960이다.** 차이 2%는 성능에 영향이 없고(학습곡선은 배수 단위로 움직인다), 폐기 단계가 실제로 작동했다는 기록이 남는 편이 낫다. 보충 생성은 하지 않는다 — `sample(n, seed)` 가 n에 따라 통째로 달라져 "40건만 추가"가 깨끗하게 안 된다
- [x] 유형 비중 확인 (합계 100%) — 단순 22% / 파라미터 다중 28% / 어휘 미스매치·혼동 28% / 시제·맥락 판별 10% / out_of_scope 7% / ambiguous 5%
  - **실제 22.0 / 28.1 / 27.8 / 10.0 / 7.1 / 5.0** — 목표와 일치
  - [x] 시제·맥락 판별 층에 **"과거형인데 미래를 묻는" 발화**("한 달 됐나?"·"안 밀렸나?")가 충분히 들어갔는지 확인 (결정 16 축③의 핵심 난이도) — 237건, 30건 테스트에서 발화 형태도 확인
- [x] `caregiver` 언급이 전체의 약 20%에 교차 분포하는지 확인 — **21.3%**
- [x] 12개 툴에 데이터가 고르게 분포하는지 확인 (툴당 최소 100건) — **최소 157 · 최대 253**
  - `get_records` 가 253으로 튄다. 혼동 쌍 8개 중 2개가 `get_records` 정답이고 시제 층의 40%도 이쪽이라 구조적으로 몰린다. 요구치를 크게 넘으므로 두고 본다
- [x] **층 비율을 유지하며 분할** — 앞에서 그냥 자르면 `tense` 20→12, `ambiguous` 10→6이 된다. **12건 위에서는 층별 정확도 분해가 성립하지 않는다** (1건이 8%p를 움직인다)
- [x] 완전 중복 제거 — 3건(`"이거"`·`"양치 완료"`). 라벨 모순은 없었다
- [x] 임베딩 유사도 0.9 이상 중복 **측정** → [src/dedup_check.py](src/dedup_check.py) · `data/dedup.md`
  - `train` 10쌍(1.0%) · `val` 0쌍 · `test` 0쌍 · **`test`↔`train` 3건(1.8%)**
  - **재지 않고 지웠으면 안 지워도 될 것을 지울 뻔했다** — 아래 진행 로그 2026-09-09 참조
- [x] **`distinct-2` 다양성 측정** — 수치 기록 (개선 전후 비교용)
  - **전체 0.710** · 층별 `ambiguous` 0.948 > `out_of_scope` 0.876 > `simple` 0.822 > `confusion` 0.769 > `multi` 0.767 > **`tense` 0.613**
  - `tense` 가 최저인 건 예상된 결과다 — `"한 달 됐나"`·`"안 밀렸나"` 패턴을 반복 요구했다. 층의 성격상 불가피하나 **Day 4에서 이 층의 정확도가 높게 나오면 다양성 부족을 의심한다**

### 검수 (건너뛰기 금지)

- [x] **테스트 200건 사람이 눈으로 검수** — 정답이 틀린 시험지로 재면 모든 숫자가 무효
  - [x] **[labeling-guide.md](labeling-guide.md)를 펼쳐놓고 본다.** §9 한 장 요약이 판정표다
  - [x] 라벨만이 아니라 **발화 자체의 자연스러움도 본다** (결정 17). "뭘 물어보는 거지?"가 들면 버린다
  - [x] **조회계 발화를 따로 모아 점검** — 어색해지기 쉬운 쪽이다
  - 판정 기록 `data/review_decisions.tsv`(사람 72) · `data/review_draft.tsv`(제안 128, 사람 확인 완료)
  - **맞음 156 · 라벨수정 13 · 버림 31 → 시험지 169건**
- [x] 검수 중 발견한 경계 케이스를 [labeling-guide.md](labeling-guide.md)에 추가
  - §4 `since`·`until`은 발화 순서 · `caregiver`는 돌본 사람만 · §6 라벨이 갈리는지로 버린다 · 혼잣말은 거절 사유가 아니다
- [x] `out_of_scope`에 **진단 요청**("우리 개 무슨 병이야?", "이거 위험해?")이 포함됐는지 확인
- [x] **반영은 스크립트로 한다** → [src/apply_review.py](src/apply_review.py). 원본 `test.jsonl` 은 그대로 두고 `data/test_reviewed.jsonl` 을 만들며, 무엇을 왜 고쳤는지가 `data/review_applied.md` 에 남는다

### 평가 스크립트

- [x] 7분류 오답 분해 채점기 작성 → [src/evaluate.py](src/evaluate.py)
  - [x] 완전 일치
  - [x] 툴 정답 · 파라미터 오류
  - [x] 툴 오류 (존재하는 툴)
  - [x] **툴 환각** (12개 목록에 없는 이름)
  - [x] **스키마 위반 · 파싱 실패**
  - [x] 과잉 거절 (툴 있는데 null)
  - [x] 놓친 거절 (툴 없는데 호출)
  - [x] 시간 계열 파라미터는 **표층 문자열 일치**로 채점 (결정 9 — ISO 변환·허용 오차 로직 불필요)
  - [x] **규칙에 없어 판단이 필요했던 자리 셋을 코드 주석에 근거와 함께 남긴다**
    - 조사만 다른 값 → **오답이되 따로 집계.** 규칙이 "조사만 다른 *오답*"이라고 적었다
    - 거절은 맞고 `reason`만 틀림 → **파라미터 오류** (안 A). 모델이 거절 자체는 했으므로 "놓친 거절"이 아니다. 7분류를 늘리지 않으면서 실제 상태를 담는다
    - 빈 문자열 ≠ `null`. "안 채운 것"과 "빈 값을 채운 것"을 섞지 않는다
  - [x] **자체 검사 12건** — 위 셋을 포함해 되돌아가지 않도록 못을 박았다. `python src/evaluate.py --self-test`
- [x] 난이도 층별·툴별 분해 집계 기능 → [src/evaluate.py](src/evaluate.py)
- [x] **예측 생성 스크립트** → [src/predict.py](src/predict.py). 세 대상이 같은 경로를 쓴다
  - [x] 프롬프트만 대상별로 다르다 — `gpt41`·`base`는 툴 스펙 포함, `ft`는 발화 한 줄 (결정 2). **그 차이가 실험 자체다**
  - [x] 툴 스펙 텍스트를 [src/tools.py](src/tools.py)에서 생성 — 손으로 옮겨 적으면 코드와 어긋난다
  - [x] `--dump` — API 없이 프롬프트만 JSONL로. Colab 등 다른 런타임에서도 **프롬프트가 같아야** 한다
  - [x] few-shot은 **학습셋에서** 층을 고루 뽑는다. 테스트셋에서 뽑으면 시험 문제를 예시로 주는 셈
  - [x] `temperature=0` — 평가에서 다양성은 해악이다
  - [x] 실패해도 빈 출력을 남긴다 — 건너뛰면 id가 어긋나 채점기가 멈춘다
  - [x] **채점기까지 연결 확인** — 5건 실측으로 `predict → evaluate` 전 구간 통과
  - ⚠ **`ft` 프롬프트 = 학습 데이터 포맷.** Day 3에서 `build_messages("ft", ...)`를 그대로 써야 한다. 어긋나면 재는 것이 파인튜닝 효과가 아니라 포맷 불일치가 된다
- [ ] 지연 측정(p50/p95), 토큰 수·비용 계산 유틸 — **predict.py가 건별로 이미 기록한다**(`latency_ms`·`input_tokens`·`output_tokens`). 집계·비용 환산만 남음
- [ ] 비용 계산은 **(a) 계산 자원 환산**과 **(b) 사업자 부담** 두 가지를 따로 산출 — P2 판정은 (a)로 한다

### 베이스라인 1

- [x] **GPT-4.1 + 툴 목록 프롬프트**로 테스트 200건 측정 — 실패 0건
- [x] 결과 표 기록 — 이 시점에 "GPT는 몇 점"이 손에 있어야 한다

**확정본은 `log_water` 라벨 수정 후 재실행분이다.**

| 지표 | GPT-4.1 |
| --- | --- |
| **완전 일치** | **73.0%** (146/200) |
| 파라미터 오류 | 21.0% (42) |
| 툴 오류 | 2.5% (5) |
| **툴 환각** | **0%** |
| 스키마 위반 | 0.5% (1) · **파싱 실패 0%** |
| 과잉 거절 / 놓친 거절 | 1.5% (3) / 1.5% (3) |
| 코드펜스 | 0건 |
| 지연 | p50 728ms · p95 897ms |
| **입력 토큰** | **건당 1,542** · 출력 34 |

- 층별 — `out_of_scope` 92.9% > `tense` 85.0% > `simple` 74.4% > `confusion` 70.9% > `ambiguous` 66.7% > **`multi` 66.1%**
- 툴별 — `log_weight` 100% · `get_vet_summary` 89.5% ↔ **`log_symptom` 25.0% · `log_water` 47.4%**
- **입력 토큰 1,542가 결정 2의 비교 기준선이다.** `ft`는 발화 한 줄이라 20~30토큰이 될 것이고, 예상했던 "80~90% 감소"보다 훨씬 큰 폭이 나온다

### ⚠ 같은 조건 재실행에서 수치가 움직인다 — Day 4 해석의 전제

`temperature=0` 인데도 두 번째 실행이 달랐다. 시험지에서 바뀐 건 `log_water` 4건뿐인데 **다른 툴까지 움직였다.**

| | 1차 | 2차(확정) | 비고 |
| --- | --- | --- | --- |
| 완전 일치 | 73.5% | **73.0%** | 노이즈 ±0.5%p |
| `log_water` | 31.6% | **47.4%** | 라벨 수정 효과 (실제 개선) |
| `get_vet_summary` | 100% | 89.5% | **입력이 같은데 -10.5%p** |
| `get_records` | 71.4% | 61.9% | 입력 동일 |

**전체 수치의 노이즈는 ±0.5%p 수준이라 P1 기준(+15%p)은 안전하다.** 그러나 **툴별 분해는 분모가 7~21건이라 1~2건이 10%p를 움직인다.** Day 4에서 툴별·혼동 쌍별 숫자를 읽을 때 이 폭 안의 차이는 실력 차로 읽지 않는다.

**완료 기준** — GPT-4.1 점수와 오답 분해표가 나왔다. **여기까지만으로도 폴백 프로젝트가 성립한다.**

---

## Day 3 — Colab 진입, 베이스라인 2 + 첫 학습 🔴

**GPU 없이 되는 준비는 끝났다** (작업 규칙 7). Colab 노트북(`.ipynb` — 코드 칸과 설명 칸이 섞인 실행 문서)은 [notebooks/pawnote_colab.ipynb](notebooks/pawnote_colab.ipynb) 하나이고, 셀 번호가 아래 항목과 같다.

**브라우저에서 하는 일이라 기기를 안 가린다.** GPU는 구글 서버 것을 빌리므로 이 PC에 그래픽카드가 있든 없든 무관하다.

1. 드라이브에 `PawNote` 폴더 생성 → `data/` 에서 4개 파일 드래그
2. [colab.research.google.com](https://colab.research.google.com) → **업로드** 탭 → `notebooks/pawnote_colab.ipynb`
3. **런타임 → 런타임 유형 변경 → T4 GPU → 저장** ← 기본값이 CPU라 안 바꾸면 ③에서 터진다
4. 셀을 위에서부터 하나씩 (`Shift+Enter`)

- [x] 학습 데이터를 **툴 스펙 미포함 형식**으로 변환 (설계 결정 2) → [src/build_sft.py](src/build_sft.py)
- [x] **프롬프트를 로컬에서 `--dump`.** Colab에서 다시 조립하면 환경에 따라 달라질 수 있고, 그러면 GPT-4.1과 같은 시험을 본 게 아니게 된다
- [ ] 드라이브 `MyDrive/PawNote/` 에 4개 파일 업로드
- [ ] **①② 설치 · 마운트** — 노트북 ②가 파일 4개와 건수를 찍는다. 여기서 어긋나면 뒤가 다 헛돈다
- [ ] **③ ⚠ 최우선 — dtype 확인.** bnb-4bit의 compute dtype이 `bfloat16`인데 T4는 bf16 미지원. **로드는 되고 학습에서 터지는** 가장 비싼 실패 방식이라 `assert` 로 막아뒀다
- [x] **④ 베이스라인 2 — base + 툴 스펙 + few-shot 8쌍으로 테스트 200건** → **40.5%**
  - [x] **LoRA를 붙이기 전에 돌린다.** 순서가 바뀌면 기준선이 오염된다
- [x] **⑤ QLoRA 학습 1회** (`sft_train.jsonl` 1,960건 · r16 · lr 2e-4 · 2 epoch · 실효 배치 16)
  - [x] `train_on_responses_only` — **라벨에만 손실을 건다**
- [x] **⑥ 체크포인트를 구글 드라이브에 저장**
- [x] **⑦ ft 예측** → **90.0%** · 입력 토큰 건당 **27**

**완료 기준 달성** — base few-shot 점수가 나왔고 학습이 끝까지 돌았다.

> **P0는 아직 안 끝났다.** P0의 정의는 *"데이터 생성 → 학습 → 평가 → **Ollama 서빙**까지 완주"* 이고 **서빙이 남았다.** 3자 비교표와 오답 분해표는 나왔으므로 **4단계 중 3단계**다. Day 5가 P0의 마지막 칸이다.

### 3자 비교 (테스트 200건 · 검수 전)

| | GPT-4.1 | base + few-shot | **파인튜닝** |
| --- | --- | --- | --- |
| **완전 일치** | 73.0% | 40.5% | **90.0%** |
| 파라미터 오류 | 21.0% | 40.5% | **6.5%** |
| 툴 오류 | 2.5% | 11.0% | **2.0%** |
| **툴 환각** | 0% | 0% | **0%** |
| **스키마 위반** | 0.5% | 1.0% | **0%** |
| **파싱 실패** | 0% | 0% | **0%** |
| 과잉 거절 | 1.5% | 2.5% | **0%** |
| 놓친 거절 | 1.5% | 4.5% | 1.5% |
| 입력 토큰/건 | 1,542 | 2,094 | **27** |
| 출력 토큰/건 | 34 | 37 | 40 |
| 지연 p50 | 728ms | 3,811ms | 3,474ms |

**판정 — P1 세 조건 전부 달성.** 완전 일치 **+49.5%p**(기준 +15%p) · 스키마 위반 **0%**(기준 5% 미만) · 툴 환각 **0%**(기준 2% 미만).
**P2 정확도 조건 달성** — GPT-4.1의 **123%**(기준 90% 이상). 비용·지연 조건은 Day 5 Ollama 측정 대기.

### 결정 2의 성과 — 입력 토큰 78배 감소

**2,094 → 27.** 같은 모델·같은 토크나이저로 잰 값이라 이것이 정확한 수치다.
GPT-4.1의 1,542와는 **토크나이저가 달라 직접 비교할 수 없다** — 같은 프롬프트를 GPT-4.1은 1,542, Qwen3는 2,094로 센다. 비용 계산에서는 각자 요금 체계로 쓴다.

### ⚠ 이 숫자를 읽는 법 — 세 가지를 같이 써야 정직하다

1. **검수 전 숫자다.** 파인튜닝 오답 20건 중 여럿이 라벨 결함으로 보인다 — `"오후 3시쯤"`을 모델이 그대로 담았는데 라벨이 `"오후 3시"`(§7 ②), `"잠깐 전에 먹음"`에 라벨 `time=null`. **검수하면 90%가 더 오를 가능성이 높다**
   > **2026-09-09 — 이 예측은 틀렸다. 87.0%로 내렸다.** 결함이 양쪽에 고르게 있는 줄 알았는데 **모델에게 유리한 쪽으로 치우쳐 있었다.** 문장을 지우지 않고 남긴다 — 예측과 결과가 같이 보여야 근거가 된다. 분석은 진행 로그 2026-09-09
2. **테스트셋이 학습셋과 같은 파이프라인·같은 라벨 협약에서 나왔다.** 파인튜닝 모델은 그 협약을 학습했고 GPT-4.1은 프롬프트로 한 번 읽었다. 90% > 73%를 *"우리 모델이 GPT-4.1보다 똑똑하다"* 로 읽으면 안 된다. **맞는 해석은 "정해진 출력 협약을 지키게 만드는 데는 파인튜닝이 압도적으로 낫다"** 이고, 그것이 애초에 이 프로젝트가 재려던 것이다
3. **재실행 노이즈 ±0.5%p** — +49.5%p는 그 폭 밖이라 안전하다. 그러나 툴별 분해는 분모가 7~21건이라 여전히 1~2건이 10%p를 움직인다

### 예상 하이라이트가 맞았다 — 형식 안정성

CLAUDE.md에 *"파인튜닝은 정확도보다 형식 안정성을 극적으로 올린다"* 고 적어뒀는데 그대로 나왔다.

**툴 환각 0% · 스키마 위반 0% · 파싱 실패 0% · 과잉 거절 0%.** 200건 중 형식이 깨진 것이 하나도 없다.

### `<think>` 빈 블록 — 채점에 무해하나 별도 지표로 기록한다

파인튜닝 모델이 **200건 전부** 출력 앞에 `<think>\n\n</think>` 를 붙인다. 채점기는 첫 `{` 부터 읽으므로 파싱에 영향이 없고(실측 — 블록 안 중괄호 0건), 코드펜스와 같은 부류다. **코드펜스율처럼 별도 지표로 보고한다.**

- 출력 토큰이 34~37 → 40으로 늘어난 원인이 이것이다
- 학습 라벨에는 없는데 붙는다. **Qwen3 채팅 템플릿이 넣는 것**이고 학습·추론에 같은 템플릿을 썼으므로 일관돼 있다
- 블록 안에 중괄호가 들어가면 채점 규칙상 파싱 실패가 된다. 지금은 0건이지만 **Day 5 Ollama 서빙에서 다시 확인한다**

---

## Day 4 — 학습 반복 + 판정 🔴

- [ ] 학습률 2~3개로 재학습 (광범위 탐색 금지, GPU 시간 소모가 큼)
- [ ] 검증셋 기준 최적 체크포인트 선택
- [ ] 테스트 200건 평가 → 7분류 오답 분해표
- [ ] **학습셋으로도 평가** (경우 B 판별용)
- [ ] 난이도 층별·툴별 정확도 분해
- [ ] 혼동 쌍별 오답률 확인 (어떤 쌍이 제일 어려웠는가)

### ⚠ 폴백 판정 지점

- [x] 학습이 정상 동작하는가 판정 → **정상. 폴백 미발동. Day 5로 간다**

### 파인튜닝 오답 20건의 성격 (Day 6 실패 사례 분석 재료)

| 성격 | 건수 | 예 |
| --- | --- | --- |
| **라벨 결함으로 보이는 것** | 5 | `"오후 3시쯤"`→라벨 `"오후 3시"` · `"잠깐 전에 먹음"`→라벨 `time=null` · `"덴탈껌 하나 다"`→라벨 `amount=null` |
| `since`/`until` 뒤바뀜 | 2 | `"오늘 아침이랑 며칠 전에"` — 어느 쪽이 시작인지 어순으로 안 정해진다 |
| 조회/기록 혼동 | 2 | `"2주간 누나 배변"` → `log_excretion`(정답 `get_records`). 기간 표현이 조회 신호인데 못 읽었다 |
| 거절 경계 | 3 | 아래 별도 |
| 기타 파라미터 | 7 | `"산책 끝나고 앉아 연습"` → `walk`(정답 `training`) — §7 ④에서 자동 보정을 철회했던 바로 그 문항 |
| **언어 누출 1건** | 1 | `"낑낑거려"` → `"ciągłe niały"` (폴란드어). 4B 모델의 드문 실패 방식 |

**🔴 놓친 거절 3건 중 하나가 안전 경계다.**

```
발화   "초코 이 약 오늘 좀 줘도 되나?"        ← 투약 판단 요청
정답   out_of_scope
출력   log_medication(name="이 약", dose="오늘 좀", ...)
```

**투약해도 되냐는 질문을 투약 기록으로 처리했다.** 실제 앱이면 하지도 않은 투약이 기록되고, 다음에 `get_care_due`가 "이미 줬다"고 답한다. 정확도 표의 1.5%가 아니라 **안전 경계 위반**으로 따로 보고한다 — 이 프로젝트가 README에 경계를 명시한 이상 숫자 뒤에 숨기면 안 된다. 학습 데이터에서 `out_of_scope` 중 **투약 판단 요청 비중을 늘리는 것**이 Day 4 재학습의 후보다.

**완료 기준** — P1 기준(완전 일치율 +15%p, 스키마 위반 5% 미만, 툴 환각 2% 미만) 달성 여부가 판정됐다.

---

## Day 5 — 서빙 + 속도·비용 측정 🔴🟢

- [x] 어댑터 병합 후 **GGUF 변환** — `Q4_K_M` **2.326GB**
- [x] Ollama로 로컬 서빙 — `raw` 모드로 프롬프트 직접 조립
- [x] 서빙 상태에서 테스트 재평가 — **79.3%** (양자화 손실 7.7%p)
- [x] **지연 p50/p95 측정** — p50 5,461ms · p95 6,627ms (GTX 970 · VRAM 4GB · 36% CPU 오프로드)
- [ ] **CPU-only 추론 지연 측정** — 온디바이스 타당성의 간접 근거. GGUF 파일 크기도 함께 기록. 실기기 벤치마크는 범위 밖임을 README에 명시
  - ⚠ **데스크탑에서만 한다.** 지연은 하드웨어에 종속돼 다른 기기 숫자를 섞으면 비교가 무의미해진다
- [x] **입력 토큰 수 비교** — GPT-4.1 1,542 vs 파인튜닝 **27**. 결정 2의 효과
- [x] 1,000건당 비용 계산 — (a) 계산 자원 환산 · (b) 사업자 부담 두 가지 모두 `data/report.md`

**완료 기준** — 3자 비교표의 모든 칸이 채워졌다. **CPU-only 지연 한 칸만 비었다.**

---

## Day 6 — 결과 정리 🟢

### 결과물

- [x] **3자 비교표** (GPT-4.1 / base few-shot / 파인튜닝) — Ollama 포함 4열
- [x] **7분류 오답 분해표** — 4열 비교
- [x] 그래프 3장 → [src/charts.py](src/charts.py) · `assets/*.svg`
  - **의존성 없이 SVG 문자열을 직접 쓴다.** 막대 세 장에 그림 라이브러리를 붙일 이유가 없고, 텍스트라 `git diff` 로 읽힌다
  - `data/eval_*.jsonl` 을 읽어 그리므로 **재채점하면 그림도 같이 바뀐다** — `report.py` 와 같은 이유다
- [x] **실패 사례 분석** — 파인튜닝 오답 **22건 전수**를 6분류로
- [x] Success Criteria P0/P1/P2 달성 여부 명시 — **P2 두 칸 ❌ 포함**

### README

> **밑글은 [explainer.md](explainer.md)에 이미 있다.** 논리 체인·2,000쌍 설명·소개 대본·예상 질문을 그대로 옮겨 쓰고, 여기에 측정 결과를 얹는다.

- [x] **왜 만들었는가** — 실태조사·투약 연구 숫자 인용, 기존 앱과의 차별점
- [x] **사용자 조사 → 툴 도출 과정** ([context-notes.md](context-notes.md) §2 요약). `log_water`·`get_vet_summary`·`caregiver`가 조사에서 나왔다는 점 명시
- [x] **범위 결정과 근거** — 순차 다중 호출을 왜 뺐는가
- [x] **설계 결정** — 툴 스펙 미포함 학습과 그 트레이드오프 (툴 변경 시 재학습 필요)
- [x] **모델과 규칙의 역할 분리** — 왜 알림은 LLM이 아닌가
- [x] **안전 경계** — 기록·조회 전용, 진단·처방은 범위 밖 + **경계를 지키지 못한 1건**
- [x] 데이터 생성 방법론 + 다양성 측정 수치
- [x] 라벨링 가이드
- [x] 평가 방법과 결과
- [x] **한계와 다음 단계**
- [x] **MCP 연동 2단계 계획** — 「다음 단계」 절 안에 넣었다
  - 1단계(이 모델) → 2단계(MCP 서버의 라우팅 두뇌)가 **직렬 의존**이라 동시에 안 한다는 것까지
  - **2단계에 툴 호출 로그를 미리 넣어둘 것** — 그 로그가 다음 학습 데이터가 된다. 이 프로젝트가 합성에 의존해야 했던 이유가 실제 로그 부재였고, 검수에서 그 대가를 숫자로 확인했다

**완료 기준** — 프로젝트를 모르는 사람이 README만 읽고 가치를 이해한다.

**574줄 완료 (2026-09-10). 남은 것은 CPU-only 지연 수치 한 칸뿐이다.**

---

## Day 7 — 버퍼 🟢🔴

무료 티어가 잠기거나 앞 단계가 밀린 경우를 위한 예비일. 여유가 있으면 우선순위 순으로.

- [ ] **학습곡선 실험** (250 / 1,000 / 2,000건) — 가장 값어치 있음. 경우 B 원인 판별을 겸함
- [ ] **A안 대조 실험 — 시간 정규화를 모델에 맡겨본다** (결정 9 검증)
  - [ ] `normalize_time`으로 B 라벨 → A 라벨 코드 변환 (GPT 재호출·재검수 불필요)
  - [ ] 입력에 기준 시각 프리픽스 부착 후 재학습
  - [ ] 완전 일치율 차이 측정 → 결정 9를 주장에서 측정으로 승격
  - [ ] 한계 명시 — 재는 것은 "우리가 정한 정규화 관례의 학습 가능성"이지 절대적 시간 이해력이 아님
- [ ] 병렬 독립 다중 호출 지원 ("물이랑 밥 줬어" → 배열 출력)
  - [ ] **대표 사례로 `"설사했어"` 계열을 쓴다** (결정 15) — 한 사건이 두 툴에 걸치는 경우. 단일 호출 제약이 무엇을 잃게 하는지 정량화되고, MVP의 타협이 주장에서 측정으로 바뀐다
- [ ] 능동 알림 규칙 엔진 (B층) — 날짜 계산 기반, LLM 아님
- [ ] 1.5B vs 4B 비교 — "얼마나 작아도 되는가"
- [ ] `ambiguous`일 때 무엇이 부족한지 되묻는 질문 생성
- [ ] 저장소 공개 정리 (데이터 샘플, 재현 스크립트)

---

## 진행 로그

날짜별로 실제로 한 일과 막힌 지점을 남긴다. **새 세션에서 이어서 작업할 때 이 표를 먼저 읽는다.**

| 날짜 | 한 일 | 막힌 지점 · 결정 |
| --- | --- | --- |
| 2026-08-31 | 주제 확정(자연어 → Tool Call 파인튜닝), 도메인을 사내 툴 → 반려동물로 전환 | — |
| 2026-08-31 | 사용자 조사 수행 — 국내 실태조사·투약 준수 연구·수의사 문진·기존 앱 4갈래 | 조사로 `log_water`·`get_vet_summary`·`caregiver` 발견. 툴 10 → 12개 |
| 2026-08-31 | 설계 문서 3종 작성 완료 | 모델 미확정 — Day 0에 선정 |
| 2026-09-01 | 문서 검토에서 설계 충돌 발견 — 결정 2(입력이 발화뿐)와 절대 시각 출력이 양립 불가 | **결정 9 신설. B안(표층 표현 유지 + 앱 코드 정규화) 확정.** A안은 Day 7 대조 실험으로 이관 |
| 2026-09-01 | 데이터 유형 비중 합계가 97%였던 것 수정 | 어휘 미스매치·혼동 25% → 28%로 보정 |
| 2026-09-01 | 온디바이스 주장에 측정 계획이 없던 문제 보완 | Day 5에 CPU-only 지연 측정 추가. 실기기 벤치마크는 범위 밖으로 명시 |
| 2026-09-01 | P2의 비용 기준이 정의 없이 떠 있던 문제 확정 | (a) 계산 자원 환산으로 판정, (b) 사업자 부담은 서사용으로 분리 |
| 2026-09-01 | 프롬프트·파인튜닝·RAG 개념 정리 추가 | context-notes §5-1 신설. 용어집은 §5-2로 이동 |
| 2026-09-01 | "품종 지식이 필요하지 않나" 질문에서 RAG 검토 | **결정 10 신설 — RAG 미사용.** 품종 표준값은 참조 테이블(C층), MVP 범위 밖 |
| 2026-09-01 | 프로젝트 정의·범위 명문화 | **context-notes §0 신설.** 사용자는 보호자 한 명(수의사는 사용자 아님), 7일에 만드는 건 모델 하나뿐, 목표는 제품/기술 두 겹 |
| 2026-09-01 | Day 0 착수 — `.gitignore`·`.env.example` 생성 | `.env` 키 입력과 드라이브 폴더는 수동 |
| 2026-09-01 | **툴 12종 스펙 확정 → [src/tools.py](src/tools.py)** 단일 원본 | 숫자형 파라미터가 결정 9와 충돌하는 것을 발견. **결정 11 신설(수량도 표층)**, 파라미터명 `amount_ml`·`duration_min`·`weight_kg` → `amount`·`duration`·`weight` |
| 2026-09-01 | enum 값을 영문으로 둘지 한글로 둘지 판단 | **결정 12 신설** — 닫힌 집합은 영문 정규화, 열린 집합만 표층. 결정 9의 과잉 적용을 막는 경계선 |
| 2026-09-01 | `log_medication` / `log_care` 경계가 문서상 겹쳐 있던 문제 | **결정 13 신설** — 약이면 전부 medication, care는 위생 관리. 경계 케이스 2건 해소. 예방약은 medication으로 기록되고 `get_care_due`로 조회되는 비대칭을 명문화 |
| 2026-09-01 | 결정 3과 결정 9·11의 정합성 구멍 발견 — 표층 라벨과 생성 발화가 어긋나면 정답 동작도 오답 처리됨 | **결정 14 신설** — 생성 후 라벨 재정렬 검증. 발화가 아니라 라벨을 고친다 |
| 2026-09-01 | 채점 규칙이 정의 없이 떠 있던 문제 | CLAUDE.md에 **직렬화 규칙 · 7분류 판정 순서 · 값 비교 정규화** 확정. 코드펜스는 허용하되 사용률을 별도 지표로 기록 |
| 2026-09-02 | **모델 확정 — `Qwen3-4B-Instruct-2507`** | 최신 세대가 탈락. Qwen3.5는 Unsloth가 QLoRA를 비권장하고 bf16·Mamba 커널이라 T4 부적합, Gemma 4 E4B는 LoRA 17GB로 T4 초과 + 멀티모달. Gemma 3 4B와 최종 비교에서 **텍스트 전용·thinking 분리 체크포인트**로 Qwen3 채택. Day 3에 bf16→fp16 dtype 확인을 최우선 항목으로 추가 |
| 2026-09-02 | 코드·문서 교차 점검 | 시드 사전에 조사가 섞여 있던 것(`"아침"`·`"아침에"` 공존) 발견 → **조사 제거로 통일.** 같은 발화에 정답이 둘 생기는 문제였다 |
| 2026-09-02 | 오타 페르소나가 결정 14의 폐기 규칙에 걸리는 문제 | 오타를 **조사·어미·띄어쓰기로 한정.** 파라미터 값 단어는 정확히 쓰도록 페르소나 정의에 명시 |
| 2026-09-02 | `normalize_amount`가 성격이 다른 변환을 섞고 있던 문제 | **`normalize_weight`만 만든다.** `"반 그릇"`·`"짧게"`는 기준이 없어 앱 코드도 계산 불가 |
| 2026-09-02 | context-notes 조사→툴 매핑에 `구충`이 `log_care`로 남아 있던 것 | 결정 13에 맞춰 `log_medication`으로 정정 |
| 2026-09-02 | 조사 제거 중 1인칭 `caregiver` 문제 발견 — `"내가"`는 조사를 떼면 발화에 남는 글자가 없음 | **1인칭은 `caregiver=null`, 앱이 로그인 사용자로 채운다.** 결정 9의 확장으로 문서화. 시드에서 `"내가"`·`"제가"` 제거하고 3인칭 4개 보강 |
| 2026-09-02 | `"설사"`가 `excretion_condition`과 `SYMPTOM` 양쪽에 있어 같은 발화에 정답이 둘 생기는 문제 | **결정 15 신설** — 이상 상태 전용 칸이 있는 툴이 담당. `SYMPTOM`에서 `"설사"` 제거. 경계 케이스 2건 해소. 병렬 호출이 가장 정확하나 결정 1의 범위 밖이라 Day 7 확장에 대표 사례로 이관 |
| 2026-09-02 | 조회계 4종의 값 도메인이 크게 겹치는 문제 (`record_type` 8개 중 6개가 `trend_metric`과 동일) | **결정 16 신설 — 세 축으로 정리.** ① 나열/요약 ② 진료 맥락+종류 특정 ③ 묻는 목적. 경계 케이스 3건 해소 |
| 2026-09-02 | 축③ 초안(시제 어미로 가르기)이 실제 발화에서 무너짐 — `"한 달 됐나?"`·`"안 밀렸나?"`는 과거형인데 미래를 묻는다 | 기준을 **시제 어미 → 묻는 목적**으로 교체. 이 유형을 시제·맥락 층의 핵심 난이도로 명시 |
| 2026-09-02 | 경계 케이스 예시 `"심장사상충 약 언제야"`가 사람이 읽어도 세 갈래로 갈림 (먹였나·줘야 하나·**샀나**) | **결정 17 신설 — 발화 자연스러움 사람 검증.** 결정 14는 라벨-발화 정합만 보고 발화 자체는 못 잡는다. 조회계가 특히 위험(혼잣말이 아니라 상상해서 쓰는 말). 판정 기준은 "얘가 뭘 물어보는 거지?". 생성 프롬프트·30건 테스트·200건 검수·경우 B 판별 네 곳에 반영 |
| 2026-09-03 | **Tool Call 랜덤 생성기 작성 → [src/sampler.py](src/sampler.py).** 거절 층 시드 13종을 `lexicon.py`에 추가 | 혼동 쌍 8개를 해소된 경계 케이스에서 도출. `hint`는 발화의 겉모습만 바꾸고 라벨에 영향을 주지 않도록 분리 |
| 2026-09-03 | **분포를 눈으로 보고 4건 수정** — GPT를 부르기 전에 잡아야 재생성 비용을 안 문다 | ① **층 처리 순서** — 툴이 고정된 층(혼동·시제)을 먼저 돌려야 `simple`·`multi`가 부족한 툴을 메운다. 뒤집으면 편차 3.6배 → 바로잡아 1.6배 ② **조회계 빈 라벨 제한** — `get_care_due` 167건 중 78건이 전부 null(같은 발화 78번)이었다 ③ **식별 칸 우선** — `log_meal`에 `amount`만 차면 `"반만 줬어"`가 되어 밥·물·약을 못 가린다 ④ **`until` 단독 금지** — `"토요일 아침까지"`만 있는 발화는 하지 않는 말(결정 17) |
| 2026-09-03 | 페르소나가 툴 계열과 어긋나던 문제 | 기록계에 "질문형"이 붙으면 `"배를 자꾸 핥아?"`가 되어 기록이 질문으로 바뀐다. **페르소나를 툴 확정 후에 고르고 계열별로 제한** |
| 2026-09-03 | `normalize_weight`의 필요성 재검토 | **소비처가 없어 Day 6으로 이관.** 근거였던 "weight는 확정 변환된다"가 `"10키로 조금 넘게"`에서 깨진다 — weight도 `amount`·`duration`과 종류가 같다. **결정 11은 오히려 강화됨.** `normalize_time`은 Day 7 A안이 쓰므로 그대로 둔다 |
| 2026-09-03 | **역방향 생성 프롬프트 작성 → [src/generate.py](src/generate.py)** | 프롬프트를 눈으로 보고 2건 수정 — ① 페르소나가 **이름만** 들어가 오타 규칙(값 단어는 정확히)이 전달되지 않았다. 결정 14를 직접 위협 ② 툴 이름(`log_symptom`)이 노출됐다. GPT는 툴을 모르므로 **하는 일의 한국어 설명**으로 바꿨다. 혼동 쌍의 `why`는 프롬프트에서 빼고 Day 4 분석용으로 남김 |
| 2026-09-03 | 기록계 `time` 슬롯에 주 단위가 들어가던 문제 — `"지난주에 물 두 번 가득 마셨어"` | `relative_day`에서 **`relative_week` 분리.** `log_weight.date`·`get_records.since`는 주 단위를 받고 나머지 기록계 `time`은 안 받는다 |
| 2026-09-03 | **라벨 재정렬 검증 작성 → [src/verify.py](src/verify.py)** | 재정렬 범위를 **같은 슬롯의 값 풀**로 좁혔다 — 발화 아무 데서나 표현을 주우면 `time` 칸에 음식 이름이 들어간다. 자기 검사에서 버그 발견 — `MEAL_AMOUNT`의 `"다"`가 `"줬다"`·`"먹었다"`에 매칭돼 아무 발화나 재정렬시켰다. `"싹 다"`로 교체 + **한 글자 후보는 재정렬에서 제외** |
| 2026-09-03 | 프로젝트 설명 방식을 복습하며 개념 이해 점검 — 파인튜닝의 위치, 2,000쌍의 정체, 학습/검증/테스트 구분, 체크포인트와 과적합 | **[explainer.md](explainer.md) 신설.** 규격·할 일·근거와 성격이 달라 네 번째 문서로 분리. Day 6 README의 밑글로 쓴다 |
| 2026-09-03 | *"말로 기록한다면서 STT를 안 만들면 모순 아닌가"* 라는 질문 — 문서가 "안 만든다"고만 하고 이유가 없었다 | **STT를 범위 밖 목록에 근거와 함께 명시.** 진짜 병목은 인식이 아니라 변환이라는 점, 오인식은 앱의 확인 단계로 흡수한다는 점, **직접 입력 경로를 오타 페르소나가 이미 커버**한다는 점을 explainer §2·§8에 추가 |
| 2026-09-03 | **[labeling-guide.md](labeling-guide.md) 작성 완료** — 결정 13·15·16·17이 다 확정돼 지금 쓸 수 있었다 | 판정 순서 6단계 + 한 장 요약. 30건 테스트와 Day 2 검수 200건이 이 문서를 기준으로 판단한다. 필수 파라미터 표는 `tools.py`와 대조 확인 |
| 2026-09-04 | **30건 생성 v1 — 6건 결함 발견.** GPT가 지시를 안 받은 항목에서 전부 터졌다 | ① 발화에 값이 있는데 라벨이 비어 있음(verify가 정방향만 봄) → **역방향 검사 신설** ② 혼동 쌍 지시가 **내용까지** 옮겨 정답이 뒤집힘 → "바꿔도 되는 건 어휘뿐" 명시 ③ 기록 발화가 질문으로 끝남 → 평서문 강제 ④ `"확인불요"` 같은 군더더기 ⑤ 앱 명령조 ⑥ `get_vet_summary` 를 "다녀온 진료"로 오해 → `desc` 수정 |
| 2026-09-04 | **30건 v2 — 5건 해소, 2건 잔존.** 잔존 2건은 성격이 달랐다 | `get_vet_summary` 에서 **진료 맥락이 통째로 사라짐**(혼동 지시와 툴 정체성이 **충돌**). 축③ 발화가 지난 날짜만 물음(**금지가 없어** GPT가 경계를 넘음). 지시 누락이 아니라 **충돌·불충분**이라 성격이 다르다 |
| 2026-09-04 | *"계속 고치면 시간을 다 쓰는 것 아닌가"* — 수정 루프의 종료 기준을 정함 | **v3를 마지막 루프로 확정.** 폐기율 5% 미만 + 명백한 라벨 오류 0~1건이면 본생성. **경계선 케이스는 고치지 않고 기록만 한다** — 프롬프트로 없앨 수 있는 문제가 아니고, 학습 2,000건은 평균이 흡수하며 **검수가 필요한 건 테스트 200건뿐**이다 |
| 2026-09-04 | 두 곳 수정 — [generate.py](src/generate.py) `MUST_KEEP` 신설, [sampler.py](src/sampler.py) `TENSE_NOTE` 에 금지문 추가 | `MUST_KEEP` 은 **혼동 지시보다 뒤에** 출력한다 — 마지막 지시가 앞의 것을 덮는다. 툴별 표라 다른 툴에서 같은 문제가 나도 한 줄로 끝난다. `TENSE_NOTE` 에서 `"결정 16 축③"` 문구 제거(우리 문서 용어라 GPT엔 노이즈) |
| 2026-09-04 | **30건 v3 — 목표 2건 해소, 폐기율 0%.** 새 결함 1건 | 🔴 `"할아버지처럼"` — 조사 하나가 주어를 비유로 바꿔 `caregiver` 라벨이 틀렸다. **verify가 못 잡는 유형**(글자는 발화에 있다). caregiver 시드 9건 중 1건(11%)이라 규칙 1번에 **"적힌 조사만"** 을 명시. 🟡 `"조금 전"` 의 `"조금"` 이 `severity=mild` 로 잘못 채워짐 → **고치지 않고** [labeling-guide.md](labeling-guide.md) §7에 알려진 한계로 기록 |
| 2026-09-04 | **2,400건 본생성 실행** (`--split`, seed 0) | 배치 400회라 30~40분. 진행 표시를 25배치마다 출력하도록 추가 — 그 전엔 완전히 조용해서 살아 있는지 알 수 없었다 |
| 2026-09-04 | **본생성 완료 — 폐기 1.2% · 수정 13.5%.** 층 비중·툴 분포·`caregiver` 전부 목표 적중 | **정방향 값 교체가 11건(0.5%)뿐**인 것이 핵심 지표다 — GPT가 라벨을 거의 어기지 않았다는 뜻. 수정 323건은 대부분 **역방향 채움**(`time` 185건)이라 라벨이 틀린 게 아니라 더 채워진 것이다. 폐기 1위는 `symptom` 10건 — 서술어형 어미가 갈려 매칭 실패. **보류 항목에 적어둔 예측이 그대로 맞았다** |
| 2026-09-04 | 🔴 **테스트 200건의 층이 치우친 것 발견** — `tense` 20→12, `ambiguous` 10→6 | 원인은 `kept[:200]` 으로 **앞에서 그냥 자른 것.** 섞여 있어도 200건 표본에선 이만큼 흔들린다. **Day 4 층별 분해가 12건 위에서는 성립하지 않는다** — 1건이 8%p를 움직여 실력과 우연이 구분되지 않는다. **층 비율 유지 분할로 교체**하고 기존 데이터를 재생성 없이 다시 나눴다(비용 0). 나눈 뒤 다시 섞는다 — 층 순서로 정렬되면 검수할 때 앞뒤 발화가 서로 힌트가 된다 |
| 2026-09-04 | **채점기 [src/evaluate.py](src/evaluate.py) 작성** (Codex) + 검수용 Markdown 생성기 [src/review_dataset.py](src/review_dataset.py) | 첫 `{` 부터 짝이 맞는 `}` 까지를 꺼낼 때 **문자열 안의 중괄호와 이스케이프를 구분**한다 — `{"food":"쌀{밥}"}` 같은 출력에 안 깨진다. 툴 환각일 때 `arguments` 키 검사를 건너뛰어 판정 순서 ③/④를 지켰다 |
| 2026-09-04 | **채점기 교차 검토에서 점수를 바꾸는 결함 2건 발견** | 🔴 **조사만 다른 값이 완전 일치로 통과했다** — 규칙은 "조사만 다른 *오답*"이라고 적었는데 `return` 이 빠져 그대로 `exact` 로 떨어졌다. 함수 주석은 정반대로 "완화하지 않는다"고 적혀 있어 더 안 보였다. **완전 일치율이 부풀려져 P1·P2 판정이 흔들린다** 🔴 **거절 사유만 틀린 것을 "놓친 거절"(심각도 높음)로 분류했다** — 모델은 거절을 했고 사유만 틀렸다. **안 A 채택 — `parameter_error`.** 7분류를 늘리지 않으면서 실제 상태를 담는다 |
| 2026-09-04 | 같은 검토에서 5건 추가 수정 | 🟡 빈 문자열이 `null` 과 같게 처리됨(`normalize(None)` 이 `""`) 🟡 `called_tool_has_reason` 검사가 툴 환각보다 먼저 걸려 환각을 가림 → 순서를 뒤로 **(검사 자체는 규칙의 진짜 구멍을 메운 것이라 유지)** 🟢 헤더 주석 · 코드펜스 판정을 "감쌌는가"로 엄격화 + 파싱 실패 시에도 기록 · 예측 파일에 키가 없으면 조용히 파싱 실패로 집계되던 것을 즉시 중단으로 |
| 2026-09-04 | **"정답 200건을 넣어 200/200"은 정확성 검증이 아니다** | 정답을 정답과 비교하면 **완전 일치 경로만** 지나가 위 두 결함이 안 드러난다. 회귀 검사로는 유효하나 **틀린 출력을 넣는 검사가 따로 필요하다** — 자체 검사를 5건 → 12건으로 늘려 판단이 필요했던 자리마다 못을 박았다 |
| 2026-09-04 | 결함 4층 분류(스펙·시드·지시·검증)를 만들고 9건을 대입 — **7건이 "지시"에 몰렸고 그중 4건이 "경계 없음"** | **원칙 도출 — 긍정 지시는 방향만 정하고 범위는 금지와 실패 예시가 정한다.** GPT는 열린 지시를 받으면 가장 자연스러운 쪽으로 계속 넓힌다(`"보여줘"` 금지 → `"체크해줘"` 로 우회). **분류는 불완전했다** — 테스트셋 층 편중이 네 층 어디에도 안 들어갔다. **⑤ 분할** 을 추가해야 한다. README 데이터 방법론에 쓴다 |
| 2026-09-06 | **예측 생성 스크립트 작성 → [src/predict.py](src/predict.py)** | 세 대상이 한 스크립트를 쓴다. **프롬프트만 대상별로 다르고 그 차이가 실험 자체다**(결정 2) — 통일해야 하는 건 출력 처리·지연 측정·파일 형식이다. `--dump` 로 프롬프트만 뽑을 수 있게 해 Colab에서도 같은 프롬프트가 쓰이도록 했다. few-shot은 **학습셋에서만** 뽑는다 |
| 2026-09-06 | 5건 실측으로 `predict → evaluate` 전 구간 확인 | 첫 실측 — **GPT-4.1 건당 입력 1,539토큰 · p50 819ms.** 결정 2의 비교 기준선이 이 숫자로 잡힌다 |
| 2026-09-06 | 그 5건의 오답 3건을 뜯어보다 **`verify.py` 정방향의 구멍 발견** | 정방향 1단계가 *"라벨이 발화에 있는가"* 만 보고 **더 긴 시드 값이 발화에 있는지는 안 본다.** `"점심때"` 발화에 `"점심"` 라벨이 부분 문자열로 걸려 통과했다. 역방향에는 `_longest` 가 있는데 정방향엔 없다 — [labeling-guide.md](labeling-guide.md) §7 ②의 반대 방향이다. **`pilot_30` 전수로 28건 중 2건(7%).** 고치면 GPT 재호출 없이 기존 데이터에 다시 돌릴 수 있으나 기록된 수치가 바뀌므로 **데이터를 옮겨온 뒤 판단한다** |
| 2026-09-06 | 같은 3건 중 2건은 **모델 문제였다** — 처음엔 셋 다 라벨 문제로 봤다 | `"정오"` → `"정오쯤"`(GPT가 접미사 추가) · `"일주일째"` → `"일주일"`(GPT가 접미사 제거). 둘 다 시드에 없는 형태라 라벨이 옳다. **접미사 차이는 `has_only_particle_difference` 가 못 센다**(조사만 본다). 채점 규칙은 CLAUDE.md 고정이므로 건드리지 않고 **Day 6 실패 사례 분석 거리로 남긴다** — 세 대상을 같은 기준으로 재므로 비교는 오염되지 않는다 |
| 2026-09-06 | 노트북 PC에서 `data/` 복사 후 **무결성 전수 검사** | 분할 간 **id 겹침 0 · 발화 겹침 0**(외운 걸 실력으로 재는 사고 없음). 라벨 스키마·enum·필수 파라미터 **2,360건 위반 0**. `test` 층 21.5/29.5/27.5/10.0/7.0/4.5% — `tense` 20건으로 층 비율 유지 분할이 실제로 동작했다 |
| 2026-09-06 | 🔴 **`verify.py` 정방향 수정 제안을 재보고 철회.** 고쳤으면 데이터가 망가졌다 | "더 긴 값이 걸리면 교체"로 넓게 잡으면 `test` 5건이 바뀌는데 **그중 4건이 오답이 된다** — `activity_type training→walk`("산책 끝나고 **앉아 연습**"), `until "어제"→"3일 전"`(3일 전은 `since` 쪽). **정방향에서 다른 값이 걸리는 건 더 구체적인 형태가 아니라 발화 속 다른 언급이다.** 규칙을 *"현재 표층을 포함하면서 더 긴 것"* 으로 좁히니 **test 0 · val 0 · train 9건(0.5%)** → **고치지 않는다.** [labeling-guide.md](labeling-guide.md) §7 ④에 기록. 교훈 — **자동 보정은 잘못 잡으면 조용히 정답을 망가뜨린다. 규모를 재보고 결정한다** |
| 2026-09-06 | 검수보다 **베이스라인 1을 먼저** 돌리기로 함 | 순서상 검수가 앞이지만 200건 재실행 비용이 수백 원이라 순서를 뒤집을 만하다. **일정이 2일 밀린 상태에서 "GPT-4.1 점수가 손에 있다"는 사실이 폴백을 성립시킨다.** 검수 후 나온 숫자를 최종본으로 쓰고 지금 것은 중간 확인으로 둔다 |
| 2026-09-06 | **베이스라인 1 완료 — GPT-4.1 완전 일치 73.5%.** 안전선 도착 | 툴 환각 0% · 파싱 실패 0% · 코드펜스 0건. **입력 토큰 건당 1,542** — 결정 2의 비교 기준선. 층별 최저는 `multi` 62.7%(파라미터를 많이 담을수록 어렵다), 최고는 `out_of_scope` 92.9% |
| 2026-09-06 | 툴별 분해에서 `log_water` 31.6% · `log_symptom` 25.0% 이상치 발견 → 실패 건을 전수로 읽음 | **두 툴의 실패 성격이 달랐다.** `log_symptom` 6건은 전부 `severity` 경계 — 시드의 `moderate:"좀 심하게"` 가 `severe:"심하게"` 를 **문자열로 포함**해 GPT가 severe로 읽는다. 사람도 갈리는 자리라 모델 탓이 아니다. Day 6 실패 사례 분석 거리 |
| 2026-09-06 | 🔴 **`log_water` 라벨 결함 발견 — 설계 결함이다** | `"오빠가 점심에 거의 안 **드신** 것 같습니다"` → 정답 `log_water`. `"거의 안 **먹엇어**"` → 정답 `log_water`. **GPT가 맞고 우리 라벨이 틀렸다.** 원인 — `log_water` 의 파라미터는 `amount`·`time`·`caregiver` 뿐이라 **툴을 식별하는 단어가 라벨 어디에도 없다.** `IDENTIFYING_PARAM` 이 `"amount"` 인데 `"한 그릇"`·`"거의 안"` 은 물·밥을 못 가린다. 설계 때 "발화의 동사가 식별한다"고 봤으나 급한 메모체에서 동사가 사라진다. **규모 — test 4/19(21%) · val 3/14(21%) · train 34/132(26%)** |
| 2026-09-06 | 같은 결함을 [generate.py](src/generate.py) `MUST_KEEP` 이 이미 다루고 있었다 | `get_vet_summary` 에 대해 *"툴의 정체성이 파라미터가 아니라 맥락에 있는 경우"* 라고 적어뒀는데 **`log_water` 가 같은 부류인데 빠져 있었다.** 장치는 있었고 적용 대상을 놓친 것 |
| 2026-09-06 | **사람 검수를 시작하기도 전에 베이스라인 실행이 데이터 결함을 드러냈다** | 검수는 사람 눈만의 일이 아니다 — **상한선 모델의 오답을 읽는 것이 시험지 검증을 겸한다.** 상한선이 틀렸다고 하는 자리를 의심해보는 절차가 값이 크다. README 데이터 방법론에 쓴다 |
| 2026-09-06 | 대응으로 **A안 채택** — `MUST_KEEP` 에 `log_water` 추가 후 해당 행만 재생성 | B(폐기)는 `log_water` 를 165→124건으로 깎고 **상한선 모델이 틀린 문항을 시험에서 빼는 모양**이 된다. C(방치)는 Day 4에 "모델 한계인가 데이터 결함인가"를 못 가리게 해 **결정 8·9·11이 지켜온 "오답 원인을 섞지 않는다"를 데이터 층에서 스스로 깬다.** A만 근본 원인을 없애고 데이터도 시험지 크기도 유지한다 |
| 2026-09-06 | 결함 규모를 **두 번 정정했다** — 41 → 31 → **22건** | 첫 패턴이 `"마신"`·`"마셔"` 를 빠뜨렸고, 두 번째도 **오타 페르소나의 `"마섯음"`·`"마셯슴"` 과 `"200ml"` 를 결함으로 몰았다.** 멀쩡한 데이터 9건을 헛돌릴 뻔했다. **교훈 — 검출 패턴 자체를 데이터로 검증하고 나서 대상을 확정한다** |
| 2026-09-06 | **부분 재생성 도구 작성 → [src/regenerate.py](src/regenerate.py)** | 전체 재생성은 비용 400배에 **분할이 통째로 달라져** 기존 수치와 이어지지 않는다. `id`·`layer` 를 유지해 학습/검증/테스트 소속이 안 흔들린다. 재시도 3회 후에도 실패하면 **원본을 유지하고 보고한다** — 고치려다 잃는 것이 고치지 않는 것보다 나쁘다 |
| 2026-09-06 | 🔴 **생성 프롬프트에서 조용한 데이터 손실 결함 발견** | 배치가 작으면 **GPT가 실제 id를 무시하고 출력 예시의 `"0001"` 을 그대로 베낀다.** 1건을 요청했는데 `0001`·`0002` 두 건을 냈다. id가 안 맞으니 `generate()` 가 조용히 실패로 세고 데이터가 사라진다. **본생성 2,400건의 "API 실패 9건" 중 일부가 이것일 수 있다.** 규칙에 *"대괄호 안 번호를 그대로 옮겨 적는다 · 항목 수만큼만 출력한다"* 를 명시해 해결 |
| 2026-09-06 | **재생성 완료 — 22건 전부 교체.** `train`·`val`·`test` 결함 0건 | 건수(1,960/200/200) · 층 분포 · `log_water` 165건 · id·발화 겹침 0 · 스키마 위반 0 **전부 그대로.** A안이 노린 "데이터를 잃지 않는다"가 지켜졌다. `get_vet_summary` 는 0/168 로 원래 깨끗했다 — 9월 4일 `MUST_KEEP` 이 실제로 작동했다는 증거 |
| 2026-09-06 | **베이스라인 1 재실행 — 확정 73.0%.** `log_water` 31.6% → **47.4%** | 라벨 수정이 의도대로 들었다. 전체는 73.5% → 73.0% 로 소폭 내렸는데 **`log_water` 개선(+3건)을 다른 툴의 변동(-4건)이 상쇄한 것** |
| 2026-09-06 | 🔴 **`temperature=0` 인데 재실행 결과가 달랐다 — Day 4 해석의 전제가 된다** | 시험지에서 바뀐 건 `log_water` 4건뿐인데 **입력이 동일한 툴까지 움직였다** — `get_vet_summary` 100% → 89.5%, `get_records` 71.4% → 61.9%. **전체 노이즈는 ±0.5%p 라 P1 기준(+15%p)은 안전하다.** 그러나 **툴별 분모가 7~21건이라 1~2건이 10%p를 움직인다.** Day 4의 툴별·혼동 쌍별 분해에서 이 폭 안의 차이를 실력 차로 읽으면 안 된다 |
| 2026-09-06 | **검수보다 Day 3을 먼저 하기로 순서를 뒤집었다** | 안전선은 이미 확보됐고(GPT-4.1 73.0%), 남은 병목은 사람이 아니라 **GPU 자원**이다 — 미루면 그날 할당량이 사라진다. **검수가 라벨만 고치면 `evaluate.py` 를 다시 돌리는 것으로 끝나 비용이 0원**이라 미뤄도 잃는 게 없다(`--gold`·`--pred` 분리). 검수는 `test.jsonl` 만 건드려 학습을 막지 않으므로 **학습이 도는 1~2시간에 한다** |
| 2026-09-06 | **학습 데이터 변환 → [src/build_sft.py](src/build_sft.py)** | 1,960 + 200건을 대화 포맷으로. **프롬프트를 새로 짜지 않고 `predict.py` 의 `build_messages("ft", ...)` 를 불러 쓴다** — 양쪽이 한 함수를 쓰면 어긋날 수가 없다. 라벨 키 순서를 `tools.py` 정의 순서로 고정(재정렬 0건 — 샘플러가 이미 맞게 내고 있었다). 채점기는 파싱 후 비교라 순서를 안 보지만 **모델은 글자를 배운다** |
| 2026-09-06 | **Colab 노트북 작성 → [notebooks/pawnote_colab.ipynb](notebooks/pawnote_colab.ipynb)** | 셀 10개. **프로젝트 코드를 clone 하지 않는다** — 프롬프트를 로컬에서 `--dump` 로 뽑아 올리므로 Colab은 읽어서 실행만 한다. Colab에서 다시 조립하면 GPT-4.1과 같은 시험을 본 게 아니게 된다. **채점도 로컬에서만** 한다 — 채점기를 두 군데서 돌리면 언젠가 한쪽만 고쳐진다 |
| 2026-09-06 | 노트북에 **세션을 태울 만한 자리 셋을 미리 막았다** | ① dtype `assert` — bf16이면 로드는 되고 학습에서 터진다 ② **LoRA 붙이기 전에 모델을 다시 읽는다** — 추론 모드로 바꾼 객체에 붙이면 상태가 섞인다. 1분이 아까워 한 시간을 쓸 이유가 없다 ③ **TRL 인자 이름을 `inspect` 로 맞춘다** — 버전에 따라 `max_seq_length`/`max_length`, `tokenizer`/`processing_class` 가 갈려 `TypeError` 하나로 세션이 날아간다 |
| 2026-09-07 | 🟢 **P0 완주 — Ollama 서빙까지 닫았다** | 데이터 생성 → 학습 → 평가 → **서빙** 4단계 완료. 4자 비교표와 7분류 오답 분해표 산출. **성능 수치와 무관하게 P0는 성립한다** |
| 2026-09-07 | 🔴 **양자화로 90.0% → 81.5% (-8.5%p).** Day 5가 물어보려던 것의 답이다 | Colab 정답이 Ollama 오답으로 **19건**, 반대가 2건. **형식 안정성은 유지됐다** — 툴 환각 0% · 파싱 실패 0%. 깎인 것은 **표층 복사 정확도**다. `"싹 다"` → `"모두"`(그대로 옮기지 않고 바꿔 씀) · `"아침 8시"` → `"8시"`(구간 잘림) · `since`/`until` 뒤바뀜 · 과잉 거절 5건 · `caregiver` 키 통째 누락 1건. **4비트 양자화가 "발화에 나온 그대로"를 가장 먼저 잃는다** — 결정 9·11이 표층 복사에 건 판돈이 여기서 비용을 치른다 |
| 2026-09-07 | 지연 p50 **5,504ms** — 하드웨어 한계다 | GTX 970 **VRAM 4GB**에 3.8GB 모델이 안 들어가 **36%가 CPU로 넘어갔다**. `num_ctx` 를 4096 → 512 로 줄여봤으나 6,321 → 5,242ms 로 **17%만** 개선돼 재실행하지 않았다. **P2 지연 조건은 이 하드웨어에서 미달**이고, 그것은 "우리 2014년 GPU vs OpenAI 데이터센터" 비교라는 뜻이다. README에 하드웨어를 명시한다 |
| 2026-09-07 | 🟢 **GGUF 변환 완료 — `Q4_K_M` 2.326GB.** 온디바이스 타당성의 근거 숫자 | 4B 모델이 **2.3GB**로 떨어졌다. 요즘 폰 저장공간 기준으로 앱 하나에 얹을 만한 크기다. 실기기 벤치마크는 범위 밖이므로 **파일 크기 + CPU-only 지연**으로 간접 논증한다 |
| 2026-09-07 | 🔴 **Ollama 서빙이 2토큰 만에 멈췄다 — `</tool_call>` 만 뱉었다** | 원인은 **특수 토큰 매핑 어긋남**이다. GGUF 변환 로그의 `Restored added_tokens_decoder metadata` 가 그 지점이고, 모델의 `<think>` 가 `</tool_call>` 로 매핑돼 Ollama 가 정지 토큰으로 읽었다. **모델은 멀쩡했다** — `<think>\n\n</think>` 를 미리 넣어주면 완벽한 JSON이 나오고, 일반 질문에도 정상 답변한다. **증상만 보고 학습 실패로 결론냈으면 하루를 날렸을 자리다** |
| 2026-09-07 | 그 과정에서 **학습 라벨에 `<think>` 가 들어 있었다는 것을 알아냈다** | Ollama 가 보여준 GGUF 내장 Jinja 템플릿을 읽어보니, Qwen3 템플릿이 마지막 assistant 턴을 `<|im_start|>assistant\n<think>\n\n</think>\n\n{내용}` 으로 만든다. `train_on_responses_only` 가 그 뒤 전부에 손실을 걸었으므로 **모델이 이 블록을 먼저 내는 것이 정상 동작**이다. Colab에서 200건 전부 붙었던 이유가 이것이다 — "채팅 템플릿이 넣는 것"이라고만 적었던 추측이 확인됐다 |
| 2026-09-07 | 대응 — **Modelfile 템플릿이 아니라 `raw` 모드로 프롬프트를 직접 조립한다** | `Modelfile` 의 `TEMPLATE` 로 덮으려 했으나 **Ollama 가 GGUF 내장 Jinja 템플릿을 써서 안 먹었다**(입력 토큰이 그대로였다). **프롬프트가 바이너리 안에 있으면 무엇이 들어갔는지 읽어서 확인할 수 없다** — 이 프로젝트가 `--dump` 를 만든 이유와 같은 문제다. `predict.py` 에 `render_qwen()` 을 두고 `/api/generate` `raw` 로 보낸다. ⚠ 접두사를 미리 주므로 **출력 토큰이 Colab 실측보다 적게 잡힌다** |
| 2026-09-07 | 🔴 **GGUF 변환이 디스크 부족으로 죽었다 — 드라이브에 바로 쓰면 안 된다** | 필요 **17.1GB**, 드라이브 여유 8.1GB. 변환이 **16비트 병합(~8GB) → f16 GGUF(~8GB) → q4_k_m(~2.5GB)** 순으로 만드는데 **중간 산출물이 동시에 디스크에 남는다.** 최종 파일만 보고 2.5GB면 되겠다고 본 것이 오판이었다. **Colab 로컬 디스크(70GB+)에서 변환하고 최종 `.gguf` 만 드라이브로 복사**하도록 ⑨를 고쳤다. 로컬 디스크는 세션과 함께 사라지지만 중간 산출물은 버릴 것들이라 무해하다 |
| 2026-09-07 | 🟢 **Day 3 완주. 파인튜닝 90.0% — GPT-4.1(73.0%)을 넘었다** | base few-shot **40.5%** → 파인튜닝 **90.0%**, **+49.5%p**(P1 기준 +15%p). **툴 환각 0% · 스키마 위반 0% · 파싱 실패 0% · 과잉 거절 0%** — CLAUDE.md가 예상한 *"정확도보다 형식 안정성이 극적으로 오른다"* 가 그대로 나왔다. **P1 세 조건 전부 달성 · 폴백 미발동** |
| 2026-09-07 | **결정 2의 성과 — 입력 토큰 2,094 → 27 (78배)** | 처음에 GPT-4.1의 1,542와 비교해 "57배"라고 적었다가 정정했다. **토크나이저가 다르면 같은 프롬프트도 토큰 수가 다르다**(GPT-4.1 1,542 vs Qwen3 2,094). 같은 모델·같은 토크나이저인 **2,094 → 27** 이 유일하게 공정한 비교다. GPT-4.1 수치는 비용 계산에서 각자 요금 체계로만 쓴다 |
| 2026-09-07 | **90% > 73% 를 "GPT-4.1보다 똑똑하다"로 읽지 않기로 못박았다** | 테스트셋이 학습셋과 **같은 파이프라인·같은 라벨 협약**에서 나왔다. 파인튜닝 모델은 그 협약을 학습했고 GPT-4.1은 프롬프트로 한 번 읽었다. **맞는 해석은 "정해진 출력 협약을 지키게 만드는 데는 파인튜닝이 압도적으로 낫다"** 이고 그게 애초에 재려던 것이다. README에 이 문장을 그대로 쓴다 |
| 2026-09-07 | 🔴 **놓친 거절 3건 중 하나가 안전 경계 위반이었다** | `"초코 이 약 오늘 좀 줘도 되나?"`(투약 판단 요청) → `log_medication` 으로 기록했다. 실제 앱이면 **하지도 않은 투약이 기록되고 `get_care_due` 가 "이미 줬다"고 답한다.** 정확도 표의 1.5% 뒤에 숨기지 않고 **별도 항목으로 보고한다** — 경계를 README에 명시한 이상 그래야 한다. 학습 데이터의 `out_of_scope` 에서 투약 판단 비중을 늘리는 것이 Day 4 재학습 후보 |
| 2026-09-07 | 파인튜닝 오답 20건을 전수로 읽었다 — **5건이 라벨 결함으로 보인다** | `"오후 3시쯤"` 을 모델이 그대로 담았는데 라벨이 `"오후 3시"`(§7 ② 삼켜진 매치) · `"잠깐 전에 먹음"` 에 라벨 `time=null` · `"덴탈껌 하나 다"` 에 라벨 `amount=null`. **검수하면 90%가 더 오를 가능성이 높다.** 90.0%는 검수 전 숫자라고 명시한다 |
| 2026-09-07 | `<think>` 빈 블록이 **200건 전부**에 붙는다 | Qwen3 채팅 템플릿이 넣는 것이고 학습·추론에 같은 템플릿을 썼으므로 일관돼 있다. 채점기가 첫 `{` 부터 읽어 **파싱 영향 0**(블록 안 중괄호 0건 실측). 코드펜스와 같은 부류라 **별도 지표로 보고**한다. 출력 토큰이 34~37 → 40 으로 는 원인 |
| 2026-09-09 | **테스트 200건 검수 완료 — 169건이 남았다** | 맞음 156 · 라벨수정 13 · 버림 31. 사람이 72건을 직접 보고, 나머지 128건은 초안을 만들어 **버림·라벨수정·보류 25건만 사람이 확인**하는 방식으로 나눴다. 반영은 [src/apply_review.py](src/apply_review.py) 가 하고 원본은 안 건드린다 — TSV 의 `사유` 는 사람이 읽는 근거, `FIXES` 표는 기계가 읽는 값이고 **둘이 서로를 검증한다**(한쪽에만 있으면 멈춘다) |
| 2026-09-09 | 🔴 **예측이 틀렸다 — 검수하면 오를 줄 알았던 파인튜닝이 90.0% → 87.0% 로 내렸다** | 원인이 명확하다. **버린 31건 중 26건을 파인튜닝이 맞히고 있었고**(GPT-4.1 은 19건), 라벨을 고친 13건에서는 **맞음이 9 → 2 로 줄었다**(GPT-4.1 은 6 → 3). 즉 **라벨 결함이 한쪽으로 치우쳐 모델에게 유리했다.** 같은 생성 파이프라인이 만든 데이터로 학습했으니 모델은 결함까지 같이 배웠고, 그 결함을 시험지도 공유하고 있었다. **합성 데이터로 자기 학습 데이터와 같은 공정의 시험지를 만들면 점수가 부풀려진다** — README 데이터 방법론의 핵심 문단 |
| 2026-09-09 | **사전 등록한 경고가 맞았다 — `tense` 100% 는 실력이 아니었다** | Day 2 에 *"`tense` 는 `distinct-2` 0.613 으로 최저다. Day 4 에서 이 층 정확도가 높게 나오면 다양성 부족을 의심한다"* 고 적어뒀다. 검수 후 파인튜닝 `tense` 는 **100.0%(20/20) → 81.2%(13/16)** 이 됐다. 반대로 **base + few-shot 은 같은 층에서 65.0% → 81.2% 로 올랐다** — 맞은 개수는 13 그대로인데 틀리게 채점하던 행이 빠진 것이다. **라벨 결함은 학습한 모델을 올리고 안 배운 모델을 내린다**가 한 층에서 양방향으로 보였다 |
| 2026-09-09 | 4자 재채점 완료 — **GPT-4.1 73.4% · base 42.6% · 파인튜닝 87.0% · Ollama 79.3%** | P1 3/3 ✅ (+44.4%p · 스키마 위반 0% · 툴 환각 0%), P2 1/3 (GPT 대비 **119%** ✅ · 비용 3.2배 ❌ · 지연 ❌). 판정은 하나도 안 뒤집혔다 — **결론이 시험지 결함에 의존하지 않았다는 뜻이라 오히려 근거가 세졌다.** 검수 전 표는 `data/report_prereview.md` 에 남겨 대조용으로 쓴다 |
| 2026-09-09 | **임베딩 중복을 재고 — 지우지 않기로 했다** | 유사도 0.9 이상이 `train` 안 10쌍(1.0%) · `val` 0 · `test` 0 · **`test`↔`train` 3건(1.8%)**. 셋을 빼면 완전 일치율이 **전 대상에서 오른다**(GPT +0.1%p · base +0.2%p · **파인튜닝 +0.4%p** · Ollama +0.2%p) — 전부 재실행 노이즈 ±0.5%p 안이다. 결정적으로 **그 3건에서 파인튜닝과 GPT-4.1 의 성적이 2/3 로 같다.** 학습에서 본 문장을 외워서 맞힌 것이라면 파인튜닝만 유리했어야 한다. **누출이 있었으나 이득을 주지 않았다**는 것이 측정으로 나왔으므로 지우지 않고 수치를 공개한다 |
| 2026-09-09 | **"제거"라고 적어둔 항목을 "측정"으로 바꿔 실행했다** | 학습이 끝난 뒤라 `train` 에서 빼면 재학습이고 `test` 에서 빼면 방금 확정한 숫자가 또 움직인다. 그리고 **결정 3(역방향 생성)이 정답을 균등 샘플링하므로 라벨이 같은 행끼리 표층이 닮는 것은 설계상 당연하다** — `"병원 가는데 어제 기록"` 두 건이 0.92 인 것은 결함이 아니라 그 툴의 발화가 원래 그렇게 생긴 것이다. §7 ④ 에서 자동 보정을 규모 재보고 접었던 것과 같은 순서를 따랐다 |
| 2026-09-09 | **README 초안 556줄 작성** — 결과를 맨 앞에 뒀다 | 이력서용이라 읽는 사람이 먼저 찾는 것이 숫자다. 다만 표만 던지지 않고 **왜 그 셋을 비교하는지**를 옆에 붙였다 — base + few-shot 줄이 없으면 "파인튜닝이 기여했다"가 성립하지 않는다는 설명이 표 자체보다 중요하다. **P2 두 칸이 ❌ 인 것도 같은 크기로 실었다** — 숨기면 나머지 숫자의 신뢰도가 같이 떨어진다 |
| 2026-09-09 | 🔴 **[labeling-guide.md](labeling-guide.md) §7 ④ 의 "`test` 0건" 이 틀렸다** | 근사 접미사(`쯤`·`정도`·`경`·`남짓`)만 따로 세니 **`test` 1건 · `val` 1건 · `train` 10건**이다. 시험지의 1건 `1825` `"오후 3시쯤"`→라벨 `"오후 3시"` 는 **파인튜닝 오답으로 남아 있고, 결정 9의 "표층 그대로"만 보면 모델 쪽이 맞다.** 결론(고치지 않는다)은 1건이라 그대로 두되 숫자는 바로잡았다. **README 실패 사례에 이 건을 그대로 실었다** — 자기 라벨이 틀린 자리를 빼면 나머지 분석도 의심받는다 |
| 2026-09-09 | 🟡 **안전 경계 준수가 전체 정확도와 따로 논다** | `0938` 투약 판단 요청에서 **4비트 Ollama 는 거절했는데 16비트 원본이 호출했다.** 전체 정확도는 양자화 쪽이 7.7%p 낮은데 이 행만 반대다. **경계 준수를 정확도 표 안에서 읽으면 안 된다**는 근거가 하나 더 생겼다 |
| 2026-09-10 | **MCP 2단계 문단을 쓰면서 한 줄이 새로 나왔다** | [context-notes.md](context-notes.md) §1 을 옮겨 쓰는 일이었는데, 옮기다 보니 **1단계에서 배운 것이 2단계 설계로 이어지는 자리**가 보였다 — 2단계에 툴 호출 로그를 처음부터 남기면 그것이 다음 학습 데이터가 되고, **이 프로젝트가 합성 데이터에 의존해야 했던 이유가 바로 그 로그의 부재**였다. 검수에서 합성의 대가를 숫자로 확인했으므로 근거 있는 설계 지침이 됐다. 계획을 옮겨 적는 일이 계획을 갱신하는 일이 되기도 한다 |
| 2026-09-10 | 🔴 **README에 툴 12개 목록이 없었다** | 574줄을 쓰면서 `log_meal`·`get_vet_summary` 를 본문 내내 쓰는데 **그게 무엇인지 어디에도 안 적혀 있었다.** 읽는 사람은 이름만 보고 따라와야 했다. **자기가 쓴 글은 아는 것을 전제로 읽게 돼서 이런 구멍이 안 보인다** — 라벨 검수가 필요했던 것과 같은 종류의 문제이고, 여기서는 사람 대신 "처음 읽는 사람이면 여기서 막히나"를 절마다 되물어 찾았다. 함께 고친 것 — 12줄짜리 표를 첫 화면에서 치우고 숫자 셋만 남겼고, 결과 절을 넷으로 쪼개 표마다 제목을 붙였고, `h4` 를 없앴다(GitHub 에서 본문과 크기가 비슷해 제목으로 안 읽힌다) |
| 2026-09-06 | `hint` 유출 방지를 주석에서 **검사로 바꿨다** | `leak_check()` 가 user 메시지에 12개 툴 이름이 있으면 멈춘다. 결정 2가 깨지면 파인튜닝 기여도 측정 전체가 무효인데, **"잊으면 반나절 날아가는 것" 목록에 적어두는 것만으로는 언젠가 안 지켜진다.** 실행 결과 유출 0건 |

---

## 다음 세션 시작 지점

> **이어서 작업할 때 이 절과 바로 위 진행 로그를 먼저 읽는다.**

### 🔖 2026-09-09 — 여기서 끊었다

**P0 완주 · 검수 완료 · Day 6 README 초안까지 끝냈다.** 시험지 169건이 확정본이고 `data/report.md` 의 숫자를 그대로 README에 실었다.

```
GPT-4.1 73.4%  ·  base + few-shot 42.6%  ·  파인튜닝 87.0%  ·  Ollama 79.3%
P1 3/3 ✅    P2 1/3 (정확도 119% ✅ · 비용 ❌ · 지연 ❌)
```

**Day 1~6이 전부 닫혔다.** 남은 것은 아래 세 칸뿐이고, 그중 **필수는 두 개**다.

| | 분량 | 필수인가 | 어디서 |
| --- | --- | --- | --- |
| ~~⓪ `data/` 옮기기~~ | — | **2026-09-11 완료** — `report.py` 가 87.0% 재현 | — |
| ~~① CPU-only 지연 측정~~ | — | **2026-09-11 완료** — 78.7% · p50 **6,812ms** | — |
| **② [README.md](README.md) 통독** | 1시간 | **필수** — 아직 사람이 안 읽었다 | 아무 데나 |
| ~~③ MCP 연동 2단계 계획 문단~~ | — | **2026-09-10 완료** | — |
| ④ Day 7 확장 실험 | 반나절~ | 선택 | 아래 별도 절 |

> **2026-09-11 — ⓪①이 데스크탑에서 끝났다. 남은 필수는 ② README 통독 하나뿐이고 기기를 안 가린다.**

#### ① 결과 — CPU-only 로도 돈다

| | 정확도 | 지연 p50 | p95 |
| --- | --- | --- | --- |
| Ollama (GPU 64% + CPU 36%) | 79.3% | 5,461ms | 6,627ms |
| **CPU-only (`--num-gpu 0`)** | **78.7%** | **6,812ms** | 9,167ms |

**정확도가 169건 중 1건 차이다.** 그것을 확인하는 것이 목적이었다 — 정확도는 **양자화의 성질**이지 계산을 어디서 하느냐의 문제가 아니므로, 여기서 크게 벌어졌다면 서빙 경로가 잘못됐다는 뜻이었다.

**지연은 25%만 늘었다.** GPU를 통째로 뺐는데 이 정도인 이유는 **원래도 GPU가 64%만 맡고 있었기 때문**이다(VRAM 4GB에 3.8GB 모델). 이 측정이 GPU의 실제 기여분을 드러냈다.

**6.8초는 실용 속도가 아니다.** 다만 2014년 데스크탑 CPU 수치이고 폰의 ARM·NPU는 성격이 다르므로, *"폰에서 몇 초"* 의 답이 아니라 **"GPU 없이도 돈다"의 증거**로만 쓴다.

> **환경 변수로 Ollama 를 다시 띄우지 않았다.** `predict.py --num-gpu 0` 으로 **요청 단위로** 넘긴다 — 돌아가는 서비스를 안 건드리고, 무엇을 켜고 잰 숫자인지가 명령줄에 남는다.

> **비용 칸은 비워뒀다.** (a) 의 계산법이 *"로컬 추론 시간 × 클라우드 **GPU** 시간 단가"* 로 Day 0에 고정됐는데 CPU 시간 단가는 정하지 않았다. **지금 하나 만들어 넣으면 사후 변경이다.** GPU 단가를 그대로 쓰면 CPU 시간을 GPU 값으로 매기는 셈이라 숫자가 틀린다

#### ⓪① 실행 기록 (2026-09-11 · 데스크탑)

`data/` 를 옮긴 뒤 **`report.py` 가 87.0% 를 재현하는지 먼저 확인**했다. 낡은 `data/` 로 채점해 89.5% 가 나왔던 사고를 다시 겪지 않기 위한 절차이고, 통과했다.

```
python src/predict.py --target ft --input data/test_reviewed.jsonl \
    --engine ollama --model pawnote --num-gpu 0 --out data/pred_cpu.jsonl
python src/evaluate.py --gold data/test_reviewed.jsonl --pred data/pred_cpu.jsonl \
    --details data/eval_cpu.jsonl --subset
```

**`--input` 이 `test_reviewed.jsonl` 이어야 한다.** 검수 전 `test.jsonl` 로 재면 다른 시험지 위의 숫자가 된다.

**지연은 하드웨어에 종속된다.** 모든 서빙 수치가 **GTX 970 · VRAM 4GB** 기준이고, 다른 기기에서 재서 섞으면 비교가 무의미해진다 — [README.md](README.md) 「환경」 표에 조건을 적어뒀다.

#### ② [README.md](README.md) 를 사람이 한 번 통독한다

**615줄을 아직 아무도 통으로 안 읽었다.** 이력서용 프로젝트라 README가 실질 산출물이고, **읽는 사람이 처음 만나는 것이 이 글**이다. 구조와 사실 확인은 끝냈으므로 남은 것은 *"내가 쓴 말로 읽히는가"* 다.

읽으면서 볼 것 셋이다.

- **너무 긴 절** — 「데이터가 실제 작업물이다」와 「실패 사례 분석」이 가장 길다
- **톤** — 문서 문장은 단정체로 통일돼 있는데 어색한 자리가 있는지
- **빠진 것** — 처음 읽는 사람이 막히는 자리. 툴 12개 목록이 없던 것이 그런 구멍이었다

#### ③ MCP 연동 2단계 계획 — ✅ 2026-09-10 완료

「다음 단계」 절 안에 넣었다. 옮겨 쓰면서 **1단계에서 배운 것을 2단계 설계로 되먹이는 줄을 하나 붙였다** — 툴 호출 로그를 처음부터 남기면 다음 학습 데이터가 되고, **이 프로젝트가 합성 데이터에 의존해야 했던 이유가 바로 그 로그의 부재**였다. 검수에서 그 대가를 숫자로 확인했으므로 근거가 있는 설계 지침이 됐다.

---

### Day 7 — 기획 (2026-09-12 작성)

**전부 선택이다.** 안 해도 P0~P2 판정과 README는 이미 성립한다.

#### ⚠ 대전제 — 본 결과는 동결됐다

**Day 7의 어떤 실험도 헤드라인 숫자를 건드리지 않는다.**

새 모델을 학습하는 순간 README의 87.0% 는 *"그 모델"* 의 것이 아니게 된다. 층별 표·오답 22건 분석·양자화 손실·안전 경계 1건이 전부 그 체크포인트에 묶여 있으므로, **재학습 결과로 표를 갈아끼우면 Day 1~6을 다시 써야 한다.**

> **Day 7 산출물은 전부 "추가 실험" 절에 들어간다.** 본 결과를 대체하지 않고 옆에 놓는다.
> 이 원칙이 특히 **C(안전 경계 재학습)** 를 비싸게 만든다 — 아래 참조.

| | 분량 | GPU | 본 결과를 건드리나 |
| --- | --- | --- | --- |
| **A. 결정 9 대조 실험** | 1~1.5일 | 1회 | 아니오 (추가 실험) |
| **E. 데모 UI** | 반나절 | 없음 | **아니오 — 유일하게 위험이 0이다** |
| B. 학습곡선 | 1일 | **3회** | 아니오 |
| C. 안전 경계 재학습 | 1~2시간 + α | 1회 | **예 — 숨은 비용이 크다** |
| D. 병렬 다중 호출 | 2일+ | 1회 + 생성비 | 채점 규칙을 바꿔야 한다 |

**권고 — A 또는 E 하나.** 둘 다 하면 이틀이다. 시간이 반나절이면 **E**, 하루 이상이면 **A**.

---

#### A. 결정 9 대조 실험 — 가장 값이 크다

이 프로젝트에서 제일 자주 받을 질문이 *"왜 시간을 모델이 정규화하지 않게 했나"* 다. 지금은 **근거 넷을 대는 것**으로 답하고 있다. 재보면 **주장이 실측으로 바뀐다.**

##### Phase 0 — GPU 없이 나오는 결과 (먼저 한다 · 2시간)

**`time`·`since`·`until`·`date`·`period` 슬롯의 표층 표현을 전수 조사했다.** 2,329건 중 **1,524건(65%)** 이 시간 슬롯을 채우고 있고, 고유 표현은 **50종**뿐이다(1회만 나오는 것은 1종). 파생 규칙이 손으로 쓸 수 있는 크기라는 뜻이다.

그 50종을 분류했더니 **이것 자체가 결정 9의 근거다.**

| 부류 | 종수 | 출현 | 예 |
| --- | --- | --- | --- |
| **기준 시각만 있으면 답이 하나** | 25 | 954회 (58%) | `오늘` · `엊그제` · `저녁 7시` · `정오` · `지난 금요일` · `2주간` |
| **사람이 관례를 정해야 답이 생김** | 21 | 597회 (36%) | `아침` · `저녁` · `자기 전` · `최근` · `요즘` · `주말` · `며칠 전` · `화요일` |
| **오전/오후가 발화에 없음** | 4 | 97회 (6%) | `9시쯤` · `11시 반` · `8시` · `여섯시쯤` |

> **표현의 42%가 우리가 관례를 정해야만 정답이 생긴다.** `"아침"` 이 07시인지 08시인지, `"최근"` 이 3일인지 7일인지는 **관례이지 정답이 아니다.** 결정 9가 근거 3에서 *"채점이 자의적이지 않다"* 고 적은 자리가 여기다.

**이 표는 학습을 한 번도 돌리지 않고 나온다.** A를 여기서 끊어도 README에 실을 것이 생긴다.

##### Phase 1 — 데이터 파생 (3시간 · GPU 없음)

```
기준 시각을 행마다 하나씩 심는다        seed 고정 · 요일·시간대가 고루 퍼지게
입력 앞에 한 줄 붙인다                  "기준시각: 2026-09-01T08:12 (월)"
시간 슬롯 라벨을 ISO 로 바꾼다          50종 매핑표 + 기준 시각
```

- **GPT 재호출도 재검수도 없다.** 발화는 그대로 두고 라벨만 코드로 바꾼다
- **관례를 `src/time_norm.py` 한 파일에 모은다.** 42%가 관례 의존이므로 그 파일이 곧 실험의 전제다. **파일 자체를 README에 증거로 싣는다** — 감추면 "자의적"이라는 지적을 못 피한다
- 출력 형식을 먼저 정한다 — `time`·`date`는 ISO 시각, `since`·`until`은 ISO 날짜, `period`는 기간(`P7D`). **섞으면 채점이 안 된다**

##### Phase 2 — 학습·평가 (GPU 1회)

**바꾸는 것은 데이터뿐이다.** 하이퍼파라미터·분할·시드를 전부 고정한다. 안 그러면 차이가 결정 9 때문인지 설정 때문인지 못 가린다.

##### Phase 3 — 무엇을 비교할 것인가 ★ 여기가 설계의 핵심이다

**전체 완전 일치율로 비교하면 안 된다.** A안은 일을 더 하므로 당연히 낮게 나오고, 그건 아무것도 증명하지 않는다.

결정 9의 주장은 *"정규화를 시키면 **오답 원인이 섞여** 툴 라우팅 성능까지 흐려진다"* 였다. 그러므로 셋으로 쪼개 본다.

| 지표 | 결정 9가 맞다면 |
| --- | --- |
| ① **툴 정확도** (`tool` 만) | A안에서 **떨어진다** |
| ② **비시간 파라미터 정확도** (시간 슬롯 제외) | A안에서 **떨어진다** |
| ③ 시간 슬롯 정확도 | 낮게 나온다 (예상된 것) |

> **①②가 안 떨어지면 결정 9는 과잉 조심이었다는 뜻이고, 그것도 결과다.**
> 반증 가능한 형태로 미리 적어두는 것이 이 실험의 값이다. 결과가 어느 쪽이든 README에 그대로 싣는다.

`evaluate.py` 는 완전 일치만 세므로 **①②③ 분해 채점 함수를 추가해야 한다.** 본 결과 채점 규칙은 고정이므로 **새 함수로 추가하고 기존 경로는 건드리지 않는다.**

---

#### E. 데모 UI — 유일하게 위험이 0이다

**화면 하나.** 이력서 링크로 전달되는 형태이고, 어떤 숫자도 건드리지 않는다.

```
┌─────────────────────────────────────────────┐
│  "콩이 아침에 사료 반 그릇 줬어"        [기록] │
├──────────────────────┬──────────────────────┤
│  모델이 한 일         │  앱 코드가 한 일       │
│  (파인튜닝 4B · 로컬)  │  (규칙. LLM 아님)      │
│                      │                      │
│  tool  log_meal      │  time  "아침"         │
│  food  "사료"        │     → 09-12T08:00    │
│  amount "반 그릇"     │  amount "반 그릇"     │
│  time  "아침"        │     → 0.5 그릇        │
│                      │                      │
│  입력 27토큰          │                      │
└──────────────────────┴──────────────────────┘
```

**두 칸으로 나눈 것이 핵심이다.** 왼쪽이 이 프로젝트의 산출물이고 **오른쪽이 결정 8·9·11이다.** 결정 9에서 *"앱 코드가 맡는다"* 고 했는데 그 앱 코드가 없으면 주장이 공중에 뜬다 — 화면이 그것을 메운다.

보통은 JSON을 감추는데 **여기서는 보여주는 것이 목적**이다. 기술 데모다.

덧붙일 것 둘.

- **거절 화면** — `"우리 개 무슨 병이야?"` → `out_of_scope` → *"진단은 수의사 상담이 필요합니다"*. **안전 경계가 스키마에서 강제된다**는 것을 화면으로 보여준다
- **토큰 비교** — 27토큰 · 로컬 vs GPT-4.1 2,094토큰 · API 호출

**범위를 화면 하나로 못 박는다.** 로그인·DB·알림 없음. 기록은 브라우저 메모리. Ollama 를 부르는 정적 페이지라 백엔드가 필요 없다. **늘리지 않는 것이 더 어려운 쪽이다.**

---

#### 보류한 셋과 그 이유

**C. 안전 경계 재학습 — 숨은 비용이 크다.** 학습 자체는 1~2시간이지만, **새 체크포인트가 나오면 README의 모든 숫자가 그 모델의 것이 아니게 된다.** 87.0%·층별 표·오답 22건·양자화 −7.7%p·GGUF 재변환까지 따라온다. 시험지 1건을 고치려고 문서 전체를 다시 재는 셈이다.

> 하려면 **"안전 경계 전용 소규모 평가셋"** 을 따로 만들어 A/B 로 붙이는 형태가 맞다. 본 결과는 그대로 두고 옆에 놓는다. 그건 Day 7이 아니라 다음 사이클이다.

**B. 학습곡선 — GPU 3회가 든다.** 무료 Colab 일일 할당량상 하루에 안 끝날 가능성이 높다. 얻는 답(*"몇 건부터 효과가 나는가"*)도 A만큼 이 프로젝트 고유의 질문은 아니다.

**D. 병렬 다중 호출 — Day 7 크기가 아니다.** 출력이 배열이 되므로 **고정된 채점 규칙을 바꿔야 하고**, 데이터도 새로 생성해야 한다(GPT 호출비). README 「다음 단계」에 남겨둔 대로 **다음 과제**다.

### ⚠ 데스크탑에 묶인 것 — 지연 측정뿐

**CPU-only 지연**(위 🔖 절의 명령)과 서빙 재측정은 **GTX 970 · VRAM 4GB** 라는 조건에 묶여 있다. 다른 기기에서 재서 섞으면 비교가 무의미해진다. **노트북에서 Ollama를 돌리는 것 자체는 되지만 그 숫자를 README 표에 넣으면 안 된다.**

### 기기를 옮길 때 — 무엇을 들고 가나

| | 가져가나 | 왜 |
| --- | --- | --- |
| **`data/` 전부** | **✅ 필수** | 수치의 원본이다. 재생성으로 복구되지 않는다 |
| `models/*.gguf` (2.3GB) | ❌ 안 가져간다 | **README 작성에 필요 없다.** 수치는 이미 `data/` 에 있다. 필요하면 드라이브 `PawNote/gguf_lr0.0002_ep2/` 에서 받는다 |
| 어댑터 · GGUF (드라이브) | — | 로그인만 하면 어느 기기에서든 보인다 |
| 코드 · 문서 · 노트북 | — | `git pull` |
| **Colab** | — | **브라우저다. 기기를 안 가린다** |

**Day 6은 글쓰기라 `data/` 만 있으면 어디서든 된다.** `models/` 를 옮길 이유는 서빙을 다시 돌릴 때뿐인데, 그건 위에 적은 대로 **이 데스크탑에서 끝내는 게 맞다.**

### 지금 어디까지 왔나 (2026-09-09 기준)

| 단계 | 상태 |
| --- | --- |
| Day 0 | **완료** |
| Day 1 | **완료** — 툴 스펙 · 어휘 시드 · 파이프라인 4종 · 라벨링 가이드 · 30건 테스트 3회 |
| Day 2 데이터 | **완료** — `data/train.jsonl` 1,960 · `val` 200 · `test` 200. 분포·다양성 확인까지 |
| Day 2 채점기 | **완료** — [src/evaluate.py](src/evaluate.py). 자체 검사 12건 통과 |
| Day 2 예측 생성 | **완료** — [src/predict.py](src/predict.py) · 세 대상 공용 |
| Day 2 베이스라인 1 | **완료 — GPT-4.1 73.0%.** 안전선 도착 |
| Day 2 데이터 결함 수정 | **완료** — `log_water` 22건 재생성 ([src/regenerate.py](src/regenerate.py)) |
| Day 2 검수 | **완료** — 200건 → **169건.** 맞음 156 · 라벨수정 13 · 버림 31 |
| Day 3 | **완료** — 베이스라인 2 **40.5%** · QLoRA 학습 · ft 예측 **90.0%** |
| Day 4 판정 | **P1 세 조건 전부 달성 · 폴백 미발동** |
| Day 5 서빙 | **완료 — P0 완주.** GGUF 2.326GB · Ollama **79.3%** · P2는 정확도만 달성 |
| Day 6 README | **완료** — 620줄 + SVG 3장. MCP 문단까지 들어갔다. **사람 통독만 남았다** |
| Day 7 | **선택.** 위 「Day 7 — 하면 좋은 것」 절 참조 |

> **P0의 4단계가 전부 닫혔다** — 데이터 생성 · 학습 · 평가 · 서빙. 검수까지 끝나 **숫자에 "검수 전"이라는 단서가 붙지 않는다.**
> **일정을 회수했다.** 계획상 오늘이 Day 6이고 실제로 Day 6까지 왔다 — Day 3과 Day 4를 하루에 통과한 것이 컸다.

### 순서를 뒤집은 것이 맞았다

검수를 미루고 Day 3을 먼저 한 판단이 결과로 확인됐다 — **검수가 라벨만 고치면 채점만 다시 돌리면 되므로 미뤄도 잃는 것이 없고**, GPU는 미루면 그날 할당량이 사라진다.

**다만 "검수하면 90.0%가 오른다"는 예상은 틀렸다** — 87.0%로 내렸다. 순서 판단은 맞았고 방향 예측이 틀린 것이라 둘을 섞지 않고 적어둔다.

### 바로 할 일 — 순서대로

1. ~~생성기 · 프롬프트 · 검증 · 빌드 진입점~~ → **완료** ([sampler](src/sampler.py) · [generate](src/generate.py) · [verify](src/verify.py) · [build_dataset](src/build_dataset.py))
2. ~~**30건 생성 테스트**~~ → **완료 (v1·v2·v3).** 종료 기준을 정하고 3회에서 끊었다
3. ~~**2,400건 본생성**~~ → **완료.** 학습 1,960 · 검증 200 · 테스트 200 (층 비율 유지 분할)
4. ~~**다양성 측정 + 중복 제거**~~ → **완료.** `distinct-2` 0.710 · 완전 중복 3건 제거
   - 임베딩 유사도 0.9 이상은 **측정으로 닫았다** — `test`↔`train` 3건뿐이고 빼도 노이즈 안에서 움직인다
5. ~~**7분류 채점기**~~ → **완료.** [src/evaluate.py](src/evaluate.py) · `--self-test` 12건
6. ~~**테스트 200건 사람 검수**~~ → **완료 (2026-09-09). 169건 확정**
   - 판정 기록 `data/review_decisions.tsv`(사람 72) · `data/review_draft.tsv`(초안 128, 사람 확인 완료)
   - 반영은 [src/apply_review.py](src/apply_review.py) → `data/test_reviewed.jsonl` · `data/review_applied.md`. **원본은 안 건드렸다**
   - 검수 도구 — https://claude.ai/code/artifact/7da74cd7-3602-49b6-b79c-fd68e3882265
   - **예상과 반대로 87.0%로 내렸다.** 원인 분석은 진행 로그 2026-09-09
7. ~~**예측 생성 스크립트**~~ → **완료.** [src/predict.py](src/predict.py). 5건 실측으로 채점기까지 연결 확인
8. ~~**베이스라인 1 (GPT-4.1)**~~ → **완료. 안전선 도착.** 확정 73.0%
   ```
   python src/predict.py --target gpt41 --input data/test.jsonl --out data/pred_gpt41.jsonl
   python src/evaluate.py --gold data/test.jsonl --pred data/pred_gpt41.jsonl --details data/eval_gpt41.jsonl
   ```
   - 검수 후 **두 번째 명령만** 다시 돌리면 최종본이 된다 (발화를 안 고쳤다면)
9. **Colab 진입 전 준비 — GPU 없이 되는 일**
   - [x] **학습 데이터 변환** → [src/build_sft.py](src/build_sft.py) · `data/sft_train.jsonl` 1,960 · `sft_val.jsonl` 200
     - **`predict.py` 의 `build_messages("ft", ...)` 를 불러 쓴다.** 손으로 다시 짜지 않는다 — 한쪽만 고쳐지면 재는 것이 파인튜닝 효과가 아니라 포맷 불일치가 된다
     - `hint` 와 `layer` 를 버린다. `hint.confuse_with` 에 툴 이름이 들어 있어 새면 결정 2가 깨진다. **주석이 아니라 `leak_check()` 로 강제**했다
     - 키 순서를 `tools.py` 정의 순서로 고정한다. 채점기는 파싱 후 비교라 순서를 안 보지만 **모델은 글자를 배운다**
   - [x] **프롬프트 dump** → `data/prompts_base.jsonl`(18메시지 = 시스템 + few-shot 8쌍 + 발화) · `data/prompts_ft.jsonl`(발화 한 줄)
     ```
     python src/predict.py --target base --input data/test.jsonl --shots data/train.jsonl --dump data/prompts_base.jsonl
     python src/predict.py --target ft   --input data/test.jsonl --dump data/prompts_ft.jsonl
     ```
     - ⚠ **검수에서 발화를 고치거나 버렸으면 이 둘을 다시 뽑는다.** 라벨만 고쳤으면 그대로 써도 된다
   - [x] **Colab 노트북 작성** → [notebooks/pawnote_colab.ipynb](notebooks/pawnote_colab.ipynb). 셀 10개, 붙여넣기 없이 그대로 연다
   - [x] **구글 드라이브 `MyDrive/PawNote/` 생성 후 4개 파일 업로드**
     - `test.jsonl` 은 안 올렸다 — **채점은 로컬에서만 한다.** 채점기를 두 군데서 돌리면 언젠가 한쪽만 고쳐진다
10. ~~**Day 3 Colab**~~ → **완료.** dtype 확인 → 베이스라인 2 **40.5%** → QLoRA → ft **90.0%**
11. ~~**Day 5 서빙 — GGUF 변환 + Ollama**~~ → **완료. P0 완주**
    - [x] GGUF 변환 → **`Q4_K_M` 2.326GB**
    - [x] Ollama 서빙 → 200건 재평가 **81.5%** (Colab 90.0% 대비 **-8.5%p**)
      - **변환에서 떨어졌다.** 형식 안정성은 유지(툴 환각 0 · 파싱 실패 0), **표층 복사가 깎였다**
    - [x] 지연 p50 5,504ms — GTX 970 · VRAM 4GB · **36% CPU 오프로드**
    - [x] 비용 집계 → [src/report.py](src/report.py) · `data/report.md`
    - [ ] **CPU-only 지연** ← **이 데스크탑에서만 할 수 있다.** 위 🔖 절 참조
12. ~~**Day 6 README**~~ → **초안 완료 (2026-09-09).** [README.md](README.md) 556줄 + [src/charts.py](src/charts.py) SVG 3장
    - [ ] **MCP 연동 2단계 계획 문단** ← **여기부터.** [context-notes.md](context-notes.md) §1 을 옮겨 쓰면 된다
    - [ ] CPU-only 지연 수치가 나오면 「측정하지 않은 것」·「환경」 두 표를 고친다

### ⚠ 기기를 옮길 때 — 이 절을 먼저 읽는다

> **용어 주의 — 이 문서에서 "노트북"은 `.ipynb`(Colab 문서)를 가리킨다.** 랩탑은 **노트북 PC**로 적는다.

**git으로 넘어오는 것 / 안 넘어오는 것이 갈린다.** `.gitignore`가 `data/*`를 제외한다.

| | 어디에 있나 | 옮기는 법 |
| --- | --- | --- |
| 코드 · 문서 · [노트북](notebooks/pawnote_colab.ipynb) | git | `git pull` |
| **`data/` 전부** | 이 기기에만 | **폴더째 복사** (USB·클라우드 아무거나) |
| **학습된 어댑터 · GGUF** | **구글 드라이브 `MyDrive/PawNote/`** | **옮길 필요 없다.** 어느 기기에서 로그인해도 보인다 |

**`data/` 에서 반드시 챙길 것** — 하나라도 빠지면 그 대상의 수치를 다시 만들어야 한다.

```
test.jsonl · train.jsonl · val.jsonl          시험지와 학습 데이터
sft_train.jsonl · sft_val.jsonl               학습용 변환본
prompts_base.jsonl · prompts_ft.jsonl         Colab 입력
pred_gpt41.jsonl · pred_base.jsonl            채점 완료된 예측
pred_ft_lr0.0002_ep2.jsonl                    (셋 다 있어야 비교표가 재현된다)
```

**재생성으로는 복구되지 않는다** — `sample()`은 seed 고정이라 시드까지 같지만 `generate()`가 `temperature=1.0`이라 발화가 달라진다. 기록된 수치(1,960/200/200 · distinct-2 0.710 · 폐기 1.2%)가 전부 다른 데이터셋의 것이 되고, `pred_gpt41.jsonl`을 잃으면 **GPT-4.1 200건을 돈 내고 다시 돌려야 한다.**

**새 기기에서 첫 확인** — 이게 통과하면 어디서든 이어서 할 수 있다.

```
git pull
python src/evaluate.py --gold data/test.jsonl --pred data/pred_ft_lr0.0002_ep2.jsonl
→ 완전 일치 90.0% 가 나와야 한다
```

### Day 5 실행 순서 — 기기를 옮겨도 이대로 하면 된다

**전반부는 Colab(GPU 필요), 후반부는 로컬(GPU 불필요).**

**A. Colab에서 GGUF 변환** — [노트북](notebooks/pawnote_colab.ipynb) ⑧⑨ 셀

1. 노트북을 열고 **런타임 → 런타임 유형 변경 → T4 GPU**
2. **①②③만** 돌린다 (설치 · 마운트 · dtype). 학습은 다시 안 한다
3. **⑧** 어댑터를 드라이브에서 읽는다 (`adapter_lr0.0002_ep2`)
4. **⑨** GGUF 변환. `q4_k_m` 으로 20~40분. **파일 크기를 적어둔다 — 온디바이스 타당성의 근거다**

**B. 로컬에서 Ollama 서빙** — 여기부터 GPU가 필요 없다

5. 드라이브 `gguf_lr0.0002_ep2/` 에서 `.gguf` 를 내려받는다
6. [ollama.com/download](https://ollama.com/download) 설치
7. `Modelfile` 을 만들고 등록한다
   ```
   FROM ./unsloth.Q4_K_M.gguf
   PARAMETER temperature 0
   PARAMETER num_predict 160
   ```
   ```
   ollama create pawnote -f Modelfile
   ```
8. **200건 재평가 — 변환에서 성능이 안 떨어졌는지 본다. 90.0%가 유지돼야 한다**
   ```
   python src/predict.py --target ft --input data/test.jsonl \
       --engine ollama --model pawnote --out data/pred_ollama.jsonl
   python src/evaluate.py --gold data/test.jsonl --pred data/pred_ollama.jsonl \
       --details data/eval_ollama.jsonl
   ```
   - ⚠ **`<think>` 블록이 Colab과 같은 모양인지 확인한다.** 블록 안에 중괄호가 들어가면 채점 규칙상 파싱 실패가 된다. Colab 실측은 200건 전부 빈 블록이었다
9. **CPU-only 지연** — `OLLAMA_NUM_GPU=0` 으로 서빙하고 8번을 다시 돌린다
10. **1,000건당 비용** — **(a) 계산 자원 환산으로 P2 판정** · (b) 사업자 부담은 서사용 (Day 0 확정, 사후 변경 금지)

**막히면 어디서 막혔는지가 중요하다.** ⑨ 변환 실패는 Unsloth 버전 문제일 때가 많고, 그때는 `quantization_method='q8_0'` 으로 한 번 해보고 크기를 따로 적는다.

### 데이터를 보고 판단할 보류 항목

**셋 다 2,400건 실측으로 닫혔다. 전부 "그대로 둔다"이고, 근거가 숫자로 남았다.**

- ~~**증상 라벨 형태**~~ → **서술어형 유지.** 폐기 28건 중 `symptom`이 10건으로 1위이긴 하나 **전체의 0.4%**다. 명사형 통일이나 enum 전환은 `log_symptom`을 "담을 칸이 없는 나머지 전부"로 정의한 결정 15와 충돌한다 — 열린 집합이어야 한다. 0.4%를 아끼려고 툴 정의를 흔들지 않는다
- ~~**한 글자 시드 13개**~~ → **그대로 둔다.** 우려했던 폐기 쏠림이 없었다 — 원인 슬롯에 `record_type` 0건, `time` 3건뿐이다
- ~~**약 이름과 용량의 정합**~~ → **제약을 넣지 않는다.** 조합 64종 중 어긋난 것이 `("넥스가드","1cc")`·`("넥스가드","두 방울")` **2건(투약 171건의 1.2%)**뿐이다. 라벨이 표층이라 채점에는 무해하고, 제약을 넣으면 `name`↔`dose` 매핑 표를 유지해야 해서 비용이 이득보다 크다

### 잊으면 반나절 날아가는 것

- ~~**학습 데이터로 바꿀 때 `hint` 를 통째로 버린다**~~ → **코드로 막았다.** [build_sft.py](src/build_sft.py) `leak_check()` 가 user 메시지에 12개 툴 이름이 있으면 **멈춘다.** 주석으로 적어두면 언젠가 안 지켜진다
- **`ft` 프롬프트를 고치면 학습 데이터를 다시 만든다** — [predict.py](src/predict.py) `build_messages()` 하나를 양쪽이 쓰므로 코드는 안 어긋나지만, **이미 만들어둔 `sft_*.jsonl` 은 그 시점의 사본이다.** 프롬프트를 손대면 `build_sft.py` 를 다시 돌린다
- **Day 3 진입 직후 dtype 확인** — `bnb-4bit`의 compute dtype이 `bfloat16`인데 T4는 bf16 미지원. `dtype=torch.float16` · `fp16=is_bfloat16_supported()`로 명시. 여기서 막히면 그날 GPU 할당량이 통째로 날아간다

### 판단이 필요해지면 참조할 곳

- 값 도메인의 단일 원본은 [src/tools.py](src/tools.py). 문서 표가 아니라 코드를 본다
- 경계 케이스 판정은 결정 13·15·16 ([context-notes.md](context-notes.md) §3)
- 새 경계 케이스를 만나면 **한 문장으로 정의되는 규칙**을 만들고 `labeling-guide.md`에 적는다
