# Checklist — PawNote

Day별 실행 목록. 완료하면 `[x]`로 바꾼다.
설계 근거는 [context-notes.md](context-notes.md), 규격은 [CLAUDE.md](CLAUDE.md).

**GPU 표시** — 🟢 GPU 불필요(로컬) · 🔴 Colab GPU 필요
무료 티어 할당량이 자원이므로 🟢 작업을 GPU 세션에서 하지 않는다.

**축소 트리거** — 작업 부담이 커지면 툴 12개 → 10개로 줄인다. 순서는 `log_activity` → `log_weight`.

---

## Day 0 — 준비 (2시간 이내) 🟢

- [x] 모델 후보 3개 조사 — Unsloth 지원 목록에서 4B급 이하 확인
- [x] 우선순위 1~3(T4 QLoRA 가능 · Unsloth 지원 · GGUF 변환 가능) 통과 여부만 확인하고 **하나로 확정**
  - **`Qwen3-4B-Instruct-2507`** — 학습 `unsloth/Qwen3-4B-Instruct-2507-unsloth-bnb-4bit`, 서빙 `-GGUF`
  - Qwen3.5는 QLoRA 비권장·bf16 중심이라 탈락, Gemma 4는 VRAM 초과·멀티모달이라 제외
- [x] 확정 모델명과 선정 이유를 [context-notes.md](context-notes.md) §3 결정 7에 1줄 추가
- [x] 저장소 초기화, `.gitignore` (데이터·체크포인트·`.env` 제외)
- [ ] OpenAI API 키 `.env` 설정 — `.env.example` 복사해서 채우기
- [ ] 구글 드라이브에 프로젝트 폴더 생성 (체크포인트 저장용)

> 모델 비교에 2시간 넘게 쓰지 말 것. 진짜 성능 비교는 Day 3이 겸한다.
> **툴 호출을 이미 잘하는 모델은 오히려 피한다** — base가 잘하면 개선폭이 안 나온다.

---

## Day 1 — 툴 스펙 + 데이터 생성기 🟢

### 툴 스펙 확정

- [x] 툴 12개 정의 — 이름·파라미터·타입·설명 → [src/tools.py](src/tools.py)
- [x] 각 파라미터의 값 도메인 정의
  - [x] `record_type` / `care_type` / `activity_type` / `metric` 의 허용값 목록 (+ `excretion_type`·`condition`·`severity`·`due_care_type`)
  - [x] `amount`는 자유 문자열 허용 ("반 그릇", "한 줌" 같은 모호 단위가 실제 발화)
  - [x] `caregiver`는 자유 문자열, 미언급 시 `null`
  - [x] **시간 계열(`time`·`since`·`until`·`date`·`period`)은 표층 표현 그대로** — `"아침"`, `"저번주 화요일"` (결정 9)
  - [x] **수량 계열(`amount`·`duration`·`weight`)도 표층 그대로** — 숫자형 폐지 (결정 11)
  - [x] **enum은 영문 키로 정규화** (결정 12)
  - [x] 필수 파라미터 지정 — 확정 못 하면 `ambiguous`
- [x] 시간 표현 목록 정리 — 모델이 뱉을 수 있는 표현의 범위 ("아침", "점심때", "어제 밤", "저번주 화요일", "3일 전") → [src/lexicon.py](src/lexicon.py) 6종 56개
- [ ] **정규화 함수 작성** (`normalize_time`) — 표층 표현 + 기준 시각 → ISO. 모델이 아니라 앱 코드 쪽. Day 7의 A안 실험에서 라벨 생성에도 재사용한다
- [ ] **정규화 함수 작성** (`normalize_amount`) — `"5킬로 200"` → `5.2`, `"반 그릇"` → 비율. 결정 11의 짝

### 데이터 생성 파이프라인

- [ ] **Tool Call 랜덤 생성기** 작성 — 파라미터 조합을 균등 샘플링
- [x] 어휘 시드 사전 작성 → [src/lexicon.py](src/lexicon.py) (열린 집합) + [src/tools.py](src/tools.py) `ENUMS` (닫힌 집합 110개)
  - [x] 반려동물 호칭 (이름·애칭·"우리 애"·"강아지")
  - [x] 사료·간식·영양제 표현
  - [x] 모호 단위 ("반 그릇", "한 줌", "조금", "많이")
  - [x] 케어 항목 은어 (심장사상충·구충제·항문낭·발톱)
  - [x] 증상 표현 — 미묘한 통증 신호(하품·입술 핥기) 포함
  - [x] 금지 목록 — GPT가 기대는 상투 표현 차단
- [x] 화자 페르소나 목록 (급한 메모체 / 존댓말 / 반말 일기체 / 오타 섞인 입력 / 질문형 / 수다형) — 6종
- [ ] **역방향 생성 프롬프트** 작성 — Tool Call → 자연스러운 한국어 발화
- [ ] **라벨 재정렬 검증 단계 작성** (결정 14) — 표층 라벨이 발화에 실제로 있는지 확인하고, 없으면 라벨을 발화 쪽 표현으로 고친다. 특정 불가면 폐기
  - [ ] 재정렬률·폐기율을 기록 — 높으면 생성 프롬프트가 라벨을 무시한다는 신호
- [ ] 소량(30건) 생성 테스트 → 발화가 실제 보호자 말투인지 확인 후 프롬프트 수정
- [ ] **`labeling-guide.md` 시작** — 경계 케이스 판정 규칙

### 라벨링 가이드에 들어가야 할 경계 케이스

- [ ] "설사했어" → `log_excretion`인가 `log_symptom`인가
- [x] "구충제 줬어" → `log_medication`인가 `log_care`인가 → **`log_medication`. 약이면 전부** (결정 13)
- [x] "영양제 챙겨줬어" → `log_medication`인가 `log_meal`인가 → **`log_medication`** (결정 13)
- [ ] "심장사상충 약 언제야" → `get_records`(과거)인가 `get_care_due`(미래)인가
- [ ] "요즘 물 얼마나 마셔?" → `get_records`인가 `get_trend`인가
- [ ] "병원 갈 건데 최근 기록 좀" → `get_vet_summary`인가 `get_records`인가
- [ ] "산책하다 자꾸 주저앉아" → `log_activity`인가 `log_symptom`인가

**완료 기준** — 30건 샘플이 실제 보호자 말투로 읽히고 서로 충분히 다르다.

---

## Day 2 — 데이터 완성 + 평가 스크립트 + 베이스라인 1 🟢

### 데이터 생성

- [ ] 2,400건 생성 (학습 2,000 / 검증 200 / 테스트 200)
- [ ] 유형 비중 확인 (합계 100%) — 단순 22% / 파라미터 다중 28% / 어휘 미스매치·혼동 28% / 시제·맥락 판별 10% / out_of_scope 7% / ambiguous 5%
- [ ] `caregiver` 언급이 전체의 약 20%에 교차 분포하는지 확인
- [ ] 12개 툴에 데이터가 고르게 분포하는지 확인 (툴당 최소 100건)
- [ ] 임베딩 유사도 0.9 이상 중복 제거
- [ ] **`distinct-2` 다양성 측정** — 수치 기록 (개선 전후 비교용)

### 검수 (건너뛰기 금지)

- [ ] **테스트 200건 사람이 눈으로 검수** — 정답이 틀린 시험지로 재면 모든 숫자가 무효
- [ ] 검수 중 발견한 경계 케이스를 `labeling-guide.md`에 추가
- [ ] `out_of_scope`에 **진단 요청**("우리 개 무슨 병이야?", "이거 위험해?")이 포함됐는지 확인

### 평가 스크립트

- [ ] 7분류 오답 분해 채점기 작성
  - [ ] 완전 일치
  - [ ] 툴 정답 · 파라미터 오류
  - [ ] 툴 오류 (존재하는 툴)
  - [ ] **툴 환각** (12개 목록에 없는 이름)
  - [ ] **스키마 위반 · 파싱 실패**
  - [ ] 과잉 거절 (툴 있는데 null)
  - [ ] 놓친 거절 (툴 없는데 호출)
  - [ ] 시간 계열 파라미터는 **표층 문자열 일치**로 채점 (결정 9 — ISO 변환·허용 오차 로직 불필요)
- [ ] 난이도 층별·툴별 분해 집계 기능
- [ ] 지연 측정(p50/p95), 토큰 수·비용 계산 유틸
- [ ] 비용 계산은 **(a) 계산 자원 환산**과 **(b) 사업자 부담** 두 가지를 따로 산출 — P2 판정은 (a)로 한다

### 베이스라인 1

- [ ] **GPT-4.1 + 툴 목록 프롬프트**로 테스트 200건 측정
- [ ] 결과 표 기록 — 이 시점에 "GPT는 몇 점"이 손에 있어야 한다

**완료 기준** — GPT-4.1 점수와 오답 분해표가 나왔다. **여기까지만으로도 폴백 프로젝트가 성립한다.**

---

## Day 3 — Colab 진입, 베이스라인 2 + 첫 학습 🔴

- [ ] Colab 노트북 생성, 구글 드라이브 마운트
- [ ] T4 할당 확인, Unsloth 설치
- [ ] **⚠ 최우선 — dtype 확인.** bnb-4bit 설정의 compute dtype이 `bfloat16`인데 T4는 bf16 미지원. fp16 경로로 떨어지는지 모델 로드 직후 확인한다. 여기서 막히면 그날 할당량이 날아간다
- [ ] **베이스라인 2 — base 모델 + few-shot으로 테스트 200건 측정**
  - [ ] 툴 스펙 포함 버전 (공정 비교용)
  - [ ] 결과 기록 — 파인튜닝 기여도 증명의 기준선
- [ ] 학습 데이터를 **툴 스펙 미포함 형식**으로 변환 (설계 결정 2)
- [ ] QLoRA 학습 1회 실행 (2,000건, 기본 하이퍼파라미터)
- [ ] **체크포인트를 구글 드라이브에 저장** — 세션 끊기면 소실
- [ ] 학습 손실 곡선 확인

**완료 기준** — base few-shot 점수가 나왔고, 학습이 한 번 끝까지 돌았다.

---

## Day 4 — 학습 반복 + 판정 🔴

- [ ] 학습률 2~3개로 재학습 (광범위 탐색 금지, GPU 시간 소모가 큼)
- [ ] 검증셋 기준 최적 체크포인트 선택
- [ ] 테스트 200건 평가 → 7분류 오답 분해표
- [ ] **학습셋으로도 평가** (경우 B 판별용)
- [ ] 난이도 층별·툴별 정확도 분해
- [ ] 혼동 쌍별 오답률 확인 (어떤 쌍이 제일 어려웠는가)

### ⚠ 폴백 판정 지점

- [ ] 학습이 정상 동작하는가 판정
  - **정상** → Day 5로
  - **실패** → 폴백 발동. 프로젝트를 *"소형 모델로 자연어 케어 기록을 구조화할 수 있는가 — 프롬프트 접근의 한계 분석"* 으로 재정의하고 Day 6으로 건너뜀

**완료 기준** — P1 기준(완전 일치율 +15%p, 스키마 위반 5% 미만, 툴 환각 2% 미만) 달성 여부가 판정됐다.

---

## Day 5 — 서빙 + 속도·비용 측정 🔴🟢

- [ ] 어댑터 병합 후 **GGUF 변환**
- [ ] Ollama로 로컬 서빙
- [ ] 서빙 상태에서 테스트 200건 재평가 (변환 과정에서 성능이 떨어지지 않았는지 확인)
- [ ] **지연 p50/p95 측정**
- [ ] **CPU-only 추론 지연 측정** — 온디바이스 타당성의 간접 근거. GGUF 파일 크기도 함께 기록. 실기기 벤치마크는 범위 밖임을 README에 명시
- [ ] **입력 토큰 수 비교** — GPT-4.1(툴 스펙 포함) vs 파인튜닝 모델(미포함). 설계 결정 2의 효과 정량화
- [ ] 1,000건당 비용 계산 — **(a) 계산 자원 환산**(P2 판정용) · **(b) 사업자 부담**(서사용) 두 가지

**완료 기준** — 3자 비교표의 모든 칸이 채워졌다.

---

## Day 6 — 결과 정리 🟢

### 결과물

- [ ] **3자 비교표** (GPT-4.1 / base few-shot / 파인튜닝)
- [ ] **7분류 오답 분해표** — 3열 비교
- [ ] 그래프 2~3개 (정확도 비교, 오답 유형 분포, 비용·지연)
- [ ] **실패 사례 분석** — 파인튜닝 모델이 틀린 케이스를 유형별로 뽑아 원인 서술
- [ ] Success Criteria P0/P1/P2 달성 여부 명시

### README

- [ ] **왜 만들었는가** — 실태조사·투약 연구 숫자 인용, 기존 앱과의 차별점
- [ ] **사용자 조사 → 툴 도출 과정** ([context-notes.md](context-notes.md) §2 요약). `log_water`·`get_vet_summary`·`caregiver`가 조사에서 나왔다는 점 명시
- [ ] **범위 결정과 근거** — 순차 다중 호출을 왜 뺐는가
- [ ] **설계 결정** — 툴 스펙 미포함 학습과 그 트레이드오프 (툴 변경 시 재학습 필요)
- [ ] **모델과 규칙의 역할 분리** — 왜 알림은 LLM이 아닌가
- [ ] **안전 경계** — 기록·조회 전용, 진단·처방은 범위 밖
- [ ] 데이터 생성 방법론 + 다양성 측정 수치
- [ ] 라벨링 가이드
- [ ] 평가 방법과 결과
- [ ] **한계와 다음 단계** — MCP 연동 2단계 계획

**완료 기준** — 프로젝트를 모르는 사람이 README만 읽고 가치를 이해한다.

---

## Day 7 — 버퍼 🟢🔴

무료 티어가 잠기거나 앞 단계가 밀린 경우를 위한 예비일. 여유가 있으면 우선순위 순으로.

- [ ] **학습곡선 실험** (250 / 1,000 / 2,000건) — 가장 값어치 있음. 경우 B 원인 판별을 겸함
- [ ] **A안 대조 실험 — 시간 정규화를 모델에 맡겨본다** (결정 9 검증)
  - [ ] `normalize_time`으로 B 라벨 → A 라벨 코드 변환 (GPT 재호출·재검수 불필요)
  - [ ] 입력에 기준 시각 프리픽스 부착 후 재학습
  - [ ] 완전 일치율 차이 측정 → 결정 9를 주장에서 측정으로 승격
  - [ ] 한계 명시 — 재는 것은 "우리가 정한 정규화 관례의 학습 가능성"이지 절대적 시간 이해력이 아님
- [ ] 병렬 독립 다중 호출 지원 ("물이랑 밥 줬어" → 배열 출력)
- [ ] 능동 알림 규칙 엔진 (B층) — 날짜 계산 기반, LLM 아님
- [ ] 1.5B vs 4B 비교 — "얼마나 작아도 되는가"
- [ ] `ambiguous`일 때 무엇이 부족한지 되묻는 질문 생성
- [ ] 저장소 공개 정리 (데이터 샘플, 재현 스크립트)

---

## 진행 로그

날짜별로 실제로 한 일과 막힌 지점을 남긴다. **새 세션에서 이어서 작업할 때 이 표를 먼저 읽는다.**

| 날짜 | 한 일 | 막힌 지점 · 결정 |
| --- | --- | --- |
| 2026-08-31 | 주제 확정(자연어 → Tool Call 파인튜닝), 도메인을 사내 툴 → 반려동물로 전환 | — |
| 2026-08-31 | 사용자 조사 수행 — 국내 실태조사·투약 준수 연구·수의사 문진·기존 앱 4갈래 | 조사로 `log_water`·`get_vet_summary`·`caregiver` 발견. 툴 10 → 12개 |
| 2026-08-31 | 설계 문서 3종 작성 완료 | 모델 미확정 — Day 0에 선정 |
| 2026-09-01 | 문서 검토에서 설계 충돌 발견 — 결정 2(입력이 발화뿐)와 절대 시각 출력이 양립 불가 | **결정 9 신설. B안(표층 표현 유지 + 앱 코드 정규화) 확정.** A안은 Day 7 대조 실험으로 이관 |
| 2026-09-01 | 데이터 유형 비중 합계가 97%였던 것 수정 | 어휘 미스매치·혼동 25% → 28%로 보정 |
| 2026-09-01 | 온디바이스 주장에 측정 계획이 없던 문제 보완 | Day 5에 CPU-only 지연 측정 추가. 실기기 벤치마크는 범위 밖으로 명시 |
| 2026-09-01 | P2의 비용 기준이 정의 없이 떠 있던 문제 확정 | (a) 계산 자원 환산으로 판정, (b) 사업자 부담은 서사용으로 분리 |
| 2026-09-01 | 프롬프트·파인튜닝·RAG 개념 정리 추가 | context-notes §5-1 신설. 용어집은 §5-2로 이동 |
| 2026-09-01 | "품종 지식이 필요하지 않나" 질문에서 RAG 검토 | **결정 10 신설 — RAG 미사용.** 품종 표준값은 참조 테이블(C층), MVP 범위 밖 |
| 2026-09-01 | 프로젝트 정의·범위 명문화 | **context-notes §0 신설.** 사용자는 보호자 한 명(수의사는 사용자 아님), 7일에 만드는 건 모델 하나뿐, 목표는 제품/기술 두 겹 |
| 2026-09-01 | Day 0 착수 — `.gitignore`·`.env.example` 생성 | `.env` 키 입력과 드라이브 폴더는 수동 |
| 2026-09-01 | **툴 12종 스펙 확정 → [src/tools.py](src/tools.py)** 단일 원본 | 숫자형 파라미터가 결정 9와 충돌하는 것을 발견. **결정 11 신설(수량도 표층)**, 파라미터명 `amount_ml`·`duration_min`·`weight_kg` → `amount`·`duration`·`weight` |
| 2026-09-01 | enum 값을 영문으로 둘지 한글로 둘지 판단 | **결정 12 신설** — 닫힌 집합은 영문 정규화, 열린 집합만 표층. 결정 9의 과잉 적용을 막는 경계선 |
| 2026-09-01 | `log_medication` / `log_care` 경계가 문서상 겹쳐 있던 문제 | **결정 13 신설** — 약이면 전부 medication, care는 위생 관리. 경계 케이스 2건 해소. 예방약은 medication으로 기록되고 `get_care_due`로 조회되는 비대칭을 명문화 |
| 2026-09-01 | 결정 3과 결정 9·11의 정합성 구멍 발견 — 표층 라벨과 생성 발화가 어긋나면 정답 동작도 오답 처리됨 | **결정 14 신설** — 생성 후 라벨 재정렬 검증. 발화가 아니라 라벨을 고친다 |
| 2026-09-01 | 채점 규칙이 정의 없이 떠 있던 문제 | CLAUDE.md에 **직렬화 규칙 · 7분류 판정 순서 · 값 비교 정규화** 확정. 코드펜스는 허용하되 사용률을 별도 지표로 기록 |
| 2026-09-02 | **모델 확정 — `Qwen3-4B-Instruct-2507`** | 최신 세대가 탈락. Qwen3.5는 Unsloth가 QLoRA를 비권장하고 bf16·Mamba 커널이라 T4 부적합, Gemma 4 E4B는 LoRA 17GB로 T4 초과 + 멀티모달. Gemma 3 4B와 최종 비교에서 **텍스트 전용·thinking 분리 체크포인트**로 Qwen3 채택. Day 3에 bf16→fp16 dtype 확인을 최우선 항목으로 추가 |
